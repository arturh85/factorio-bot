name: 'LDOC'
description: 'Generate Lua Api Docs'
inputs:
  config-path:  # path to lua
    description: 'Who to greet'
    required: true
    default: 'World'
runs:
  using: 'docker'
  image: 'docker://anibali/ldoc'
  args:
    - -c
    - ${{ inputs.config-path }}