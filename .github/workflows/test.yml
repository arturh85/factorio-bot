name: "tests"
on:
  push:
    branches:
      - master

jobs:
  test-tauri:
    runs-on: ubuntu-latest
    steps:
      - name: 🔥 checkout code
        uses: actions/checkout@v3

      - name: 🚧 build lua api docs (ubuntu only)
        uses: ./.github/actions/ldoc
        with:
          config-path: docs/lua/config.ld
          source-path: docs/lua/src
