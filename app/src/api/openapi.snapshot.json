{
  "components": {
    "schemas": {
      "ActionFailure": {
        "description": "A structured failure, carried *beside* [`EventKind::ActionSettled`]'s\n`error` string rather than instead of it: the string is what a person\nreads when they open the log, `kind` is what a query groups by, and\nreplacing one with the other loses whichever audience it was serving.",
        "properties": {
          "detail": {
            "description": "Free-text detail, e.g. the item name for [`FailureKind::MissingItem`].",
            "type": [
              "string",
              "null"
            ]
          },
          "kind": {
            "$ref": "#/components/schemas/FailureKind"
          }
        },
        "required": [
          "kind"
        ],
        "type": "object"
      },
      "AddResearchBody": {
        "properties": {
          "tech": {
            "type": "string"
          }
        },
        "required": [
          "tech"
        ],
        "type": "object"
      },
      "AppSettings": {
        "properties": {
          "factorio": {
            "$ref": "#/components/schemas/FactorioSettings"
          },
          "gui": {
            "$ref": "#/components/schemas/GuiSettings"
          },
          "restapi": {
            "$ref": "#/components/schemas/RestApiSettings"
          }
        },
        "required": [
          "factorio",
          "restapi",
          "gui"
        ],
        "type": "object"
      },
      "BotSample": {
        "properties": {
          "crafting_queue": {
            "description": "Queue *length*, not its contents: the contents are large, change every\ntick, and answer no question we have.",
            "format": "int32",
            "minimum": 0,
            "type": "integer"
          },
          "id": {
            "format": "int32",
            "minimum": 0,
            "type": "integer"
          },
          "inventory": {
            "additionalProperties": {
              "format": "int32",
              "minimum": 0,
              "type": "integer"
            },
            "description": "Item name to count. Built mod-side from Factorio 2.0's\n`get_contents()`, which returns an array of `{name, count, quality}`.",
            "propertyNames": {
              "type": "string"
            },
            "type": "object"
          },
          "mining": {
            "description": "Name of whatever the bot's character is mining, or `None` when it\nisn't. Resolved mod-side from `LuaControl.mining_state.position` via\n`LuaSurface.find_entities_filtered` -- *not* from\n`LuaEntity.mining_target`, which belongs to mining drills, not\ncharacters, and raises when read off one (see `sample_bots` in\n`mods/BotBridge/control.lua`).",
            "type": [
              "string",
              "null"
            ]
          },
          "position": {
            "$ref": "#/components/schemas/Position",
            "description": "As the game reports it. A tile centre stays `-40.5`; nothing rounds."
          }
        },
        "required": [
          "id",
          "position",
          "inventory",
          "crafting_queue"
        ],
        "type": "object"
      },
      "Bounds": {
        "properties": {
          "bottom": {
            "format": "double",
            "type": "number"
          },
          "left": {
            "format": "double",
            "type": "number"
          },
          "right": {
            "format": "double",
            "type": "number"
          },
          "top": {
            "format": "double",
            "type": "number"
          }
        },
        "required": [
          "left",
          "top",
          "right",
          "bottom"
        ],
        "type": "object"
      },
      "Calibration": {
        "description": "One `(host clock, encoder clock)` observation.\n\nThe first pair fixes the video's zero -- ffmpeg's PTS starts when it\ncaptured its first frame, which is some unknown time after we spawned it\n(process start, X11 connection, window lookup, first grab), and guessing\nthat offset is the mistake that makes every seek wrong by a constant nobody\ncan see. The second pair, at stop, fixes the *rate*, which is a check rather\nthan a parameter.",
        "properties": {
          "host_wall_ms": {
            "description": "Milliseconds since the recorder's epoch -- the same epoch\n[`TickSample::wall_ms`] counts from.",
            "format": "int64",
            "minimum": 0,
            "type": "integer"
          },
          "out_time_ms": {
            "description": "ffmpeg's own `out_time_us`, in milliseconds.",
            "format": "int64",
            "minimum": 0,
            "type": "integer"
          }
        },
        "required": [
          "host_wall_ms",
          "out_time_ms"
        ],
        "type": "object"
      },
      "CheatItemBody": {
        "properties": {
          "count": {
            "format": "int32",
            "minimum": 0,
            "type": "integer"
          },
          "name": {
            "type": "string"
          },
          "player_id": {
            "$ref": "#/components/schemas/u8"
          }
        },
        "required": [
          "name",
          "count",
          "player_id"
        ],
        "type": "object"
      },
      "CheatTechnologyBody": {
        "properties": {
          "tech": {
            "type": "string"
          }
        },
        "required": [
          "tech"
        ],
        "type": "object"
      },
      "Delivery": {
        "description": "What a bot's hands put into a machine or a chest, on\n[`EventKind::ActionDispatched::delivery`].\n\n**The planner's intent, not the game's answer**, on the same terms as\n[`EventKind::ActionDispatched::target`]: it travels from\n`ActionKind::Insert`'s own fields, which is what the run asked for. The\naction's settle says whether the game did it; nothing here does.\n\nOne variant of one enum reaches this struct -- `ActionKind::Insert` --\nand it covers every hand delivery the planner can express, fuel included:\na `fuel` action IS an `Insert` into `InventorySlot::Fuel`, and the label\nis the only place the two are spelled differently. `ActionKind::Remove`\n(a `take`) deliberately writes nothing here: it moves material *out* of a\nmachine and into a bot, which credits no production.",
        "properties": {
          "count": {
            "description": "How many. A count of items, never of stacks.",
            "format": "int32",
            "minimum": 0,
            "type": "integer"
          },
          "entity": {
            "description": "The prototype name of what received it -- `stone-furnace`,\n`burner-mining-drill`, `wooden-chest`. Needed because the same coal\nbuys 26.7 s in a drill and 44.4 s in a furnace, so a fuel delivery\ncannot be converted into output without knowing which.",
            "type": "string"
          },
          "item": {
            "description": "What was put in.",
            "type": "string"
          },
          "slot": {
            "description": "Which inventory: the planner's own `InventorySlot` name, as\n`goal.plan`'s steps publish it (`fuel`, `furnace_source`,\n`assembler_input`, `chest`, ...) rather than Factorio's unified\n`defines.inventory` key, so a reader can still tell a furnace's input\nfrom an assembler's.",
            "type": "string"
          }
        },
        "required": [
          "item",
          "count",
          "entity",
          "slot"
        ],
        "type": "object"
      },
      "Divergence": {
        "properties": {
          "entity": {
            "$ref": "#/components/schemas/EntitySnapshot"
          },
          "only_in": {
            "description": "Which side has it: `\"game\"` or `\"model\"`.",
            "type": "string"
          }
        },
        "required": [
          "entity",
          "only_in"
        ],
        "type": "object"
      },
      "EntitySnapshot": {
        "properties": {
          "direction": {
            "description": "Factorio 2.0 uses 16 values. For an inserter this is the side it PICKS\nUP from, not the side it drops into.",
            "format": "int32",
            "minimum": 0,
            "type": "integer"
          },
          "name": {
            "type": "string"
          },
          "position": {
            "$ref": "#/components/schemas/Position",
            "description": "Unrounded. A resource sits at a tile centre and stays there."
          }
        },
        "required": [
          "name",
          "position",
          "direction"
        ],
        "type": "object"
      },
      "ErrorResponse": {
        "description": "Error body returned by every failing endpoint.\n\n`status` is the HTTP status [`IntoResponse`] answers with. It is not part of\nthe JSON body — it would be redundant with the response line, and clients\ngenerated from the OpenAPI schema must not see a field the server never\nserializes — so it is `#[serde(skip)]`ped and defaults to 400 on both\nconstruction and deserialization.",
        "properties": {
          "code": {
            "format": "int32",
            "minimum": 0,
            "type": "integer"
          },
          "message": {
            "type": "string"
          },
          "running_job_id": {
            "description": "The job holding the single execution slot, on the `409` from\n`POST /api/v1/scripts/execute`. Skipped when absent so every other\nerror body is byte-for-byte what it was before this field existed --\na client that switched on the presence of a key would otherwise start\nseeing `\"running_job_id\": null` on unrelated failures.",
            "type": [
              "string",
              "null"
            ]
          }
        },
        "required": [
          "message",
          "code"
        ],
        "type": "object"
      },
      "Event": {
        "allOf": [
          {
            "$ref": "#/components/schemas/EventKind"
          },
          {
            "properties": {
              "tick": {
                "format": "int64",
                "minimum": 0,
                "type": "integer"
              }
            },
            "required": [
              "tick"
            ],
            "type": "object"
          }
        ],
        "description": "One line of `events.jsonl`.\n\n`tick` is *when this happened*; a duration is always `elapsed_ticks`. The\ntwo were briefly both called `ticks`, which reads fine until someone plots\nit.\n\nThere used to be a `wall_ms` here too, and it is gone on purpose --\nsee [`RunRecorder::record`] for the full account. In short: it was\nstamped from `self.started.elapsed()` at the moment `record()` was\n*called*, but `record.actions()`/`record.teleports()`/`record.refusals()`\n(`crates/scripting_lua/src/globals/record.rs`) are each called once per\nsupervisor \"ran\" transition, i.e. once an entire multi-bot plan has\nfinished executing -- which can be minutes of real time and thousands of\nticks after the earliest event in that same call flushed. Every event in\nsuch a batch therefore got the wall clock reading from the moment the\n*batch* was written, not the moment each event actually happened, which\nis exactly the `docs/superpowers/notes/2026-09-02-inventory-shortfall.md`\nfinding of `wall_ms` jumping `33780 -> 738866` while `tick` moved only\n`10`. Making it mean \"real time this event happened\" would require the\nexecutor to capture `SystemTime::now()` at the point it actually\ndispatches/observes each action (`ExecutionLog::start`/`observe`/\n`start_walk`/`observe_walk` in `crates/executor/src/log.rs`, called from\n`crates/executor/src/run.rs`) and carry that across the mlua boundary\n(`build_observation` in `crates/scripting_lua/src/globals/goal/run.rs`)\ninto `record.actions()` -- a cross-crate change to the executor's public\nattempt/walk types and every test that asserts their shape, not a fix to\na wrong timestamp. Nothing reads the field today (`app/src/lib/\nrunTimeline.ts`, `runDiff.ts` and the analysis page never touch it; the\nonly Rust readers were test-fixture helpers), so there is no consumer to\npreserve, and a batch-stamped column that looks like a per-event\ntimestamp is worse than no column at all. `#[serde(default)]` is not\nneeded for removal -- an old `events.jsonl` line's leftover `\"wall_ms\"`\nkey deserializes as an ordinary ignored extra field (serde's default for\na struct with no `deny_unknown_fields`), so every run already on disk\nstays readable."
      },
      "EventKind": {
        "description": "What happened. Internally tagged as `kind`, so a line is one flat object.\n\n[`EventKind::Unknown`] is the `serde(other)` catch-all: a reader built\nbefore a kind existed must skip it rather than refuse the file, so an older\nviewer can still open a newer run.",
        "oneOf": [
          {
            "properties": {
              "bots": {
                "items": {
                  "format": "int32",
                  "minimum": 0,
                  "type": "integer"
                },
                "type": "array"
              },
              "factorio": {
                "type": [
                  "string",
                  "null"
                ]
              },
              "git": {
                "type": [
                  "string",
                  "null"
                ]
              },
              "kind": {
                "enum": [
                  "run_started"
                ],
                "type": "string"
              },
              "run_id": {
                "type": "string"
              },
              "seed": {
                "description": "Present-and-null when unknown, never absent: a key that is always\nthere says \"we looked\", where a missing key cannot be told apart\nfrom \"we never asked\".",
                "format": "int64",
                "minimum": 0,
                "type": [
                  "integer",
                  "null"
                ]
              }
            },
            "required": [
              "run_id",
              "bots",
              "kind"
            ],
            "type": "object"
          },
          {
            "properties": {
              "goal": {
                "type": "string"
              },
              "index": {
                "format": "int32",
                "minimum": 0,
                "type": "integer"
              },
              "kind": {
                "enum": [
                  "milestone_started"
                ],
                "type": "string"
              }
            },
            "required": [
              "index",
              "goal",
              "kind"
            ],
            "type": "object"
          },
          {
            "description": "A milestone was reached.\n\nCarries no duration on purpose: the event says *when*, and\n[`splits`] derives *how long* from the started/satisfied tick pair.\nIt briefly carried an `elapsed_ticks` the caller could not compute, so\nevery live run recorded a confident zero beside a split that had the\nreal number.",
            "properties": {
              "index": {
                "format": "int32",
                "minimum": 0,
                "type": "integer"
              },
              "iterations": {
                "format": "int32",
                "minimum": 0,
                "type": "integer"
              },
              "kind": {
                "enum": [
                  "milestone_satisfied"
                ],
                "type": "string"
              },
              "reason": {
                "$ref": "#/components/schemas/SatisfiedReason",
                "description": "Why no further work was needed. `run-1788277287-11819` closed a\nmilestone at the tick it started, with zero iterations, and the\nrecord could not say whether the world already had it or the\nplanner gave up empty-handed -- this field exists to answer that.\nDefaulted for every event recorded before it existed, via\n[`SatisfiedReason::unknown`] rather than the ordinary derived\ndefault, so an old record reads as \"we don't know\" and not as a\nguess at either real answer."
              }
            },
            "required": [
              "index",
              "iterations",
              "kind"
            ],
            "type": "object"
          },
          {
            "properties": {
              "best_steps": {
                "format": "int32",
                "minimum": 0,
                "type": [
                  "integer",
                  "null"
                ]
              },
              "index": {
                "format": "int32",
                "minimum": 0,
                "type": "integer"
              },
              "kind": {
                "enum": [
                  "milestone_stuck"
                ],
                "type": "string"
              },
              "last_error": {
                "type": [
                  "string",
                  "null"
                ]
              },
              "outcome": {
                "type": "string"
              }
            },
            "required": [
              "index",
              "outcome",
              "kind"
            ],
            "type": "object"
          },
          {
            "description": "The world as it stood when a milestone closed was written to disk, and\na later run can start from it. See [`savepoint`].\n\nRecorded only once the file is *finished* -- `game.server_save` returns\nlong before the engine has written anything, and a savepoint announced\non the strength of the call returning would be a claim about a file\nthat may never appear.",
            "properties": {
              "bytes": {
                "format": "int64",
                "minimum": 0,
                "type": "integer"
              },
              "file": {
                "description": "Path relative to the run directory, e.g.\n`savepoints/milestone-3.zip`. Relative because the archive is\nmoved and copied, and an absolute path in it would name a machine.",
                "type": "string"
              },
              "kind": {
                "enum": [
                  "savepoint_written"
                ],
                "type": "string"
              },
              "milestone_index": {
                "format": "int32",
                "minimum": 0,
                "type": "integer"
              },
              "wrote_ms": {
                "description": "Wall-clock milliseconds between asking and the file being complete.\nThe one number that says whether saving is cheap enough to do at\nevery milestone; it is wall clock rather than ticks because the\nengine writes outside the tick this was requested in.",
                "format": "int64",
                "minimum": 0,
                "type": "integer"
              }
            },
            "required": [
              "milestone_index",
              "file",
              "bytes",
              "wrote_ms",
              "kind"
            ],
            "type": "object"
          },
          {
            "description": "A savepoint was asked for and did not arrive.\n\nPresent so that a milestone with no savepoint can be told apart from a\nrun that never asked for one. Four separate mechanisms in this project\nhave been found reporting nothing while broken; a failed save is not\ngoing to be the fifth.",
            "properties": {
              "error": {
                "type": "string"
              },
              "kind": {
                "enum": [
                  "savepoint_failed"
                ],
                "type": "string"
              },
              "milestone_index": {
                "format": "int32",
                "minimum": 0,
                "type": "integer"
              }
            },
            "required": [
              "milestone_index",
              "error",
              "kind"
            ],
            "type": "object"
          },
          {
            "properties": {
              "bots": {
                "description": "The **roster the planner expanded this plan against**, ascending --\nevery bot it was allowed to give work to, not the bots it happened\nto use. Present-and-null when the caller did not say, never absent.\n\nTwo wrong answers have been recorded here, in opposite directions,\nand the field's history is the reason it now comes from the caller:\n\n1. **The bots in the steps.** That deleted exactly the bots a reader\n   is asking about -- a live run of four recorded `bots: [2]`,\n   indistinguishable from a run of one, and \"why did bot 4 do\n   nothing\" could not be asked of it at all.\n2. **The binding's ambient roster**, i.e. the bots the *process* was\n   started with. That is not the roster either, whenever a script\n   plans with `goal.plan{bots = ...}` -- which the shipped driver\n   does, from `rcon.players()`. Run 30 recorded `bots: [1, 2]` for a\n   plan made for `[2]` alone (bot 1 was invisible to\n   `rcon.players()` for the 750 ticks of freeplay's crash-site\n   cutscene, during which `LuaPlayer::character` is nil), so the\n   record asserted that a bot had been offered work it was never\n   offered. See `docs/superpowers/notes/2026-09-02-bot-one-idle.md`.\n\nThe only thing that knows the answer is whatever called\n`goal.plan`, and `plan.bots` is where it reads it. So the Lua\nbinding takes it as an argument and writes `null` when it is not\ngiven: a plan whose roster nobody stated is a plan whose roster this\nrecord does not know, and the ambient value is a plausible-looking\nsubstitute for it rather than a weaker version of it.\n\nThis is also the field to be most careful with. `plan_created` is\nwritten at planning time and carries the whole DAG, which makes it\nthe part of an archived run that stays trustworthy where outcomes do\nnot -- a lie here is worse than a lie in a record already known to\nbe suspect.",
                "items": {
                  "format": "int32",
                  "minimum": 0,
                  "type": "integer"
                },
                "type": [
                  "array",
                  "null"
                ]
              },
              "kind": {
                "enum": [
                  "plan_created"
                ],
                "type": "string"
              },
              "makespan": {
                "format": "int64",
                "minimum": 0,
                "type": "integer"
              },
              "milestone_index": {
                "format": "int32",
                "minimum": 0,
                "type": "integer"
              },
              "plan": {
                "description": "The steps the planner actually produced, in enough detail to draw\nthe DAG: who runs each one, what it waits on, and when the\nplanner expected it to start and finish. `#[serde(default)]` so a\nrun recorded before this field existed keeps opening -- it just\nhas nothing to draw here.",
                "items": {
                  "$ref": "#/components/schemas/PlannedStep"
                },
                "type": "array"
              },
              "steps": {
                "format": "int32",
                "minimum": 0,
                "type": "integer"
              }
            },
            "required": [
              "milestone_index",
              "steps",
              "makespan",
              "kind"
            ],
            "type": "object"
          },
          {
            "description": "A plan is being executed **right now**, and this is how far it has got.\n\n# The silence this exists to break\n\nEvery other event about a plan's execution is written *after* the whole\nbatch has finished: the driver calls `record.actions()`/`record.walks()`\non the supervisor's \"ran\" transition, once `goal.run` has returned\n(`scripts/factory_stage2.lua`). So between `plan_created` and the first\n`action_dispatched` the record says **nothing at all**, for however long\nthe batch takes -- sixteen minutes is an ordinary figure for a\nfour-bot plan.\n\nThat made two entirely different situations byte-identical from outside:\n\n  * a run executing a long plan perfectly well, and\n  * a run that planned and then dispatched nothing, ever.\n\n`run-1788465258-49050` is the second one being diagnosed and killed when\nit was in fact the first. Its last line was a `plan_created` for a\n152-step plan; fourteen minutes later it was killed as hung. The mod's\nown `script-output/botbridge/samples.jsonl` -- which nothing in the run\nrecord reads -- showed bot 1 crafting copper-cable and the force's\niron-plate production climbing at the moment of the kill. Nine of the\ntwenty-four runs archived at the time end on a `plan_created` with\nnothing after it, and not one of them can be told apart from that run.\n\n# What it is, and what it is not\n\nIt is a heartbeat with counters: written on a fixed wall-clock interval\nwhile a batch is in flight, carrying only facts that were true when it\nwas written. It states **no verdict** -- there is no `stalled` flag and\nno threshold anywhere in the writer, deliberately, because \"how long is\ntoo long\" depends on the batch and would need tuning, whereas\n`dispatched: 0` on a plan of 152 steps needs none: it is unambiguous at\nany duration.\n\nThe interval decides the *resolution* of the answer, never its\ncorrectness. A batch shorter than one interval writes none of these and\nis none the worse for it -- its `action_dispatched` lines arrive\nimmediately afterwards.\n\nIt names no milestone and no plan, because the thing that writes it --\nthe executor's side of `goal.start` -- knows neither: a milestone is a\nconcept of the supervisor script, and `milestone_index` reaches\n[`EventKind::PlanCreated`] from Lua. A reader attributes one of these to\nthe `plan_created` it follows in the log, which is exact **while one\nbatch runs at a time** -- the shipped drivers all wait on each run\nbefore planning the next. A script that ran two plans concurrently with\n`goal.start` would interleave two heartbeats with nothing to tell them\napart, and that is the case to fix here if it ever arises rather than to\npaper over in the reader.\n\nIts `tick` is the game's own clock, like every live event's (see\n[`RunRecorder::not_before`]) -- and unlike the batched\n`action_dispatched`/`action_settled` lines, which carry the tick each\naction really happened at rather than the moment they were flushed. The\nwall-clock durations below are on the variant rather than on\n[`Event`] for that exact reason: `Event::wall_ms` was removed because a\nbatched event's write time is not its happening time, and these two\nnumbers are honest only because this event *is* written at the moment\nit describes.",
            "properties": {
              "abandoned": {
                "description": "Actions the run decided not to dispatch because a predecessor did\nnot succeed, or the bot's walk halted it. **Not** in `dispatched`\nor `settled`: the game never saw them. This is the count that says\na batch is being truncated while it is still running -- 48 of 878\nin `run-1788914717-24351`, which nothing reported until the batch\nwas over and the number had become `pending`. `0` on lines\nwritten before the field existed.",
                "format": "int32",
                "minimum": 0,
                "type": "integer"
              },
              "bots_in_flight": {
                "description": "Which bots have an action in flight, ascending. Empty is not by\nitself a problem -- see `walks_dispatched`.",
                "items": {
                  "format": "int32",
                  "minimum": 0,
                  "type": "integer"
                },
                "type": "array"
              },
              "dispatched": {
                "description": "How many actions have been dispatched at least once, i.e. are no\nlonger `pending`. **This is the number the whole event is for.**\n`dispatched: 0` beside a `plan_created` with steps is a plan that\nis not being executed, whatever else the run looks like.",
                "format": "int32",
                "minimum": 0,
                "type": "integer"
              },
              "elapsed_ms": {
                "description": "Wall-clock milliseconds since the executor was handed this batch.\n\nWall clock, not ticks: the question this event answers is \"is\nanything happening\", and a stopped game is exactly the case where\nthe tick clock cannot answer it.",
                "format": "int64",
                "minimum": 0,
                "type": "integer"
              },
              "failed": {
                "description": "Of the settled, how many the game judged and refused.",
                "format": "int32",
                "minimum": 0,
                "type": "integer"
              },
              "ground_generate_calls": {
                "description": "**The disclosure for the one act in this project a player cannot\nperform.** How many times this batch asked the game to *create\nground* (`generate_chunks`), how many chunks that actually made,\nand how many of those asks failed.\n\nIt is here rather than in `provenance.json` for the reason\n[`EventKind::VisionMeasured`] gives: provenance is written once at\nrun start, before any survey has run, and the manifest exists only\nfor runs that finished -- while a killed run still generated its\nground. This event is flushed per line, so the disclosure survives\nthe kill.\n\n**Why it is needed at all.** A bot cannot walk into ungenerated\nground: the pathfinder refuses. A human crosses that edge by\nwalking and the engine makes the ground as they go; a bot steered\nthrough `request_path` cannot, so exploration asks for the ground\nexplicitly. The ask is clamped mod-side to the reveal a character\nstanding there would have got for free, but it still arrives\n*before* the bot does, which is a small piece of free vision and is\ntherefore counted rather than argued about. See\n`docs/superpowers/notes/2026-09-06-exploration.md`.\n\n**States no verdict.** `ground_generate_calls: 0` is a run that\nnever explored -- every run before 2026-09-06. Calls with zero\nchunks and zero failures is ground that already existed; failures\nequal to calls is the verb not working, which is the case that\nwould otherwise be invisible because the executor has no logger.\n\n`#[serde(default)]` so every run recorded before this existed still\nopens, and reads zero -- which for those runs is the truth.",
                "format": "int32",
                "minimum": 0,
                "type": "integer"
              },
              "ground_generate_failures": {
                "format": "int32",
                "minimum": 0,
                "type": "integer"
              },
              "ground_generated_chunks": {
                "format": "int32",
                "minimum": 0,
                "type": "integer"
              },
              "in_flight": {
                "description": "How many are in flight right now: dispatched, no verdict yet.",
                "format": "int32",
                "minimum": 0,
                "type": "integer"
              },
              "kind": {
                "enum": [
                  "batch_progress"
                ],
                "type": "string"
              },
              "lost": {
                "description": "Of the settled, how many were acknowledged and never answered.\nKept apart from `failed` here for the same reason\n[`EventKind::ActionSettled`] keeps them apart.",
                "format": "int32",
                "minimum": 0,
                "type": "integer"
              },
              "reach_corrections": {
                "description": "How many walks have come to rest outside the reach of the action\nthey served and needed a corrective step, cumulative since the\nactuator was built.\n\nA walk stops on the **outer** ring of its annulus — as far from the\ntarget as the action's reach allows, less\n[`crate::factorio::rcon::ARRIVAL_MARGIN`], which is worth\n8,200-10,200 ticks of a four-bot green run. That margin was\nmeasured (128 probe walks; worst overshoot 0.301 tiles) and\nmeasurement is not proof, so the executor re-checks reach after\nevery walk and this is how often it had to act.\n\n**It states no verdict either.** Zero says the margin is not being\ntested and could be tightened; a number that grows says it is too\nthin. Which of those is worth acting on is not decided here, and\nnothing compares this against a threshold.\n\n`#[serde(default)]` so a run recorded before the outer ring existed\nstill opens — and reads zero, which for such a run is the truth:\nits walks stopped on the inner ring and had no margin to be wrong\nabout.",
                "format": "int32",
                "minimum": 0,
                "type": "integer"
              },
              "settled": {
                "description": "How many have reached a verdict of any kind (succeeded, failed or\nlost).",
                "format": "int32",
                "minimum": 0,
                "type": "integer"
              },
              "since_last_dispatch_ms": {
                "description": "Wall-clock milliseconds since `dispatched` last went up, or since\nthe batch began when it has never gone up at all.\n\nDeliberately measured against *dispatches* and not against settles:\na bot waiting out a legitimate lag edge (a furnace working, machine\ntime the plan modelled) settles nothing and dispatches nothing, and\nthis number growing is the honest report of that. It is a\nmeasurement, not an accusation -- nothing here decides what value of\nit is too large.",
                "format": "int64",
                "minimum": 0,
                "type": "integer"
              },
              "total": {
                "description": "How many actions the plan has in total -- the denominator for\nevery count below. Actions only: walks have no `ActionId` and are\ncounted separately.",
                "format": "int32",
                "minimum": 0,
                "type": "integer"
              },
              "waiting": {
                "description": "**What each waiting step is waiting for**, longest wait first.\n\n# The counters name a bot; this names the work\n\nEvery field above is a number or a bot id, and that was enough to\nprove a run was alive and not enough to say what it was doing. On\n`2026-09-04` one action sat in flight for **eleven minutes**: the\nheartbeats showed frozen counters, `bots_in_flight: [n]`, `lost: 0`,\nand nothing else. Identifying which action it was needed a live\n`factorio-bot rcon -s localhost` query against the running game,\nand even then only by inference -- and a run that dies mid-batch\ncannot be queried at all, so the action would never have been\nidentified. That is not a hypothetical: **a batch's\n`action_dispatched` lines are written only after `goal.run`\nreturns**, so while a batch is in flight the dispatches are not in\nthe file yet. In that run the newest recorded dispatch was tick\n258,893 while the record's own latest tick was 260,531.\n\nSo each entry names the step (id, bot, the plan's own label, the\ntarget), the state it is in, and how long it has been in it -- all\nread from `factorio_bot_executor::ExecutionLog`'s wait registry,\nwhich the executor maintains as it enters and leaves each state.\n\n# Still no verdict\n\n`waiting_ms` is a measurement and nothing here compares it against\nanything. A 12,000-tick smelt and a reply that will never come both\nappear as a growing number; what separates them is\n[`WaitingStep::waiting_on`], not a threshold this writer applied.\n\n# Bounded, and it says when it truncated\n\nAt most [`MAX_WAITING_REPORTED`] entries, longest wait first, with\n`waiting_total` giving the true count. The longest wait is\ntherefore always present, which is the one a reader is looking for.\n\n# An empty list is a claim, and a checkable one\n\nEmpty means nothing is waiting. It is **contradicted** by\n`in_flight > 0`, because a dispatched action is by definition\nwaiting for its reply, and those two numbers come from different\nwriters -- the statuses and the wait registry. A heartbeat with\n`in_flight: 3` and an empty `waiting` is not a quiet run, it is a\nbroken reporter, and it is the one shape of failure this project\nhas hit four separate times.",
                "items": {
                  "$ref": "#/components/schemas/WaitingStep"
                },
                "type": "array"
              },
              "waiting_total": {
                "description": "How many steps were waiting in total, before the list above was\ntruncated to [`MAX_WAITING_REPORTED`]. Equal to `waiting.len()`\nwhenever nothing was dropped.",
                "format": "int32",
                "minimum": 0,
                "type": "integer"
              },
              "walks_dispatched": {
                "description": "Walks dispatched and walks settled, counted separately because a\nwalk has no `ActionId` and appears in none of the counts above.\n\nWithout these a batch whose every bot is walking reports\n`in_flight: 0` and reads as four idle bots, which is the misreading\nthis event exists to prevent rather than to introduce. Walking is\nmost of the wall clock in these plans.",
                "format": "int32",
                "minimum": 0,
                "type": "integer"
              },
              "walks_settled": {
                "format": "int32",
                "minimum": 0,
                "type": "integer"
              }
            },
            "required": [
              "elapsed_ms",
              "total",
              "dispatched",
              "in_flight",
              "settled",
              "failed",
              "lost",
              "walks_dispatched",
              "walks_settled",
              "since_last_dispatch_ms",
              "bots_in_flight",
              "kind"
            ],
            "type": "object"
          },
          {
            "properties": {
              "action": {
                "type": "string"
              },
              "bot": {
                "format": "int32",
                "minimum": 0,
                "type": "integer"
              },
              "delivery": {
                "oneOf": [
                  {
                    "type": "null"
                  },
                  {
                    "$ref": "#/components/schemas/Delivery",
                    "description": "What this action put INTO a machine or a chest, when it put\nanything in. `None` for every action that delivers nothing -- a\nwalk, a craft, a place, a `mine`, a `take`.\n\n# Why the label was not enough\n\nThe quantity was always in `action`, as prose: *\"fuel the\nstone-furnace with 23 coal (36800 ticks, 153 iron-plate, then it\nstops)\"*. Reading it back means parsing five different sentence\nshapes written at five call sites in the planner, and a shape\nnobody anticipated reads as \"no delivery\" rather than as an error.\n\nThe **hand-credit mass balance** (`tools/run_analysis.py`) needs\nthese numbers to be right, because it accounts for what the roster\nput in against what the machines made and reports `unknown` when a\nsingle delivery cannot be priced. It is the check that replaces a\nhand-sized lead-in parameter, which failed *towards a false pass*\non `run-1788674059-90744`\n(`docs/superpowers/notes/2026-09-06-standing-goals-first-rung.md`).\n\n`#[serde(default)]` so every run archived before this field opens\nunchanged, and reads `None` -- for those runs the analyser parses\nthe label and says which source it used."
                  }
                ]
              },
              "id": {
                "format": "int32",
                "minimum": 0,
                "type": "integer"
              },
              "kind": {
                "enum": [
                  "action_dispatched"
                ],
                "type": "string"
              },
              "target": {
                "oneOf": [
                  {
                    "type": "null"
                  },
                  {
                    "$ref": "#/components/schemas/Position",
                    "description": "Where the plan sent this action, if it sends it anywhere.\n\n`None` for a `craft`/`research` action, which act on no location --\na true absence, not a gap in what was recorded. Every `mine`,\n`place`, `insert` or `remove` action carries a real position here,\nalways, so `None` can never be read as \"we failed to observe the\ntarget\" for those four.\n\nThis is the **planner's intent**, not the game's resolution: it\ntravels straight from `ActionKind::target_position()`\n(`crates/planner/src/action.rs`), computed before the action ever\nreaches the game. Only a `place` action has a game-side answer to\ncompare it against at all -- `RconActuator::place` hands back the\nentity the game actually created, recorded separately as this run's\n`map.jsonl` `placed` line (`actual`, next to `intent`, which is the\nsame position as this field) -- so a bot sent to a tile the planner\nchose that the game resolved elsewhere shows up as a mismatch\nbetween the two records, not inside this one field."
                  }
                ]
              }
            },
            "required": [
              "id",
              "bot",
              "action",
              "kind"
            ],
            "type": "object"
          },
          {
            "description": "An action reached a verdict.\n\n`elapsed_ticks` is null when the game never reported a dispatch tick to\nsubtract from -- a duration nobody measured, which is not the same as a\nduration of zero.\n\nEVERY ATTEMPT THAT REACHED A VERDICT GETS EXACTLY ONE OF THESE, whether\nor not the game stamped a tick for it. `record.actions`\n(`crates/scripting_lua/src/globals/record.rs`) used to make it\nconditional on a measured reply tick, which made `status: \"lost\"`\nstructurally unrecordable -- a lost action is *defined* by no reply\narriving, so it never has a reply tick -- and `run-1788347034-00981`\nrecorded 179 `action_dispatched` lines against 170 of these as a result,\nnine lost `craft`s with a dispatch and nothing after it. Counting the two\nkinds over a run is the cheapest check that this still holds.\n\n`status` is `\"success\"`, `\"failed\"` or `\"lost\"`, straight from the\nexecutor's `Status`. `failed` and `lost` are different facts and are\nnever collapsed: `failed` is the game judging the action and saying no,\n`lost` is the game acknowledging it and never answering. Only the first\nis a verdict; only the second leaves work possibly still outstanding.\n\nA settle with NO `action_dispatched` beside it is legal and is a finding,\nnot a gap: the action reached a verdict before the game acknowledged any\ndispatch, so there was no dispatch to record.\n\n`elapsed_ticks` is also what tells a reader that this event's own `tick`\nis the record's high-water mark rather than the game's clock (see\n[`RunRecorder::not_before`]): a settle the game timed always carries a\nduration, so a null one marks a synthesized stamp.",
            "properties": {
              "bot": {
                "format": "int32",
                "minimum": 0,
                "type": "integer"
              },
              "elapsed_ticks": {
                "format": "int64",
                "minimum": 0,
                "type": [
                  "integer",
                  "null"
                ]
              },
              "error": {
                "description": "The human-readable verdict, as the game or the executor reported\nit. Kept *beside* `failure`, never replaced by it: this is what a\nperson reads, `failure` is what a query groups by, and one is not\na substitute for the other.\n\n**Not only failures**, despite the name. `status` is what says\nwhether anything went wrong; this is whatever this run has to say\nabout the attempt in words, and three states write it (see\n`Attempt::error`, `crates/executor/src/log.rs`). A `success` with a\nmessage is a success that needs qualifying, and today that is\nexactly one thing: an `insert` whose destination had no room for\nthe rest, which reads `destination full: moved 3 of 17 coal, which\nnow holds 50`. Without it, a run whose every boiler top-up moved 3\nof 17 would be indistinguishable from one whose top-ups all moved\n17 -- and the difference is the diagnosis. See\n[`FailureKind::PartialTransfer`] for the failure it is kept apart\nfrom.",
                "type": [
                  "string",
                  "null"
                ]
              },
              "failure": {
                "oneOf": [
                  {
                    "type": "null"
                  },
                  {
                    "$ref": "#/components/schemas/ActionFailure",
                    "description": "The same failure, classified. `None` on success, and also on a\nfailure recorded before this field existed -- `#[serde(default)]`\nmakes that an absence rather than a parse error."
                  }
                ]
              },
              "id": {
                "format": "int32",
                "minimum": 0,
                "type": "integer"
              },
              "kind": {
                "enum": [
                  "action_settled"
                ],
                "type": "string"
              },
              "status": {
                "type": "string"
              }
            },
            "required": [
              "id",
              "bot",
              "status",
              "kind"
            ],
            "type": "object"
          },
          {
            "description": "A bot was sent walking.\n\nThe walking half of [`EventKind::ActionDispatched`], and it exists for\nthe same reason that one does: without it the record shows a bot at one\nplace and then at another with nothing in between. Walking is most of a\nrun's wall clock, and until this variant existed **no walk reached\n`events.jsonl` at all** -- run 30 failed three of them and left exactly\none `last_error` string behind, the other two surviving only in\n`workspace/server-log.txt`, which the next run overwrites. See\n`docs/superpowers/notes/2026-09-02-walks-in-the-record.md`.\n\nWritten only when the game stamped a dispatch tick, exactly as\n`action_dispatched` is: a walk the game never acknowledged was never\ndispatched, and saying otherwise would be an invention.",
            "properties": {
              "bot": {
                "format": "int32",
                "minimum": 0,
                "type": "integer"
              },
              "kind": {
                "enum": [
                  "walk_dispatched"
                ],
                "type": "string"
              },
              "planned_duration": {
                "description": "How long the scheduler expected the walk to take, in ticks.\n\nCarried here because it is carried nowhere else:\n[`EventKind::PlanCreated`]'s `plan` holds only the steps that have\nan `ActionId`, so a walk's prediction has never been in the record\nand a measured walk had nothing to be compared against.",
                "format": "int64",
                "minimum": 0,
                "type": "integer"
              },
              "planned_start": {
                "description": "The tick the *scheduler* placed this walk's start at, counted from\nthe plan's start. **Not a `game.tick`**, and not comparable with\nthis event's own `tick` -- the same two clocks\n[`PlannedStep::planned_start`] keeps apart, for the same reason.",
                "format": "int64",
                "minimum": 0,
                "type": "integer"
              },
              "step_index": {
                "description": "Which walk this is: its index in **this bot's own slice** of the\nschedule, in schedule order (`run_bot_signalled`,\n`crates/executor/src/run.rs`).\n\nA walk has no `ActionId` -- the scheduler emits it as its own\n`StepKind::Walk`, which names no action -- so `(bot, step_index)`\nis the only thing that identifies one. **It is neither an\n`ActionId` nor an index into [`EventKind::PlanCreated`]'s `plan`**,\nwhich is indexed over every step of every bot and omits walks\nentirely. Joining it to either produces confident nonsense, the\nsame trap [`EventKind::Teleport`]'s `action_id` documents.",
                "format": "int32",
                "minimum": 0,
                "type": "integer"
              },
              "to": {
                "$ref": "#/components/schemas/Position",
                "description": "Where the **schedule** sent the bot, straight from\n`StepKind::Walk`'s own `to`.\n\nAn intent, and the only endpoint this pair records. No *arrival*\nposition is recorded anywhere here, on purpose:\n`on_player_changed_position` fires once per **tile crossed**, so a\nbot that comes to rest partway into a tile reports its entry and\nnothing corrects it. Every parked bot's observed position is\ntherefore up to a tile stale -- enough to produce a wrong\ndiagnosis, and it did. The only *observed* positions in the walk\nrecord are [`WalkFailure`]'s, which the mod wrote at the instant it\ngave up.\n\nThis is also routinely a position the bot **cannot stand on**: `to`\ncomes from the `Condition::AtPosition` the walk exists to satisfy,\nso it is often the tile a furnace occupies. Arrival means within\nthe step's radius of it, never on it."
              }
            },
            "required": [
              "bot",
              "step_index",
              "to",
              "planned_start",
              "planned_duration",
              "kind"
            ],
            "type": "object"
          },
          {
            "description": "A walk reached a verdict.\n\nEVERY WALK THAT REACHED A VERDICT GETS EXACTLY ONE OF THESE, whether or\nnot the game stamped a tick for it -- the rule\n[`EventKind::ActionSettled`] already states, applied to the other half\nof the schedule. It is stated again rather than assumed because the\naction side reached it the expensive way: the settle used to be written\ninside `if let Some(replied) = replied`, and a lost attempt never has a\nreply tick, so `status: \"lost\"` was structurally unrecordable. This\nvariant was built with that already fixed.\n\n`status` is `\"success\"`, `\"failed\"` or `\"lost\"`, straight from the\nexecutor's `Status`. `failed` and `lost` are different facts and are\nnever collapsed: `failed` is the game refusing the walk, `lost` is the\ngame acknowledging it and never answering -- a bot that may still be\nwalking as far as anyone here knows. `pending` and `running` are not\nverdicts and write nothing.\n\n`elapsed_ticks` is null when the walk was not timed at both ends, which\nis a duration nobody measured rather than a duration of zero, and is\nalso what marks this event's `tick` as the record's high-water mark\nrather than the game's clock (see [`RunRecorder::not_before`]).",
            "properties": {
              "abandoned": {
                "description": "How many of this bot's remaining steps were abandoned because this\nwalk failed. `None` on every walk that did not halt its bot.\n\n**A failed walk halts the bot** -- deliberately, because a walk's\neffect is a position and no plan edge carries it\n(`factorio_bot_executor::run::run_bot_signalled`). Everything after\nit is dropped without ever being dispatched, so it produces no\nattempt, no `ActionSettled`, and no trace of any kind: the record\nshowed those steps as `pending`, which is also exactly what a run\nsomebody killed early shows.\n\n`run-1788833726-34821` -- the first run in this project's history\nin which a bot died -- ended `success=246 failed=7 lost=1\n**pending=2041**` of 2,295, and the 2,041 were four halts. The\nlargest was **not** a death: bot 3's walk stalled on a `tree-01` at\ntick 7,980 and took **474 steps** with it, 19,000 ticks before any\nbot was killed. Three deaths accounted for the other three halts\nand 1,572 steps. None of that was derivable from the record as it\nstood; it took reconstructing the plan and diffing it against the\ndispatches.\n\n`Some(0)` is written for a bot halted on its own last step and\nmeans \"halted, taking nothing with it\" -- a different fact from\n`None`, which means \"did not halt\".",
                "format": "int32",
                "minimum": 0,
                "type": [
                  "integer",
                  "null"
                ]
              },
              "bot": {
                "format": "int32",
                "minimum": 0,
                "type": "integer"
              },
              "elapsed_ticks": {
                "format": "int64",
                "minimum": 0,
                "type": [
                  "integer",
                  "null"
                ]
              },
              "error": {
                "description": "The verdict as the game or the executor worded it, kept beside\n`failure` and never replaced by it: this is what a person reads,\n`failure` is what a query groups by.",
                "type": [
                  "string",
                  "null"
                ]
              },
              "failure": {
                "oneOf": [
                  {
                    "type": "null"
                  },
                  {
                    "$ref": "#/components/schemas/WalkFailure",
                    "description": "The same failure, classified. `None` on success."
                  }
                ]
              },
              "kind": {
                "enum": [
                  "walk_settled"
                ],
                "type": "string"
              },
              "stalls": {
                "description": "Every stall this walk was retried through, in the order the game\nanswered them. See [`WalkStallRecord`] for what their absence cost.\n\n**`null` and `[]` are different answers.** `null` is \"nobody was\nwatching for stalls on this walk\" -- an actuator that does not\nreport them, or a run archived before this field existed, which\n`#[serde(default)]` makes an absence rather than a parse error.\n`[]` is \"watched, and this walk did not stall\". A reader that\ncollapses them turns an uninstrumented run into a clean one, which\nis the substitution this field was added to end.\n\nA **failed** walk's last stall is not here: the attempt that is not\nretried becomes this event's own `error` and `failure.kind:\nstalled`. So a stalled failure's true total is `stalls.len() + 1`,\nand on a successful walk `stalls.len()` is the whole story.",
                "items": {
                  "$ref": "#/components/schemas/WalkStallRecord"
                },
                "type": [
                  "array",
                  "null"
                ]
              },
              "status": {
                "type": "string"
              },
              "step_index": {
                "description": "The same `(bot, step_index)` identity as\n[`EventKind::WalkDispatched`] -- see its documentation.",
                "format": "int32",
                "minimum": 0,
                "type": "integer"
              },
              "to": {
                "$ref": "#/components/schemas/Position",
                "description": "Where the schedule sent the bot.\n\nRepeated from [`EventKind::WalkDispatched`] rather than looked up\nthere, because a walk the game never acknowledged has no dispatch\nline at all and a settle carrying only a bot and an index would\nname nothing: unlike an action, a walk has no label anywhere in the\nrecord. It is the schedule's destination, with all the caveats\nstated on the dispatch -- in particular it is **not** where the bot\nended up."
              }
            },
            "required": [
              "bot",
              "step_index",
              "to",
              "status",
              "kind"
            ],
            "type": "object"
          },
          {
            "description": "A bot was moved by `player.teleport` rather than by walking.\n\nBefore this variant existed, none of the mod's three teleport sites\nwere observable to Rust at all: `on_player_changed_position` fires\nidentically whether the position changed by walking or by teleport, so\na run whose bots teleported repeatedly recorded ordinary-looking walk\ndurations with nothing to say otherwise. Every walk duration in a\nrecord from before this variant was ever emitted may be fiction for\nthat reason, and there is no way to tell after the fact -- this event\nonly exists going forward.",
            "properties": {
              "action_id": {
                "description": "The walk action this teleport happened during, when there is one.\n`None` for the two blueprint/ghost-revive sites, which are\nsynchronous RCON calls with no dispatched action to attach to.",
                "format": "int32",
                "minimum": 0,
                "type": [
                  "integer",
                  "null"
                ]
              },
              "bot": {
                "format": "int32",
                "minimum": 0,
                "type": "integer"
              },
              "distance": {
                "format": "double",
                "type": "number"
              },
              "from": {
                "$ref": "#/components/schemas/Position"
              },
              "kind": {
                "enum": [
                  "teleport"
                ],
                "type": "string"
              },
              "reason": {
                "description": "Why the mod teleported the bot rather than moving it normally --\ne.g. `walk_stuck` (the leg timed out), `revive_ghost_blocked` or\n`place_blueprint_blocked` (the bot stood inside the entity's\nbounding box). Free text rather than an enum: the set of reasons\nis small and mod-defined, and a reader filtering on it can match\nsubstrings without this crate publishing a closed list the mod\nmust stay in lockstep with.",
                "type": "string"
              },
              "to": {
                "$ref": "#/components/schemas/Position"
              }
            },
            "required": [
              "bot",
              "reason",
              "from",
              "to",
              "distance",
              "kind"
            ],
            "type": "object"
          },
          {
            "description": "The game refused a build, and the planner has stopped offering that\nsite.\n\nWritten by `record.refusals()` (`crates/scripting_lua`) from the\nledger two writers fill: `FactorioRcon::place_entity_timed`, when a\ndispatched build is refused *without* naming the acting player as the\ncause, and `FactorioRcon::can_place_entities`, the pre-flight check\n`goal.plan` runs over a plan's chosen sites before returning it.\n`source` says which. From here to the end of the run,\n`PlanState::from_world` excludes the collision box of `entity` centred\nat `position`, so every later plan sites around it.\n\nThat consequence is the reason this is a record line at all. A planner\nthat has silently started preferring distant tiles is a planner nobody\ncan diagnose; four consecutive runs were spent on refusals whose only\ntrace was an error string, and the next reader should be able to see\nwhich ground the planner has written off and when it learned to.\n\nA refusal the acting player caused is deliberately absent: the mod\nnames that one, the RCON layer walks the bot aside and retries it, and\nthe ground is fine once the bot moves.",
            "properties": {
              "blockers": {
                "description": "The distinct names of the entities the game found in the tested\ncollision box, sorted.\n\nFilled on both sources. The pre-check always reported it; a\n`dispatch` refusal carries it since the mod started appending what\nit found to its `said 'no'` line (`rcon_place_entity`,\n`mods/BotBridge/control.lua`) -- before that it was always empty\nhere, and that absence of a cause is what five consecutive runs\nwere spent on. Empty now means the game scanned the box and found\nno entity in it, so the ground itself is the answer -- see `tile`.\nEmpty **and** `tile: null` on a `dispatch` line is the older\nshape, where nothing was asked.",
                "items": {
                  "type": "string"
                },
                "type": "array"
              },
              "direction": {
                "description": "The `defines.direction` the build was aimed with, when known. The\nbox the game tested is the prototype's box turned this way, so\nthis is what makes the excluded region the right shape for a\nbuilding that is not square: a steam engine facing east claims\n4.7 by 2.5 tiles, not 2.5 by 4.7. `null` on lines written before\nthe direction was carried.",
                "format": "int32",
                "minimum": 0,
                "type": [
                  "integer",
                  "null"
                ]
              },
              "entity": {
                "description": "The item the bot was holding, which is the name the collision box\nexcluded from later plans is looked up under.",
                "type": "string"
              },
              "kind": {
                "enum": [
                  "placement_refused"
                ],
                "type": "string"
              },
              "position": {
                "$ref": "#/components/schemas/Position",
                "description": "The centre the build was aimed at. The excluded region is that\nentity's collision box centred here, not this single tile -- the\ngame tested the box, so the box is what the refusal is about."
              },
              "source": {
                "description": "`\"dispatch\"` or `\"pre_check\"` -- whether a bot flew to this site\nand was refused, or the planner asked before committing to it.\n\nThe two cost very different things and a reader must not have to\nguess which happened. `dispatch` means an action failed here and\nits dependents were abandoned; there is an\n[`EventKind::ActionSettled`] failure beside it. `pre_check` means\nno action was ever created for this site: `goal.plan` asked the\ngame, re-expanded, and the plan that reached the executor sites\nsomewhere else. There is deliberately **no** `action_settled` line\nnext to a `pre_check` refusal, and its absence is not a gap.",
                "type": "string"
              },
              "tile": {
                "description": "The tile under the refused centre, when the game named it. `None`\nonly on a `dispatch` line whose reply named nothing.",
                "type": [
                  "string",
                  "null"
                ]
              }
            },
            "required": [
              "entity",
              "position",
              "source",
              "blockers",
              "kind"
            ],
            "type": "object"
          },
          {
            "description": "A character cannot reach open ground from where it stands.\n\n**The condition this whole event exists for went unnamed for a whole\nrun.** In `run-1788432181-42528` bots 2 and 3 reported byte-identical\npositions from tick ~48 000 to the end -- 77% of the run -- while bot 1\nperformed 746 of 831 dispatches. Twenty walks failed, none of them\nbot 1's. Every artefact the run produced was consistent with a\nscheduling quirk, and the truth was found only by reading\n`samples.jsonl` by hand. A `walk_settled` with `failure.kind: \"no_path\"`\nsays a *destination* could not be reached; it cannot say the bot could\nreach nothing at all, and nineteen of them in a row still cannot.\n\nWritten by `record.enclosures()` (`crates/scripting_lua`) from the\nledger `crates/executor`'s `walk_memory` fills. The check runs when the\ngame's pathfinder has refused a route from this spot, so the event\nalways sits beside a failed walk -- it is the *diagnosis* of that\nfailure, not a second report of it.\n\nNothing acts on this. It changes no plan and moves no bot; it exists so\nthe next reader can see the condition without reconstructing it.",
            "properties": {
              "bot": {
                "format": "int32",
                "minimum": 0,
                "type": "integer"
              },
              "kind": {
                "enum": [
                  "bot_enclosed"
                ],
                "type": "string"
              },
              "pocket_tiles": {
                "description": "How much ground is still reachable, in whole tiles of the game's\nown pathfinding grid -- a tile counts when the character's box\ncentred on it is clear, which is the test `request_path` makes.\nThe tile the character stands on is always one of them. Reported\nbecause \"boxed into 4 tiles\" and \"boxed into 300\" are different\nsituations.",
                "format": "double",
                "type": "number"
              },
              "position": {
                "$ref": "#/components/schemas/Position",
                "description": "Where the character stood. The fill was seeded here, so this is the\nposition the claim is about rather than an approximation of it --\nand unlike a walk's `to`, it is *observed*: the world reported it\nat the instant the path was refused."
              },
              "searched_tiles": {
                "description": "How far the fill was allowed to look, in tiles.\n\nThis is what bounds the claim, and it is on the event rather than\nimplied by the build that wrote it: an enclosure wider than this\nwindow is invisible to the search and produces **no event at all**.\nSo the absence of this event is not evidence that no bot was walled\nin -- only that none was walled into a pen this small. A reader\ncomparing two runs whose builds disagree about the radius needs the\nnumber that was actually used.",
                "format": "double",
                "type": "number"
              }
            },
            "required": [
              "bot",
              "position",
              "pocket_tiles",
              "searched_tiles",
              "kind"
            ],
            "type": "object"
          },
          {
            "description": "The game said this bot cannot move, and the next plan will not send it\nanywhere.\n\nAfter a walk the pathfinder refused, the executor asked the game for a\nshort path from the character in each of four directions\n(`FactorioRcon::probe_player_hops`) and every one was refused. That\nis the game's own verdict on the *character*, which neither\n[`EventKind::BotEnclosed`] (a fill over this process's occupancy\nmodel) nor a `no_path` walk (a verdict on one destination) can give:\nin `run-1788614781-38058` bot 6 stood overlapping a furnace, the fill\nsaid open, four walks said `no_path`, and seven plans in a row sent it\nthe same walk until the run halted `stuck` with seven healthy bots\nidle.\n\nUnlike `bot_enclosed`, this one is acted on: `crates/planner`'s\n`PlanState::from_world` reads the bench and gives the bot no step\nthat would need it to walk. It is lifted by [`EventKind::BotReleased`].\n\nWritten by `record.enclosures()` from `FactorioSurface::benches`.",
            "properties": {
              "bot": {
                "format": "int32",
                "minimum": 0,
                "type": "integer"
              },
              "hop_tiles": {
                "description": "How far each hop was aimed, in tiles.",
                "format": "double",
                "type": "number"
              },
              "kind": {
                "enum": [
                  "bot_benched"
                ],
                "type": "string"
              },
              "position": {
                "$ref": "#/components/schemas/Position",
                "description": "Where the character stood when every hop was refused. Observed."
              },
              "refused_hops": {
                "description": "How many hops were asked for and refused.",
                "format": "int32",
                "minimum": 0,
                "type": "integer"
              }
            },
            "required": [
              "bot",
              "position",
              "refused_hops",
              "hop_tiles",
              "kind"
            ],
            "type": "object"
          },
          {
            "description": "A benched bot can move again, and the next plan may send it.\n\nWritten by `record.enclosures()` when the executor lifted a bench:\n`why` is `\"walked\"` when a walk for the bot succeeded, or `\"probed\"`\nwhen the re-probe before a plan found a hop the game would path.",
            "properties": {
              "bot": {
                "format": "int32",
                "minimum": 0,
                "type": "integer"
              },
              "kind": {
                "enum": [
                  "bot_released"
                ],
                "type": "string"
              },
              "position": {
                "$ref": "#/components/schemas/Position",
                "description": "Where the bench had been earned."
              },
              "why": {
                "type": "string"
              }
            },
            "required": [
              "bot",
              "position",
              "why",
              "kind"
            ],
            "type": "object"
          },
          {
            "description": "A bot was walked clear of a placement that would otherwise have sealed\nit in -- the [`EventKind::BotEnclosed`] that did not happen.\n\nWritten by `record.enclosures()` from the queue `crates/executor`'s\npre-place check fills (`FactorioSurface::step_asides`). The check runs\nbefore every placement: with the footprint added to the occupancy\nmodel, would the fill around the character about to build it close?\nIn `run-1788552801-73005` it would have, and did -- bot 1 placed an\n`assembling-machine-1` from the one patch of ground whose every exit\ncrossed it, and stood there for 40 000 ticks.\n\nThis is a bot doing something the plan did not ask for. It is in the\nrecord so that a reader comparing the plan to what happened can see\nwhy a `place` was preceded by a walk the schedule has no step for.",
            "properties": {
              "bot": {
                "format": "int32",
                "minimum": 0,
                "type": "integer"
              },
              "from": {
                "$ref": "#/components/schemas/Position",
                "description": "Where the character stood when it was about to place. Observed."
              },
              "kind": {
                "enum": [
                  "bot_stepped_aside"
                ],
                "type": "string"
              },
              "placing": {
                "description": "The entity that was about to be placed.",
                "type": "string"
              },
              "pocket_tiles": {
                "description": "How many tiles the character would have been left with, had it\nstayed -- the same count [`EventKind::BotEnclosed`] reports. `0`\nfor `reason = \"footprint\"`, where the placement stands on the\ncharacter's own tile.",
                "format": "double",
                "type": "number"
              },
              "reason": {
                "description": "`\"enclosure\"` or `\"footprint\"`: whether the placement would have\nwalled the character in, or would have stood on the tile the\ncharacter was standing on. The second is the acting bot inside\nits own placement's collision box -- the game refuses that build\nfor the actor's sake, and `run-1788569499-05724` lost a 407-step\nplan to exactly that refusal being read as a verdict about the\nground.",
                "type": "string"
              },
              "site": {
                "$ref": "#/components/schemas/Position",
                "description": "Where it was about to be placed."
              },
              "to": {
                "$ref": "#/components/schemas/Position",
                "description": "The tile centre it was walked to: the nearest tile reachable from\n`from` that stays connected to open ground once the placement\nstands and is still within building reach of `site`."
              }
            },
            "required": [
              "bot",
              "from",
              "to",
              "placing",
              "site",
              "reason",
              "pocket_tiles",
              "kind"
            ],
            "type": "object"
          },
          {
            "description": "A bot lost its character: the game's `on_player_died`, as the mod\nreported it.\n\nBefore this variant, nothing in the record could say a bot had died.\nA dead player keeps its `LuaPlayer` and loses its character, the mod's\nevery entry point answered `not connected` for it, and the roster --\nfixed at script start -- kept assigning it work. So a killed bot\ndegraded into an ordinary stream of failures with no distinguishing\nkind, the same silent roster degradation run 30 paid for from the\nother direction (`docs/superpowers/notes/2026-09-02-bot-one-idle.md`).\nThis event, [`FailureKind::NoCharacter`] and\n[`EventKind::RosterChanged`] are the three halves of the fix.\n\nWritten by `record.deaths()` (`crates/scripting_lua`), drained from\nthe queue `OutputParser` fills. **No archived run contains one**: the\nnearest charted enemy structure on the benchmark seed is 246 tiles\nout, so the first real instance of this event will be produced by the\nfirst run that goes there.",
            "properties": {
              "bot": {
                "format": "int32",
                "minimum": 0,
                "type": "integer"
              },
              "cause": {
                "description": "The entity that did it (`medium-worm-turret`, `small-biter`), when\nthe game named one. `None` is \"the game named nothing\", which it\ndoes for a death with no attacker.",
                "type": [
                  "string",
                  "null"
                ]
              },
              "cause_type": {
                "description": "Its prototype type (`turret`, `unit`). Null exactly when `cause` is.",
                "type": [
                  "string",
                  "null"
                ]
              },
              "kind": {
                "enum": [
                  "bot_died"
                ],
                "type": "string"
              },
              "position": {
                "oneOf": [
                  {
                    "type": "null"
                  },
                  {
                    "$ref": "#/components/schemas/Position",
                    "description": "Where the character stood when it died, as the mod read the\nghost's position. `None` when the mod could not read one."
                  }
                ]
              },
              "respawn_in": {
                "description": "`ticks_to_respawn` as it stood the tick after death. The default\ncharacter prototype respawns after 600; `None` means the mod could\nnot read the timer, not that the bot will never respawn.",
                "format": "int32",
                "minimum": 0,
                "type": [
                  "integer",
                  "null"
                ]
              }
            },
            "required": [
              "bot",
              "kind"
            ],
            "type": "object"
          },
          {
            "description": "The character is back: the game's `on_player_respawned`.\n\nPairs with [`EventKind::BotDied`] by `bot`; the gap between the two is\nthe stretch during which every action for that bot was refused with\n[`FailureKind::NoCharacter`].",
            "properties": {
              "bot": {
                "format": "int32",
                "minimum": 0,
                "type": "integer"
              },
              "kind": {
                "enum": [
                  "bot_respawned"
                ],
                "type": "string"
              },
              "position": {
                "oneOf": [
                  {
                    "type": "null"
                  },
                  {
                    "$ref": "#/components/schemas/Position",
                    "description": "Where the new character stands -- the spawn point, ordinarily,\nwhich is not where the walk in flight was going."
                  }
                ]
              }
            },
            "required": [
              "bot",
              "kind"
            ],
            "type": "object"
          },
          {
            "description": "The mod completed a Factorio 2.0 *trigger* technology on a headless\nrun because the force had already done what the trigger names.\n\nA server-side character has no player, and the game fires `craft-item`\nfor machine output but never for a character's hand craft, and never\n`build-entity` for a mod-placed entity\n(`docs/superpowers/notes/2026-09-05-research-triggers.md`). So\n`emulate_research_triggers` in `mods/BotBridge/control.lua` completes\nsuch a technology from the counters it keeps, only once every\nprerequisite is researched, and writes this line with the count that\nearned it. It is the answer to \"why does `on_research_finished` for\n`automation-science-pack` appear with no research ever started?\", and\na run whose log has the finish without this line is one in which the\ngame fired the trigger itself. Written by `record.research_triggers()`.",
            "properties": {
              "count": {
                "description": "What the force had done when the sweep read it -- at least\n`needed`, by construction.",
                "format": "int32",
                "minimum": 0,
                "type": "integer"
              },
              "entity": {
                "description": "The entity a `build-entity` trigger counted; `null` for `craft-item`.",
                "type": [
                  "string",
                  "null"
                ]
              },
              "item": {
                "description": "The item a `craft-item` trigger counted; `null` for `build-entity`.",
                "type": [
                  "string",
                  "null"
                ]
              },
              "kind": {
                "enum": [
                  "research_trigger_emulated"
                ],
                "type": "string"
              },
              "needed": {
                "description": "What the trigger asked for.",
                "format": "int32",
                "minimum": 0,
                "type": "integer"
              },
              "technology": {
                "type": "string"
              },
              "trigger": {
                "description": "The trigger's `type`: `craft-item` or `build-entity`.",
                "type": "string"
              }
            },
            "required": [
              "technology",
              "trigger",
              "needed",
              "count",
              "kind"
            ],
            "type": "object"
          },
          {
            "description": "The mod discarded generated chunks because they are not on Nauvis.\n\n**This project supports exactly one surface, and this is the line that\nsays when it met a second.** `on_chunk_generated` in\n`mods/BotBridge/control.lua` returns early for any surface but Nauvis,\nso those chunks' entities, resources and tiles never reach\n`EntityGraph`. The guard is deliberate -- the world model keys by\nposition alone, so a Vulcanus chunk would merge into Nauvis with no\nerror anywhere -- but until 2026-09-06 it was also invisible: it\nprinted a bare `\"unknown surface\"` with no `§tick§key§` envelope, which\nreached the server log and no artefact. A run that discarded a whole\nplanet read exactly like one that never left home.\n\n**Space Age is enabled in this workspace** (`workspace/mods/\nmod-list.json`), so every run ever measured here has been a Space Age\nrun that happened never to leave Nauvis. A single one of these rows\nmeans the world model this run planned against is incomplete, and any\nmap fingerprint, resource distance or `entity_at` answer from it\ndescribes Nauvis only. Written by `record.surface_chunks_dropped()`;\nsee `docs/superpowers/notes/2026-09-06-surfaces-survey.md`.",
            "properties": {
              "chunks": {
                "description": "How many chunks were dropped for this surface since the last\nflush -- **not** the run's total, which is the sum over every row\nnaming this surface.",
                "format": "int64",
                "minimum": 0,
                "type": "integer"
              },
              "first_left_top_x": {
                "description": "One example: the top-left **tile** of the first chunk dropped in\nthis window -- (-32, 64), not chunk (-1, 2). A count alone cannot\nbe looked at; a coordinate can.",
                "format": "double",
                "type": "number"
              },
              "first_left_top_y": {
                "format": "double",
                "type": "number"
              },
              "kind": {
                "enum": [
                  "surface_chunk_dropped"
                ],
                "type": "string"
              },
              "surface": {
                "description": "The surface that was refused, by name (`LuaSurface.name`, which is\nunique among surfaces; the *index* is reused after a deletion).",
                "type": "string"
              }
            },
            "required": [
              "surface",
              "chunks",
              "first_left_top_x",
              "first_left_top_y",
              "kind"
            ],
            "type": "object"
          },
          {
            "description": "The supervisor changed the roster it plans for.\n\nThe roster used to be computed once at script start and never again.\n`scripts/supervisor.lua` now re-checks `rcon.players()` before every\nplan: a rostered bot that has had no character for longer than the\nbounded respawn wait is dropped, and one that was dropped and came\nback is picked up again. Written by `record.roster_changed()` from the\nsupervisor's `rerostered` transition, so the reader of a run whose\n`plan_created.bots` shrinks from four to three finds the line that\nsays why, rather than the silence run 30 left.\n\n**Only removals and returns, never additions.** A bot that was never\nin the roster this run started with is not picked up mid-run, because\n\"the first non-empty answer is the roster\" is the bug that froze run\n30 on `[2]`, and re-rostering must not reintroduce it.",
            "properties": {
              "bots": {
                "description": "The roster from here on, ascending.",
                "items": {
                  "format": "int32",
                  "minimum": 0,
                  "type": "integer"
                },
                "type": "array"
              },
              "kind": {
                "enum": [
                  "roster_changed"
                ],
                "type": "string"
              },
              "left": {
                "description": "Bots that were in the roster and are not any more.",
                "items": {
                  "format": "int32",
                  "minimum": 0,
                  "type": "integer"
                },
                "type": "array"
              },
              "reason": {
                "description": "The supervisor's own sentence for why: which bots it waited for,\nfor how many checks, and what it found.",
                "type": "string"
              },
              "returned": {
                "description": "Bots that had been dropped earlier in this run and are back.",
                "items": {
                  "format": "int32",
                  "minimum": 0,
                  "type": "integer"
                },
                "type": "array"
              }
            },
            "required": [
              "bots",
              "left",
              "returned",
              "reason",
              "kind"
            ],
            "type": "object"
          },
          {
            "description": "How much ground the world model was given, against how much ground a\nbot actually covered.\n\n# The cheat nobody had counted\n\nThe mod ingests every entity of every chunk the *engine generates*\n(`on_chunk_generated`, `mods/BotBridge/control.lua`) and never consults\nthe force's charted area. That is `force.chart` with extra steps: the\nmodel gains complete knowledge of ground no character has been near,\nincluding ore, water and nests, and it has been in every run this\nproject has ever made. In `run-1788532631-48030` the furthest any bot\nreached was **63.8 tiles** while the model that run produced held crude\noil at **380 and 505 tiles**, 559 uranium tiles and 36 biter spawners\nout to 500.\n\nThe repo's rule is that a final measured run must be as cheat-free as\nwe can get it, and that whatever is not must be *recorded*. This event\nis that record. It changes nothing and refuses nothing -- like\n[`EventKind::BatchProgress`] it states no verdict, because \"how much\nfree vision is too much\" is a judgement for the reader of a result and\nnot for the writer of a measurement. See\n`docs/superpowers/specs/2026-09-04-exploration-design.md`.\n\n# Why an event rather than `provenance.json` or the manifest\n\n[`provenance::Provenance`] is written once at run start and never\nrewritten, which is right for what a run was *launched* with and wrong\nfor a figure whose second half only exists once bots have moved.\n[`Manifest`] is written in [`RunRecorder::finish`] and so exists only\nfor runs that finished -- nine of the twenty-four runs archived when\nprovenance was added had no manifest at all, and they are the killed\nones. A run that was killed still got its free vision, so the\ndisclosure has to survive the kill.\n\nSo it is written into `events.jsonl`, which is flushed per line: once\nat the run's first recorded event (a baseline, before any bot has\nmoved), every [`VISION_MEASURE_INTERVAL_TICKS`] thereafter, and once\nmore at `finish` after the last samples are ingested. The **last** one\nin the log is the run's answer; the earlier ones show it growing.\n\n# What a reader sees when the measurement is short\n\nEvery field that can be unknown is `Option`, and each `None` has a\ncounter beside it that says why -- see `travelled_tiles` and\n`model_tiles`. A run with no travel data reports `null` and\n`bot_samples: 0`, never `0.0`: a bot that was never observed has not\nbeen observed at the origin.",
            "properties": {
              "bot_samples": {
                "description": "How many bot positions this run has archived so far -- the\ndenominator behind `travelled_tiles`. Zero beside a null distance\nmeans the instrument said nothing; non-zero beside a null distance\nwould mean the instrument spoke and this writer dropped it, which\nis a defect and is visible here rather than invisible.",
                "format": "int32",
                "minimum": 0,
                "type": "integer"
              },
              "kind": {
                "enum": [
                  "vision_measured"
                ],
                "type": "string"
              },
              "model_enemy_structures": {
                "description": "How many enemy structures the model holds. Counted apart from the\nresource tiles because a nest 500 tiles out and an ore tile 500\ntiles out are the same disclosure but not the same finding.",
                "format": "int32",
                "minimum": 0,
                "type": "integer"
              },
              "model_furthest": {
                "description": "What that furthest thing is: an entity name such as `\"crude-oil\"`\nor `\"biter-spawner\"`. A distance alone reads as an abstraction;\nthe name is what makes a reader look. Null exactly when\n`model_tiles` is.",
                "type": [
                  "string",
                  "null"
                ]
              },
              "model_furthest_position": {
                "oneOf": [
                  {
                    "type": "null"
                  },
                  {
                    "$ref": "#/components/schemas/Position",
                    "description": "Where it is. Null exactly when `model_tiles` is."
                  }
                ]
              },
              "model_resource_tiles": {
                "description": "How many resource tiles the model holds -- the census behind\n`model_tiles`. Zero beside a null `model_tiles` is a consistent\n\"nothing has been read\"; non-zero beside a null would be the same\ndefect `bot_samples` guards against.",
                "format": "int32",
                "minimum": 0,
                "type": "integer"
              },
              "model_tiles": {
                "description": "The furthest resource tile or enemy structure the world model\nholds, measured from the map origin, in tiles. `None` when the model holds no resource tile and no enemy\nstructure at all -- a world nothing has read yet, which is not a\nmodel that reaches zero tiles.\n\n**Bounded by the mod, not by the map.** `on_chunk_generated` drops\nevery chunk outside a +/-512-tile box (`control.lua`, unexplained\nsince the mod's first commit in 2021), so this can never exceed\n`hypot(512, 512)` = 724 and a value near that is the model sitting\nagainst the mod's wall rather than the edge of what exists. A run\nwhose figure is at the wall has no idea how much more it was not\nshown.",
                "format": "double",
                "type": [
                  "number",
                  "null"
                ]
              },
              "travelled_at_tick": {
                "description": "The tick that bot was seen there. Null exactly when\n`travelled_tiles` is. Not the tick of this event: the high-water\nmark is carried forward from whenever it was set.",
                "format": "int64",
                "minimum": 0,
                "type": [
                  "integer",
                  "null"
                ]
              },
              "travelled_bot": {
                "description": "Which bot was that far out. Null exactly when `travelled_tiles`\nis. A single distance for a whole run hides which bot earned it,\nand \"one bot went and three sat still\" is a finding this project\nhas already had to reconstruct by hand once.",
                "format": "int32",
                "minimum": 0,
                "type": [
                  "integer",
                  "null"
                ]
              },
              "travelled_tiles": {
                "description": "The furthest any bot has been observed from the map origin so far\nthis run, in tiles, Euclidean.\n\n**`None` is \"nobody has observed a bot position\", not \"no bot\nmoved\".** It comes from the archived sample stream, so it is null\nfor the whole of a run whose sampling never started, and null on\nthe baseline measurement of every run -- the first event is\nrecorded before the first sample is ingested. `bot_samples` is what\nseparates the two.",
                "format": "double",
                "type": [
                  "number",
                  "null"
                ]
              },
              "unearned_ratio": {
                "description": "`model_tiles / travelled_tiles`: how many times further the model\nsees than the furthest bot went.\n\nThe one number this event exists to produce, and `None` whenever\neither half is unknown or no bot has left the origin. It is\n**not** floored at 1: a value below 1 means the bots outran the\nmodel, which is a legitimate reading and not an error to clamp\naway.",
                "format": "double",
                "type": [
                  "number",
                  "null"
                ]
              }
            },
            "required": [
              "bot_samples",
              "model_resource_tiles",
              "model_enemy_structures",
              "kind"
            ],
            "type": "object"
          },
          {
            "description": "What one `goal.plan` cost: wall time, and what the game clock did\nwhile the planner thought.\n\nThe planner is wall-clock work -- ~3 s for automation, ~20 s for green\n-- and a game left running through it is charged `60 * speed` ticks\nper second of thinking: 334 ticks for automation at 1x, 1,837 at 10x,\n6,438 for green at 5x (2026-09-05). That was the whole of the\n\"faster game, longer run\" tax. `goal.plan` now pauses the clock around\nexpansion, and this event is the receipt: `tick_after - tick_before`\nis what the run was charged, which is zero when `paused` held.\n\nWritten by the plan itself rather than folded into `plan_created`,\nwhich the driver script records later with the plan's steps: a plan\nthat raised has no `plan_created` and still cost time.",
            "properties": {
              "kind": {
                "enum": [
                  "planning_timed"
                ],
                "type": "string"
              },
              "paused": {
                "description": "Whether the game clock was stopped for the duration. `false` on a\nbuild without RCON, or when the pause request failed -- in which\ncase `tick_after - tick_before` says what it cost.",
                "type": "boolean"
              },
              "planning_ms": {
                "description": "Wall clock spent inside expansion and scheduling, including the\nplacement pre-check round trips.",
                "format": "int64",
                "minimum": 0,
                "type": "integer"
              },
              "reason": {
                "description": "Why the clock was left running, when it was: `\"attached server,\nclock left running\"` for a `--connect` / `--server` run, whose\ngame may be somebody's live multiplayer session, or `\"pause\nrequest failed\"`. `None` whenever `paused` is true, and on a\nbuild with no game to ask.",
                "type": [
                  "string",
                  "null"
                ]
              },
              "tick_after": {
                "description": "`game.tick` when planning ended; `None` when nobody could ask.",
                "format": "int64",
                "minimum": 0,
                "type": [
                  "integer",
                  "null"
                ]
              },
              "tick_before": {
                "description": "`game.tick` when planning began; `None` when nobody could ask.",
                "format": "int64",
                "minimum": 0,
                "type": [
                  "integer",
                  "null"
                ]
              }
            },
            "required": [
              "planning_ms",
              "paused",
              "kind"
            ],
            "type": "object"
          },
          {
            "properties": {
              "elapsed_ticks": {
                "format": "int64",
                "minimum": 0,
                "type": "integer"
              },
              "kind": {
                "enum": [
                  "run_finished"
                ],
                "type": "string"
              },
              "outcome": {
                "type": "string"
              }
            },
            "required": [
              "outcome",
              "elapsed_ticks",
              "kind"
            ],
            "type": "object"
          },
          {
            "description": "A kind this build does not know. Readers skip it; writers never emit it.",
            "properties": {
              "kind": {
                "enum": [
                  "unknown"
                ],
                "type": "string"
              }
            },
            "required": [
              "kind"
            ],
            "type": "object"
          }
        ]
      },
      "EventsResponse": {
        "description": "`GET /api/v1/runs/{id}/events` response.",
        "properties": {
          "events": {
            "items": {
              "$ref": "#/components/schemas/Event"
            },
            "type": "array"
          },
          "skipped": {
            "description": "Lines that did not parse -- in practice the truncated last line of a\ncrashed run. Reported rather than swallowed.",
            "minimum": 0,
            "type": "integer"
          }
        },
        "required": [
          "events",
          "skipped"
        ],
        "type": "object"
      },
      "EvolutionSample": {
        "description": "Enemy evolution **and its three causes**.\n\nThe decomposition is the entire reason this is worth recording.\n[`Self::factor`] alone cannot distinguish \"our factory did this\" from \"this\nwould have happened anyway\"; with the three terms beside it, a rise is\nattributable. `by_pollution` is what our own production provoked, `by_time`\nis what the clock did, `by_killing_spawners` is what our combat did.\n\nThe three do not simply sum to [`Self::factor`] -- Factorio combines them\nthrough a saturating formula -- so do not treat them as a partition. They\nare the game's own accounting of each cause's contribution, read straight\noff `LuaForce`.",
        "properties": {
          "by_killing_spawners": {
            "description": "`get_evolution_factor_by_killing_spawners(surface)`.",
            "format": "double",
            "type": "number"
          },
          "by_pollution": {
            "description": "`get_evolution_factor_by_pollution(surface)`.",
            "format": "double",
            "type": "number"
          },
          "by_time": {
            "description": "`get_evolution_factor_by_time(surface)`.",
            "format": "double",
            "type": "number"
          },
          "factor": {
            "description": "`get_evolution_factor(surface)`, 0.0 to 1.0.",
            "format": "double",
            "type": "number"
          }
        },
        "required": [
          "factor",
          "by_pollution",
          "by_time",
          "by_killing_spawners"
        ],
        "type": "object"
      },
      "ExecuteAccepted": {
        "description": "The `202` body: the job to poll or stream from.",
        "properties": {
          "job_id": {
            "$ref": "#/components/schemas/JobId"
          }
        },
        "required": [
          "job_id"
        ],
        "type": "object"
      },
      "ExecuteRequest": {
        "properties": {
          "bot_count": {
            "description": "Defaults to the configured `factorio.client_count`.",
            "format": "int32",
            "minimum": 0,
            "type": [
              "integer",
              "null"
            ]
          },
          "code": {
            "description": "Inline code to run. Mutually exclusive with `path`.",
            "type": [
              "string",
              "null"
            ]
          },
          "language": {
            "description": "Defaults to `\"lua\"` when `code` is given; ignored when `path` is.",
            "type": [
              "string",
              "null"
            ]
          },
          "path": {
            "description": "Script to run, relative to the scripts root. Mutually exclusive with `code`.",
            "type": [
              "string",
              "null"
            ]
          }
        },
        "type": "object"
      },
      "ExistsResponse": {
        "properties": {
          "exists": {
            "type": "boolean"
          }
        },
        "required": [
          "exists"
        ],
        "type": "object"
      },
      "FactorioEntity": {
        "properties": {
          "amount": {
            "format": "int32",
            "minimum": 0,
            "type": [
              "integer",
              "null"
            ]
          },
          "bounding_box": {
            "$ref": "#/components/schemas/Rect"
          },
          "direction": {
            "format": "int32",
            "minimum": 0,
            "type": "integer"
          },
          "drop_position": {
            "oneOf": [
              {
                "type": "null"
              },
              {
                "$ref": "#/components/schemas/Position"
              }
            ]
          },
          "entity_type": {
            "type": "string"
          },
          "fuel_inventory": {
            "items": {
              "$ref": "#/components/schemas/InventoryItemWithQuality"
            },
            "type": [
              "array",
              "null"
            ]
          },
          "ghost_name": {
            "type": [
              "string",
              "null"
            ]
          },
          "ghost_type": {
            "type": [
              "string",
              "null"
            ]
          },
          "input_inventory": {
            "items": {
              "$ref": "#/components/schemas/InventoryItemWithQuality"
            },
            "type": [
              "array",
              "null"
            ]
          },
          "name": {
            "type": "string"
          },
          "output_inventory": {
            "items": {
              "$ref": "#/components/schemas/InventoryItemWithQuality"
            },
            "type": [
              "array",
              "null"
            ]
          },
          "pickup_position": {
            "oneOf": [
              {
                "type": "null"
              },
              {
                "$ref": "#/components/schemas/Position"
              }
            ]
          },
          "position": {
            "$ref": "#/components/schemas/Position"
          },
          "recipe": {
            "type": [
              "string",
              "null"
            ]
          },
          "status": {
            "type": [
              "string",
              "null"
            ]
          },
          "surface": {
            "oneOf": [
              {
                "type": "null"
              },
              {
                "$ref": "#/components/schemas/SurfaceId",
                "description": "Which surface this entity stands on, as the mod read it from\n`LuaEntity.surface.name`.\n\n**`None` means the sender did not say, not \"Nauvis\".** Every world dump\nand run record written before this field existed lacks it, and while\nevery one of them is in fact a Nauvis-only run, that is a fact about\nthe mod's `on_chunk_generated` guard rather than something the archived\nbytes assert. So an 865 MB dump from yesterday still deserialises --\npinned by `crates/core/tests/surface_id.rs`.\n\nNothing keys on it yet -- the containers (`EntityGraph`, `PlanState`)\nare still position-only and would alias two surfaces into one. This\ncarries the fact so the record can be read; keying is the next rung."
              }
            ]
          },
          "transport_lines": {
            "items": {
              "$ref": "#/components/schemas/TransportLine"
            },
            "type": [
              "array",
              "null"
            ]
          },
          "underground_half": {
            "oneOf": [
              {
                "type": "null"
              },
              {
                "$ref": "#/components/schemas/UndergroundHalf",
                "description": "`Some` only for one half of an underground-belt pair -- see\n`crate::blueprint::UndergroundHalf`. An `Option` defaulting to `None`\non a missing field, so every archived run record and world dump\nwritten before this field existed still deserialises."
              }
            ]
          }
        },
        "required": [
          "name",
          "entity_type",
          "position",
          "bounding_box",
          "direction"
        ],
        "type": "object"
      },
      "FactorioEntityPrototype": {
        "properties": {
          "attack_range": {
            "description": "How far this entity's attack reaches, in tiles -- the `range` of its\n`attack_parameters`.\n\nThe field a planner needs to answer *\"is standing here inside a worm's\nreach\"*, and **nothing fills it yet**: `mods/BotBridge/types.lua` reads\nno attack parameter of any kind, so on every dump written to date, and\non every live capture until the mod grows a sender, this is `None` for\n`small-worm-turret` exactly as it is for `stone-furnace`.\n\nIt exists anyway, and is read first, so that the seam is a *seam*\nrather than a rewrite: the day the mod sends a range,\n[`crate::graph::entity_graph::EntityGraph`]'s consumers pick it up with\nno further change, and the named fallback that currently answers every\ncall stops being load-bearing. Until then the fallback is documented\nwhere it lives (`crates/planner/src/state.rs`, `threat_standoff`) with\nthe data-file line each of its numbers came from.\n\n# `None` is *unknown reach*, never *no reach*\n\nThe distinction this repo keeps paying for. A caller deciding whether\nsomewhere is safe to send a bot must not read the absence of a range as\na harmless entity -- a `small-worm-turret` with no range on its\nprototype is still a worm. `threat_standoff` therefore never returns\n\"no standoff\" for an unrecognised name; it returns the widest standoff\nit knows and says the answer was a guess.\n\n`default`, so every dump written before this field existed still loads.",
            "format": "double",
            "type": [
              "number",
              "null"
            ]
          },
          "beacon_profile": {
            "description": "The beacon's `profile`: an extra multiplier applied to what a receiver\ngets, **indexed by how many beacons reach that receiver**. Factorio 2.0\nadded it, and it is the reason beacon effect is not a single scalar:\nthe second beacon on a machine is worth a different amount from the\nfirst.\n\nKept under a name that says what it profiles. `LuaEntityPrototype`\ncalls it `profile`, which on a struct describing every prototype in the\ngame says nothing; the mod emits `beacon_profile` to match, and\n`a_serialised_beacon_prototype_carries_its_geometry` pins the pairing,\nbecause serde drops an unrecognised key in silence and this repo has\npaid for that twice.",
            "items": {
              "format": "double",
              "type": "number"
            },
            "type": [
              "array",
              "null"
            ]
          },
          "collision_box": {
            "$ref": "#/components/schemas/Rect"
          },
          "collision_mask": {
            "items": {
              "type": "string"
            },
            "type": [
              "array",
              "null"
            ]
          },
          "crafting_categories": {
            "description": "The recipe categories this prototype's machine runs. Sorted by the mod\nso the order is the data's, not `pairs()`'s. Read live off a Space Age\n2.1.17 game, 18 of 1028 prototypes report any:\n\n```text\nstone-furnace         furnace              smelting\nassembling-machine-1  assembling-machine   advanced-crafting, crafting, parameters\noil-refinery          assembling-machine   oil-processing, parameters\nchemical-plant        assembling-machine   chemistry, parameters\ncentrifuge            assembling-machine   centrifuging, parameters\n```\n\n**`parameters` is not a category anybody makes anything in** -- it is\nFactorio 2.0's blueprint-parameter pseudo-category, and it sits on four\nof those five. A reader must not take a `parameters` match as evidence\nthat a machine runs a recipe. (The chemical plant's real category is\nspelled `chemistry`, not `chemical`.)\n\n# This is the field that says *what* a machine crafts\n\n[`Self::crafting_speed`] already says a prototype crafts. Nothing said\nwhat, and it could not be derived: measured over\n`crates/core/tests/live-2.1.17-world-snapshot.json`, twelve prototypes\nthere declare a crafting speed, and `oil-refinery`, `chemical-plant`,\n`centrifuge`, `electromagnetic-plant` and all three assembling machines\nare one and the same `entity_type`, `assembling-machine`. So a reader\nholding a recipe's category had no field, and no combination of fields,\nfrom which the machine that runs it follows.\n\nIt is the crafting half of the rule\n[`Self::resource_categories`] already carries for mining: a recipe\ncarries a category, a machine carries the categories it supports, and\nthe machine runs the recipe iff the former is in the latter.\n\n# `None` is not `Some(vec![])`\n\n`None` is *the sender did not say* -- every world dumped or snapshotted\nbefore 2026-09-07, and every prototype that is not a crafting machine,\nfor which the optional attribute reads nil. `Some(vec![])` is *the game\nsays this machine runs no category*. A reader that folded the two\ntogether would answer \"no machine runs `oil-processing`\" for an archive\nthat never carried the question.\n\n`deserialize_with`, because the mod sends an empty set as the JSON\nobject `{}` -- `helpers.table_to_json` renders an empty Lua table that\nway -- and a bare `Option<Vec<String>>` would reject it.\n\n# It is an attribute\n\n`LuaEntityPrototype.crafting_categories` is an attribute in 2.1.17,\nchecked in `workspace/client1/doc-html/runtime-api.json`, unlike\n`get_crafting_speed()`, `get_supply_area_distance()` and\n`get_max_wire_distance()`, which are methods with no attribute at all.\nThat distinction is not cosmetic: reading a method as an attribute\nraises, the mod's `pcall` swallows the error, and the field arrives\n`None` for every prototype in the game with nothing anywhere saying it\nshould not have -- which is how [`Self::crafting_speed`] was nil for\n1028 live prototypes.",
            "items": {
              "type": "string"
            },
            "type": [
              "array",
              "null"
            ]
          },
          "crafting_speed": {
            "format": "double",
            "type": [
              "number",
              "null"
            ]
          },
          "distribution_effectivity": {
            "description": "A **beacon's** `distribution_effectivity`: the fraction of a module's\neffect a receiver in range actually gets. `None` for anything that is\nnot a beacon.\n\nNot sufficient on its own -- see [`Self::beacon_profile`], which scales\nit by how many beacons reach the same machine.",
            "format": "double",
            "type": [
              "number",
              "null"
            ]
          },
          "electric_buffer_capacity": {
            "description": "How many **joules** this entity's own electric buffer holds, from\n`LuaElectricEnergySourcePrototype::buffer_capacity`.\n\n**Reached through a sub-prototype, which is why solar sizing was\npreviously blocked at the panel.** `electric_energy_source_prototype`\nis an optional attribute on `LuaEntityPrototype` returning a\n`LuaElectricEnergySourcePrototype`, and `buffer_capacity` is an\nattribute on that; nothing in this project had ever read through a\nsub-prototype before.\n\nAn `accumulator`'s is the number that answers \"how many accumulators\nper panel\" — 5 MJ in vanilla — and [`Self::max_energy_production`]\nanswers only its 300 kW discharge *rate*, which is a different\nquestion. Sent for every electric entity, so a machine's small internal\nbuffer appears here too; that is not storage anybody plans with, and\nfiltering it upstream would put the \"what counts as an accumulator\"\ndecision in Lua where no Rust test can see it.\n\n`default`: `None` means the sender did not say — an entity with no\nelectric energy source at all reports nothing here, and so does every\nworld written before this field existed.",
            "format": "double",
            "type": [
              "number",
              "null"
            ]
          },
          "electric_energy_usage": {
            "description": "What one of these draws from an **electric** network while running, in\n**joules per tick** — the game's own unit, unconverted. Use\n[`Self::energy_usage_kw`] rather than scaling it at a call site.\n\n# The gate is the field, not a caller's responsibility\n\nThe mod sends this **only when the prototype has an electric energy\nsource**. A `stone-furnace` has an `energy_usage` of 90 kW *of coal*,\nand a burner machine charged against an electric budget is a number in\nthe wrong units that every test would agree with. That is why\n`crates/planner/src/state.rs`'s `consumer_kw` leaves burner machines\nout rather than zeroing them, and the gate lives upstream where the\nenergy source is visible.\n\n# An inserter is electric and still has no figure here\n\nMeasured on all 1,028 prototypes of a live 2.1.17 game: **28 carry this\nfield and not one of them reports 0.** An `inserter` has an electric\nenergy source and passes the gate, but `energy_usage` is an *optional*\nattribute and is simply absent on it — its cost is\n`energy_per_movement` and `energy_per_rotation`, per swing, not a\nstanding draw. What a budget wants is what a *busy* one costs, which is\na duty cycle rather than a prototype field, so `state.rs` keeps its\n`INSERTER_DUTY_KW` and reaches it through the ordinary absent-field\nfallback.\n\n`state.rs` treats a zero the same way for safety, but that path has\nnever fired in vanilla and the reason to expect absence rather than\nzero is stated here so nobody re-derives it from the guard.\n\n`default`, so every dump and snapshot written before this field\nexisted reads `None` — *the sender did not say*, never zero draw.",
            "format": "double",
            "type": [
              "number",
              "null"
            ]
          },
          "entity_type": {
            "type": "string"
          },
          "fluid_source_offset": {
            "oneOf": [
              {
                "type": "null"
              },
              {
                "$ref": "#/components/schemas/Position",
                "description": "Which **tile** this entity draws its fluid from, as an offset from its\nown position in the north frame -- `LuaEntityPrototype`'s\n`fluid_source_offset`, `{0, -1}` for the vanilla `offshore-pump`.\n\n# It is what makes the water rule derivable instead of written out\n\nA Factorio 2.0 offshore pump's output fluidbox has no filter at all\n([`FluidFilter::Any`], measured across all 56 boxes in this mod set on\n2026-09-08); it produces whatever the ground under its source tile\ngives. So *which fluid does this pump make* is answered by pairing\nthis offset with [`TileFluid`] on the tile it lands on, and neither\nhalf alone can say.\n\n# `None` is a fact here, not a gap -- and that is unusual\n\nThe runtime API marks the attribute `subclasses: [\"OffshorePump\"]`, so\nit is absent for every other prototype in the game. **An entity with no\nfluid source offset does not draw from the ground**, which is exactly\nthe discriminator a caller needs to keep this rule off boilers and\npumpjacks without naming either. It is still `None` for a record\nwritten before the field existed, and a caller must not read that as\n\"definitely not a ground-drawing entity\" -- the tile it would point at\nis `TileFluid::Unknown` in such a record too, so the rule declines\nrather than misfiring either way.\n\n`default`, so every archived dump and snapshot still loads."
              }
            ]
          },
          "fluidbox_prototypes": {
            "items": {
              "$ref": "#/components/schemas/FactorioFluidBoxPrototype"
            },
            "type": [
              "array",
              "null"
            ]
          },
          "inserter_drop_position": {
            "oneOf": [
              {
                "type": "null"
              },
              {
                "$ref": "#/components/schemas/Position",
                "description": "Where an inserter of this prototype **puts** what it took, as an offset\nfrom its own position in the north frame -- `LuaEntityPrototype`'s\n`inserter_drop_position`, `{0, 1.2}` for the vanilla `inserter` and\n`{0, 2.2}` for `long-handed-inserter`.\n\nThe other half of [`Self::inserter_pickup_position`]; read that field's\ndoc for why the pair is not a mirror, why `None` means two different\nthings, and what the fallback is. Named `insert_position` at data stage\nand `inserter_drop_position` on the runtime API -- the same two-names\ntrap as `mining_drill_radius`, and grepping the data files for the\nruntime spelling finds nothing.\n\n`default`, so every archived dump and snapshot still loads."
              }
            ]
          },
          "inserter_pickup_position": {
            "oneOf": [
              {
                "type": "null"
              },
              {
                "$ref": "#/components/schemas/Position",
                "description": "Where an inserter of this prototype takes items **from**, as an offset\nfrom its own position in the north frame -- `LuaEntityPrototype`'s\n`inserter_pickup_position`, `{0, -1}` for the vanilla `inserter` and\n`{0, -2}` for `long-handed-inserter`.\n\n# It is what makes reach derivable instead of written out\n\n`crates/planner/src/state.rs`'s `inserter_reach` is a table of vanilla\ninserter names, and every geometry in the planner that asks \"can this\ninserter touch that tile\" reads it. A mod that adds an inserter, or\nchanges a vanilla one's swing, is invisible to a table -- the same\nmod-compatibility defect that `supply_area_distance` and\n`maximum_wire_distance` were landed to close for poles.\n\n# It is not the mirror of [`Self::inserter_drop_position`]\n\nVanilla pairs `{0, -1}` with `{0, 1.2}`, and the long-handed one pairs\n`{0, -2}` with `{0, 2.2}`. The extra fifth of a tile is the hand\nreleasing past the tile centre, so **the two vectors differ in\nmagnitude while landing on tiles that are symmetric about the\ninserter**. A reader wanting tile offsets must floor each vector into\nits own tile rather than negate one to get the other.\n\n# `None` is two different things, and a caller must not merge them\n\nThe runtime API marks the attribute `subclasses: [\"Inserter\"]`, so for\na `stone-furnace` `None` is a **fact**: a furnace does not reach out\nand take from the chest beside it, and that absence is the\ndiscriminator that keeps inserter geometry off everything else. But it\nis *also* what every world dumped before 2026-09-08 says about every\nprototype including the inserters, because the mod did not send the\nfield. Those two are indistinguishable here, which is why\n`state.rs::inserter_reach` keeps a fallback table of vanilla names --\n**documented there as a fallback for old senders and never as the\ndefinition of reach.** Deleting it would make every archived dump\nreport that a `long-handed-inserter` reaches nowhere, which is a\nplausible lie rather than a diagnosable failure.\n\n`default`, so every archived dump and snapshot still loads."
              }
            ]
          },
          "max_energy_production": {
            "description": "What one of these contributes to an electric network at full output, in\n**joules per tick**. [`Self::max_energy_production_kw`] converts it.\n\n`LuaEntityPrototype::get_max_energy_production()`, a **method** in\n2.1.17 — there is no attribute of that name, and reading one raises.\n\n# It is the only way to a generator's output, because the inputs are\nnot exposed\n\nA `steam-engine` carries no output figure at the data stage either: its\n900 kW is `fluid_usage_per_tick * 60 * heat_capacity *\n(maximum_temperature - default_temperature) * effectivity`. Of those,\n`LuaEntityPrototype` exposes `maximum_temperature` and `effectivity`\nand **not** `fluid_usage_per_tick` -- doclint-allow: a Factorio\ndata-stage name, and its ABSENCE from this tree is exactly the claim.\nSo the physics cannot be reassembled downstream: the runtime does it\nand hands over the answer.\n\n# Nameplate, and for a solar panel that is a trap\n\nThis is maximum production, so a `solar-panel` reports its **noon**\nfigure and an `accumulator` reports its discharge limit. Neither is\nwhat a day averages, and neither is derivable from any prototype: the\nday/night curve lives on `LuaSurface` and arrives as\n[`SurfaceDaylight`], whose [`SurfaceDaylight::average_solar_fraction`]\nturns this figure into the average and whose\n[`SurfaceDaylight::night_deficit_fraction`] sizes the accumulators.\n`state.rs` still credits only deterministic sources in\n`electric_supply_kw` and exposes the solar answers separately.\n\n`default`: `None` on every world written before this field, meaning\n*unknown*, never \"produces nothing\".",
            "format": "double",
            "type": [
              "number",
              "null"
            ]
          },
          "max_underground_distance": {
            "format": "int32",
            "minimum": 0,
            "type": [
              "integer",
              "null"
            ]
          },
          "maximum_wire_distance": {
            "description": "How far this entity can throw a wire, in tiles — `7.5` for a\n`small-electric-pole`, `32` for a `big-electric-pole`.\n\nIndependent of [`Self::supply_area_distance`], which is what a pole\n*covers*: a big pole covers 4x4 and spans 32 tiles, a `substation`\ncovers 18x18 and spans 18. The two are not orderable against each other\nand neither can be derived from the other.\n\n# This is one pole's reach, never a verdict about a pair\n\n**The game wires two poles when their centres are within the SMALLER of\nthe two distances**, so a reader holding one of these numbers holds an\ninput to a pairwise minimum. `method::power`'s `POLE_WIRE_REACH_TILES`\nis what happens when that is forgotten: a single 7.5 — a *small* pole's\nreach — applied to every pole in a blueprint, manufacturing\n`disconnected_poles` refusals for blocks whose poles really are wired.\n\n# It is the maximum over EVERY wire kind, so it is not \"is this a pole\"\n\nMeasured over all 1,028 prototypes of a live 2.1.17 game (seed 31337,\n2026-09-07): **4 report a pole's copper span** — 7.5, 9, 32, 18,\nmatching the data stage exactly — **94 more report a CIRCUIT wire\ndistance** (`stone-furnace` 9, `wooden-chest` 9, `power-switch` 10,\n`agricultural-tower` 30), and 930 report 0. So a caller that read a\npositive number here as \"this is a pole\" would find 98 poles in\nvanilla, and would wire a network through an assembling machine.\n`crates/planner/src/state.rs`'s `pole_wire_reach` gates on\n`entity_type == \"electric-pole\"` before believing it; this field is\nwhat the game says, not a classification.\n\n# `Some(0.0)` and `None` are different answers\n\n`get_max_wire_distance()` answers **0** for an entity nothing connects\nto — a tree, an explosion, a corpse, and also a `steam-engine` — and\nthe mod sends that zero rather than dropping it, so `Some(0.0)` is the\ngame speaking. `None` is *the sender did not say*: every dump and\nsnapshot written before 2026-09-07, `workspace/scripts/map.json`\nincluded. Only the second falls back to\n`crates/planner/src/state.rs`'s `vanilla_pole_wire_reach`, and merging\nthe two would either blind the planner on every archived world or\nsilently re-credit a wireless entity with a vanilla pole's span.\n\n# It is a method on the runtime API, and the attribute spelling exists\n\n`maximum_wire_distance` is the **data-stage** name — it is what\n`base/prototypes/entity/entities.lua` writes and what a reader would\ntry first — and there is no such attribute on `LuaEntityPrototype` in\n2.1.17. `get_max_wire_distance(quality)` is the only way to it.\nReading the attribute raises, the mod's `pcall` swallows it, and the\nfield arrives `None` for every prototype in the game with nothing\nsaying it should not have; see [`Self::crafting_speed`], which did\nexactly that for all 1028 prototypes of a live game.",
            "format": "double",
            "type": [
              "number",
              "null"
            ]
          },
          "mine_result": {
            "additionalProperties": {
              "format": "int32",
              "minimum": 0,
              "type": "integer"
            },
            "propertyNames": {
              "type": "string"
            },
            "type": [
              "object",
              "null"
            ]
          },
          "mining_drill_radius": {
            "description": "How far a mining drill reaches **beyond the tile it stands on**, which\n[`Self::collision_box`] cannot say. Measured on a live 2.1.17 game:\n\n| entity | footprint | radius | tiles worked |\n|---|---|---|---|\n| `burner-mining-drill` | 2x2 | 0.99 | **2x2**, its own footprint |\n| `electric-mining-drill` | 3x3 | 2.49 | **5x5**, a ring beyond it |\n| `pumpjack` | 3x3 | 0.49 | **1x1**, a single tile |\n\nA **pumpjack is a `mining-drill` too**, and the tightest-reaching one:\nit must be centred on the crude-oil well itself, and its 3x3 box\noverstates its reach by eight tiles.\n\n# Compare in TILES, never in collision-box extents\n\n`radius > collision_box_half_width` is wrong for **every** drill in the\ngame. A burner drill's 0.99 exceeds its half-width of 0.699, so that\ntest says it reaches beyond itself -- it does not: 2 x 0.99 = 1.98,\nwhich is the 2x2 its box already occupies. Factorio sizes a 2x2 box\nslightly under 2 so neighbours do not touch, and comparing a radius\nagainst that shaved number measures the shaving. Ceil both sides to\ntiles first. This was caught by a test written the wrong way round; see\n`crates/core/tests/mining_drill_radius.rs`.\n\n# The field has two names\n\n`resource_searching_radius` in the prototype definitions,\n`mining_drill_radius` on the runtime API. Grepping the data files for\nthe runtime name finds nothing, which looks exactly like the field not\nexisting. Both sources were read: `entity/mining-drill.lua` lines 1757,\n1853 and 1900, and a live 2.1.17 RCON probe, agreeing.\n\nWithout this the planner cannot express \"a drill mines a tile it does\nnot stand on\", and that one gap made two unrelated behaviours\nneedlessly conservative: ore-aware siting asked whether ore lay under a\ndrill's own 3x3 because that was all the model offered, and the\nwill-not-bury placement rule could not distinguish a belt over ore a\ndrill can still reach from a belt over ore nobody can mine. With the\nradius, burying the outer ring under an electric drill is **not a cost\nat all**; under a burner drill it genuinely is.\n\n`default`, so a dump or snapshot written before this field existed\nstill loads, with `None` meaning *unknown reach* -- never zero reach.",
            "format": "double",
            "type": [
              "number",
              "null"
            ]
          },
          "mining_fluid": {
            "description": "`mineable_properties.required_fluid`: the fluid that must be piped in\nto mine this at all. Uranium ore needs sulfuric acid, and a character\nhas no pipe.",
            "type": [
              "string",
              "null"
            ]
          },
          "mining_speed": {
            "format": "double",
            "type": [
              "number",
              "null"
            ]
          },
          "mining_time": {
            "format": "double",
            "type": [
              "number",
              "null"
            ]
          },
          "name": {
            "type": "string"
          },
          "pumping_speed": {
            "description": "How much fluid an **offshore pump produces, or a normal pump moves, per\nTICK** — `20` for both of vanilla's, i.e. 1,200 per second. Use\n[`Self::pumping_speed_per_second`] rather than scaling it at a call\nsite.\n\n# The unit is per tick, and that is measured\n\nThe prototype stage documents `pumping_speed` as \"How many units of\nfluid are produced per tick\" and `base/prototypes/entity/entities.lua`\nwrites `20` for `offshore-pump` and for `pump`. The *runtime* method\nanswers the same `20` on a live 2.1.17 game (2026-09-07), so it did not\nconvert on the way out.\n\n**Checked against the running game and not only against the docs**: an\noffshore pump piped into a storage tank filled it 3,964.84 units in\neach of two consecutive 203-tick windows, i.e. **19.53 units/tick** —\n97.7% of 20, the pipe run taking the rest. Read as per *second* that\nsame figure is 60x wrong, and every doc string would still have agreed.\nA units error of exactly this shape (`mining_speed / mining_time`\ncounting *operations*, not items) hid in [`FlowGraph`]'s drill arm for\nmonths, because every solid ore yields one item per operation.\n\n# It is a method on the runtime API, and there is no attribute\n\n`LuaEntityPrototype::get_pumping_speed(quality)`, subclasses\n`OffshorePump` and `Pump`. Reading `entity.pumping_speed` on a live\nserver raises `LuaEntityPrototype doesn't contain key pumping_speed.`\n— measured, not recalled — the mod's `pcall` swallows it, and the field\narrives `None` for every prototype in the game with nothing saying it\nshould not have. See [`Self::crafting_speed`], which did exactly that\nfor 1028 prototypes of a live game.\n\n# `None` is *the sender did not say*\n\n`default`, so every world dumped before 2026-09-07 reads `None` —\nincluding the 2.94 GB world-record census this rate was measured\nagainst. `crates/core/src/graph/flow_graph.rs` falls back to a\n**vanilla table keyed by prototype name** on exactly that answer, the\nsame shape `crates/planner/src/state.rs` uses for\n[`Self::maximum_wire_distance`], and warns and emits no flow for a pump\nit has never heard of rather than inventing a rate for it.",
            "format": "double",
            "type": [
              "number",
              "null"
            ]
          },
          "resource_categories": {
            "description": "The categories a `character` or `mining-drill` prototype can mine.\nSorted by the mod so the order is the data's, not `pairs()`'s.",
            "items": {
              "type": "string"
            },
            "type": [
              "array",
              "null"
            ]
          },
          "resource_category": {
            "description": "A `resource` prototype's category -- `basic-solid`, `basic-fluid`,\n`hard-solid` -- named `category` at data stage and `resource_category`\nat runtime. This is the discriminator the game itself uses: a character\nor a drill mines a resource iff the category is in its own\n`resource_categories`.",
            "type": [
              "string",
              "null"
            ]
          },
          "solar_panel_performance_at_day": {
            "description": "The **fraction of [`Self::max_energy_production`] a solar panel makes\nwhile the sun is fully up**, from `solar_panel_performance_at_day`.\n\nOne endpoint of the daylight curve. `1.0` in vanilla, so it looks\nredundant and is not: it is the scale a mod moves, and it is also the\nfield whose *presence* says \"this prototype is a solar panel\". The\nattribute carries `subclasses: [\"SolarPanel\"]` in 2.1.17, so reading it\non anything else raises and the mod's `pcall` drops it.\n\nThe other terms of the average — how much of a day is spent up there —\nare on `LuaSurface` and arrive as [`SurfaceDaylight`], never on a\nprototype. `default`: `None` is *the sender did not say*, never \"makes\nnothing by day\".",
            "format": "double",
            "type": [
              "number",
              "null"
            ]
          },
          "solar_panel_performance_at_night": {
            "description": "The other endpoint, from `solar_panel_performance_at_night`: the\nfraction of [`Self::max_energy_production`] a solar panel makes at\nmidnight. `0.0` in vanilla.\n\n**A zero here is a real answer and must not be read as absence.** That\nis the whole reason this is `Option<f64>` and not `f64` — a vanilla\npanel genuinely produces nothing at night, and a modded one that\nproduces a little is a different world, not a better-populated one.",
            "format": "double",
            "type": [
              "number",
              "null"
            ]
          },
          "supply_area_distance": {
            "description": "Half the side of the square this **electric pole or beacon** supplies,\nso `2.5` for a `small-electric-pole`'s 5x5. `LuaEntityPrototype`'s\nsubclasses for it are exactly `ElectricPole` and `Beacon`.\n\n**`None` is \"no supply area\", never zero.** A furnace has none; a\nbeacon with a zero one would be `Some(0.0)`, and a caller that wants to\nknow whether beacon ground is worth reserving must be able to tell\nthose apart. Every dump and snapshot written before this field existed\nalso reads `None`, i.e. \"the sender did not say\".\n\n# It is a method on the runtime API, and that is not a detail\n\nThere is no `supply_area_distance` **attribute** in 2.1.17 --\n`get_supply_area_distance(quality)` is the only way to it. Reading the\nattribute raises, the mod's `pcall` swallows it, and the field arrives\n`None` with nothing anywhere saying it should not have. That is exactly\nhow [`Self::crafting_speed`] arrived nil for all 1028 prototypes of a\nlive game while `mining_speed`, still an attribute, arrived fine.\n\n# What it is for\n\n`crates/planner/src/method/power.rs` hard-codes\n`pole_supply_half_extent` as a table of vanilla pole names, and says in\nits own doc that sending this field is the follow-up that deletes it.\n**Nothing reads this yet** -- carrying the datum and consuming it are\ntwo changes, and the second one moves plans.",
            "format": "double",
            "type": [
              "number",
              "null"
            ]
          }
        },
        "required": [
          "name",
          "entity_type",
          "collision_box"
        ],
        "type": "object"
      },
      "FactorioFluidBoxConnection": {
        "properties": {
          "connection_type": {
            "type": [
              "string",
              "null"
            ]
          },
          "max_underground_distance": {
            "format": "int32",
            "minimum": 0,
            "type": [
              "integer",
              "null"
            ]
          },
          "positions": {
            "items": {
              "$ref": "#/components/schemas/Position"
            },
            "type": "array"
          }
        },
        "required": [
          "positions"
        ],
        "type": "object"
      },
      "FactorioFluidBoxPrototype": {
        "properties": {
          "filter": {
            "$ref": "#/components/schemas/FluidFilter"
          },
          "pipe_connections": {
            "items": {
              "$ref": "#/components/schemas/FactorioFluidBoxConnection"
            },
            "type": [
              "array",
              "null"
            ]
          },
          "production_type": {
            "type": "string"
          },
          "volume": {
            "format": "double",
            "type": [
              "number",
              "null"
            ]
          }
        },
        "required": [
          "pipe_connections",
          "production_type"
        ],
        "type": "object"
      },
      "FactorioItemPrototype": {
        "properties": {
          "fuel_value": {
            "format": "int64",
            "minimum": 0,
            "type": "integer"
          },
          "group": {
            "type": "string"
          },
          "item_type": {
            "type": "string"
          },
          "name": {
            "type": "string"
          },
          "place_result": {
            "type": "string"
          },
          "stack_size": {
            "format": "int32",
            "minimum": 0,
            "type": "integer"
          },
          "subgroup": {
            "type": "string"
          }
        },
        "required": [
          "name",
          "item_type",
          "stack_size",
          "fuel_value",
          "place_result",
          "group",
          "subgroup"
        ],
        "type": "object"
      },
      "FactorioPlayer": {
        "properties": {
          "build_distance": {
            "format": "int32",
            "minimum": 0,
            "type": "integer"
          },
          "drop_item_distance": {
            "format": "int32",
            "minimum": 0,
            "type": "integer"
          },
          "item_pickup_distance": {
            "description": "`double`. Not in use; for picking up items from the ground.",
            "format": "double",
            "type": "number"
          },
          "loot_pickup_distance": {
            "description": "`double`. Not in use; for picking up items from the ground automatically.",
            "format": "double",
            "type": "number"
          },
          "main_inventory": {
            "additionalProperties": {
              "format": "int32",
              "minimum": 0,
              "type": "integer"
            },
            "propertyNames": {
              "type": "string"
            },
            "type": "object"
          },
          "player_id": {
            "$ref": "#/components/schemas/u8"
          },
          "position": {
            "$ref": "#/components/schemas/Position"
          },
          "reach_distance": {
            "format": "int32",
            "minimum": 0,
            "type": "integer"
          },
          "resource_reach_distance": {
            "description": "`double`, for mine. Documented as \"the resource reach distance of this\ncharacter **or max double** when not a character or player connected to\na character\", so this is out of integer range as well as fractional.",
            "format": "double",
            "type": "number"
          },
          "surface": {
            "oneOf": [
              {
                "type": "null"
              },
              {
                "$ref": "#/components/schemas/SurfaceId",
                "description": "Which surface this player's character stands on, from\n`LuaPlayer.surface.name`. `None` means the mod did not say -- see\n[`FactorioEntity::surface`].\n\nA bot that dies on another surface respawns on Nauvis today\n(`create_bot_character(game.surfaces[1], ...)`), and nothing in the\nplanner can express a bot being anywhere but \"the\" surface. This field\nis what makes that observable rather than invisible."
              }
            ]
          }
        },
        "required": [
          "player_id",
          "position",
          "main_inventory",
          "build_distance",
          "reach_distance",
          "drop_item_distance",
          "item_pickup_distance",
          "loot_pickup_distance",
          "resource_reach_distance"
        ],
        "type": "object"
      },
      "FactorioSettings": {
        "properties": {
          "client_count": {
            "format": "int32",
            "minimum": 0,
            "type": "integer"
          },
          "factorio_archive_path": {
            "type": "string"
          },
          "factorio_port": {
            "description": "The game port the server listens on; `None` means Factorio's default\n34197. Set it, with `rcon_port` and `workspace_path`, to run a second\ninstance beside a live one.",
            "format": "int32",
            "minimum": 0,
            "type": [
              "integer",
              "null"
            ]
          },
          "map_exchange_string": {
            "type": "string"
          },
          "rcon_pass": {
            "type": "string"
          },
          "rcon_port": {
            "format": "int32",
            "minimum": 0,
            "type": "integer"
          },
          "recreate": {
            "type": "boolean"
          },
          "seed": {
            "type": "string"
          },
          "workspace_path": {
            "type": "string"
          }
        },
        "required": [
          "client_count",
          "factorio_archive_path",
          "map_exchange_string",
          "rcon_pass",
          "rcon_port",
          "recreate",
          "seed",
          "workspace_path"
        ],
        "type": "object"
      },
      "FactorioTile": {
        "properties": {
          "color": {
            "items": {
              "format": "int32",
              "minimum": 0,
              "type": "integer"
            },
            "type": [
              "array",
              "null"
            ]
          },
          "fluid": {
            "$ref": "#/components/schemas/TileFluid",
            "description": "Which fluid an offshore pump standing here would draw, from\n`LuaTilePrototype::fluid`. See [`TileFluid`] for why this is a tile\nquestion at all and why it is not an `Option<String>`.\n\n[`TileFluid::Unknown`] is the default, so every archived server log,\nboth world dumps and any older mod deserialise to *\"not captured\"*\nrather than asserting that the whole map is [`TileFluid::Dry`]."
          },
          "name": {
            "type": "string"
          },
          "player_collidable": {
            "type": "boolean"
          },
          "position": {
            "$ref": "#/components/schemas/Position"
          },
          "surface": {
            "oneOf": [
              {
                "type": "null"
              },
              {
                "$ref": "#/components/schemas/SurfaceId",
                "description": "Which surface this tile belongs to, from `LuaTile.surface.name`\n(verified present on `LuaTile` in `runtime-api.json` 2.1.17). `None`\nmeans the mod did not say -- see [`FactorioEntity::surface`].\n\n`rcon_find_tiles_filtered` asks `game.surfaces[1]` unconditionally\ntoday, so a shoreline found for a boiler is a *Nauvis* shoreline\nwhatever the bot asking is standing on. Carrying the answer's surface\nis what will let a later reader notice that."
              }
            ]
          }
        },
        "required": [
          "name",
          "player_collidable",
          "position"
        ],
        "type": "object"
      },
      "FailureKind": {
        "description": "How an [`ActionFailure`] failed, coarse enough to group by in a query and\nspecific enough to be worth grouping by.",
        "enum": [
          "missing_item",
          "unreachable",
          "blocked",
          "abandoned",
          "partial_transfer",
          "rejected",
          "timeout",
          "no_character",
          "other"
        ],
        "type": "string"
      },
      "FlowExport": {
        "description": "A point-in-time snapshot of the flow graph -- one line of `flow.jsonl`,\nor the body of `/api/v1/runs/{id}/flow`'s array and of the live\n`/api/v1/game/flow`.\n\nBuilt by [`super::flow_graph::FlowGraph::export`]. Deliberately not\n`Default` or otherwise constructible from nothing outside that method:\nan empty-looking `FlowExport` and one honestly built from an empty graph\nmust not be interchangeable by accident.",
        "properties": {
          "edges": {
            "items": {
              "$ref": "#/components/schemas/FlowExportEdge"
            },
            "type": "array"
          },
          "nodes": {
            "items": {
              "$ref": "#/components/schemas/FlowExportNode"
            },
            "type": "array"
          },
          "tick": {
            "format": "int64",
            "minimum": 0,
            "type": "integer"
          }
        },
        "required": [
          "tick",
          "nodes",
          "edges"
        ],
        "type": "object"
      },
      "FlowExportEdge": {
        "description": "One flow-graph edge: an inserter, belt or pipe link between two nodes.",
        "properties": {
          "from": {
            "description": "A [`FlowExportNode::id`] in this same export.",
            "format": "int32",
            "minimum": 0,
            "type": "integer"
          },
          "lanes": {
            "description": "One entry for a single-lane connection (a pipe, most belts); two for\na belt whose left and right lane carry different items\n(`FlowEdge::Double` in `flow_graph.rs`). Never empty and never more\nthan two -- a Factorio belt has exactly two lanes.",
            "items": {
              "items": {
                "$ref": "#/components/schemas/FlowExportRate"
              },
              "type": "array"
            },
            "type": "array"
          },
          "to": {
            "format": "int32",
            "minimum": 0,
            "type": "integer"
          }
        },
        "required": [
          "from",
          "to",
          "lanes"
        ],
        "type": "object"
      },
      "FlowExportNode": {
        "description": "One flow-graph node: a machine, chest, drill or lab the flow graph\ntracks, at the moment [`FlowExport::tick`] names.",
        "properties": {
          "id": {
            "description": "Scoped to this one document -- the index the node held in\n[`super::flow_graph::FlowGraphInner`] when this export was built.\nStable within the document (every edge's `from`/`to` names a node id\nthat appears in this same export's `nodes`), not across two exports:\na rebuild of the underlying `StableGraph` can renumber freely. Join\ntwo exports on `position`, never on `id`.",
            "format": "int32",
            "minimum": 0,
            "type": "integer"
          },
          "kind": {
            "description": "The entity type, kebab-case (`EntityType`'s `Display`), e.g.\n`mining-drill`, `assembling-machine`, `transport-belt`.",
            "type": "string"
          },
          "miner_ore": {
            "description": "The ore a mining drill sits on, when this node is one.",
            "type": [
              "string",
              "null"
            ]
          },
          "name": {
            "description": "The entity's prototype name, e.g. `stone-furnace`.",
            "type": "string"
          },
          "position": {
            "$ref": "#/components/schemas/Position"
          },
          "recipe": {
            "description": "What `get_recipe()` last reported for a crafting machine. `None` for\nevery non-crafting node, and for a crafting machine with none set --\nthe two are not distinguished here any more than they are on\n`FactorioEntity.recipe` itself.",
            "type": [
              "string",
              "null"
            ]
          }
        },
        "required": [
          "id",
          "position",
          "name",
          "kind"
        ],
        "type": "object"
      },
      "FlowExportRate": {
        "description": "One modelled item rate, in items per second -- the same unit\n`FlowGraph::FlowRates` already uses everywhere else in this crate.",
        "properties": {
          "item": {
            "type": "string"
          },
          "per_second": {
            "format": "double",
            "type": "number"
          }
        },
        "required": [
          "item",
          "per_second"
        ],
        "type": "object"
      },
      "FluidFilter": {
        "description": "Which fluid a fluid box will ever accept, as its **prototype** declares it.\n\n# Three answers, and the two that look alike are the point\n\nThis is `LuaFluidBoxPrototype::filter`, which is an *attribute* and not a\nmethod (checked against `runtime-api.json` for 2.1.17, not recalled -- the\nshape that made `volume` need a `pcall` two fields down). It is the\nquestion *\"what will this machine ever take here\"*, asked of the\nprototype. It is **not** `LuaEntity::get_fluid_filter`, the runtime filter\na pump can be set to, and it is not what a box currently contains --\nnothing in this crate models either.\n\nAn `Option<String>` would have two slots for three states and would\nsilently merge the middle one into the last:\n\n- [`FluidFilter::Only`] -- *\"a boiler's output is steam\"*. Definite.\n- [`FluidFilter::Any`] -- *\"a chemical plant's input has no filter, so the\n  recipe decides\"*. **Also definite, and a completely different fact.**\n- [`FluidFilter::Unknown`] -- the sender never said: an archived dump, a\n  mod predating this field, a build whose read raised. Not a fact about\n  the game at all.\n\n`Any` is the common case for the machines this project cares about most:\nevery crafting machine's boxes are unfiltered, because the recipe is what\npicks the fluid. Reading that as \"we could not tell\" would throw away the\none thing that makes the chemistry rung composable.\n\n# Two predicates, neither the negation of the other\n\n[`Self::is_only`] and [`Self::excludes`] are both *positive* claims, and\nboth are false for `Any` and for `Unknown`. That is deliberate: a single\n`accepts(fluid) -> bool` cannot distinguish its own two meanings, which is\nthe defect class this type exists to avoid.",
        "oneOf": [
          {
            "description": "The prototype names exactly one fluid this box accepts.",
            "properties": {
              "fluid": {
                "type": "string"
              },
              "kind": {
                "enum": [
                  "only"
                ],
                "type": "string"
              }
            },
            "required": [
              "fluid",
              "kind"
            ],
            "type": "object"
          },
          {
            "description": "The prototype sets no filter: the box accepts whatever the machine's\nrecipe (or its neighbours) put in it. An answer, not an absence.",
            "properties": {
              "kind": {
                "enum": [
                  "any"
                ],
                "type": "string"
              }
            },
            "required": [
              "kind"
            ],
            "type": "object"
          },
          {
            "description": "Nobody said. The default, so an archived record or an older mod\ndeserialises to *\"not captured\"* rather than to `Any`.",
            "properties": {
              "kind": {
                "enum": [
                  "unknown"
                ],
                "type": "string"
              }
            },
            "required": [
              "kind"
            ],
            "type": "object"
          }
        ]
      },
      "GitProvenance": {
        "description": "Which commit the code came from, and whether it had been edited.\n\n# Read at run start, from the working tree\n\nDeliberately **not** stamped into the binary by `build.rs`. A build script\nthat shells out to `git` makes every build depend on git state and on git\nbeing installed; `app/src-tauri/build.rs` does not do this today and this\nmodule is not a reason to start.\n\nThe honest cost of reading it at run start is stated in [`Self::source`]:\nthis is the working tree *now*, which is the commit the binary was built\nfrom only if nobody has moved since. `dirty` is the part that makes it\nuseful anyway -- a dirty tree means the commit hash alone does not describe\nthe code, and a comparison across two dirty runs is not a comparison across\ntwo commits.",
        "properties": {
          "commit": {
            "description": "Full 40-character commit hash of `HEAD`.",
            "type": "string"
          },
          "dirty": {
            "description": "Whether the working tree had uncommitted changes to tracked files.\n`true` makes `commit` an approximation rather than an identity.",
            "type": "boolean"
          },
          "source": {
            "description": "How this was obtained, so a reader is never misled about its strength.\nAlways `\"working-tree-at-run-start\"` today.",
            "type": "string"
          }
        },
        "required": [
          "commit",
          "dirty",
          "source"
        ],
        "type": "object"
      },
      "GuiSettings": {
        "properties": {
          "enable_autostart": {
            "type": "boolean"
          },
          "enable_restapi": {
            "type": "boolean"
          }
        },
        "required": [
          "enable_autostart",
          "enable_restapi"
        ],
        "type": "object"
      },
      "InsertToInventoryBody": {
        "properties": {
          "entity_name": {
            "type": "string"
          },
          "entity_position": {
            "type": "string"
          },
          "inventory_type": {
            "format": "int32",
            "minimum": 0,
            "type": "integer"
          },
          "item_count": {
            "format": "int32",
            "minimum": 0,
            "type": "integer"
          },
          "item_name": {
            "type": "string"
          },
          "player_id": {
            "$ref": "#/components/schemas/u8"
          }
        },
        "required": [
          "player_id",
          "entity_name",
          "entity_position",
          "inventory_type",
          "item_name",
          "item_count"
        ],
        "type": "object"
      },
      "InstanceStatus": {
        "properties": {
          "client_count": {
            "format": "int32",
            "minimum": 0,
            "type": "integer"
          },
          "last_error": {
            "description": "Why the last start attempt failed, or `null`.",
            "type": [
              "string",
              "null"
            ]
          },
          "rcon_port": {
            "format": "int32",
            "minimum": 0,
            "type": [
              "integer",
              "null"
            ]
          },
          "server_port": {
            "format": "int32",
            "minimum": 0,
            "type": [
              "integer",
              "null"
            ]
          },
          "started": {
            "type": "boolean"
          },
          "starting": {
            "description": "A start accepted by `POST /api/v1/instance/start` is still running.",
            "type": "boolean"
          }
        },
        "required": [
          "started",
          "starting",
          "client_count"
        ],
        "type": "object"
      },
      "InventoryItemWithQuality": {
        "description": "Inventory item with quality field (Factorio 2.0 format)",
        "properties": {
          "count": {
            "format": "int32",
            "minimum": 0,
            "type": "integer"
          },
          "name": {
            "type": "string"
          },
          "quality": {
            "type": "string"
          }
        },
        "required": [
          "name",
          "quality",
          "count"
        ],
        "type": "object"
      },
      "InventoryResponse": {
        "properties": {
          "fuel_inventory": {
            "items": {
              "$ref": "#/components/schemas/InventoryItemWithQuality"
            },
            "type": [
              "array",
              "null"
            ]
          },
          "input_inventory": {
            "description": "What the machine has been given and has not turned into anything yet --\na furnace's ore, an assembler's ingredients, a lab's science.\n\n# `None` is not empty, and neither is a count of zero\n\n`None` means the entity has no input inventory at all (a\n`wooden-chest`, a belt) **or** the reply came from a mod build that\npredates the field -- `serde(default)` is what makes the second of\nthose possible, and the archived `inventory_contents_at` payload in\n`crates/core/tests/live_2_1_payloads.rs` is exactly such a reply.\n`Some(empty)` means the entity has one and it is standing empty, which\nis a real observation. The mod omits the key rather than sending an\nempty table for the first case; see `rcon_inventory_contents_at` in\n`mods/BotBridge/control.lua`.\n\n# Never a buffer\n\nNothing withdraws from here. `crates/planner`'s `withdraw_slot` maps a\nfurnace to its *result* slot, which is what an `ActionKind::Remove` can\naddress, so ore sitting in an input slot is never counted as material\nthe plan may spend. It is a reading about whether a machine is busy,\non the same terms as the fuel slot beside it.",
            "items": {
              "$ref": "#/components/schemas/InventoryItemWithQuality"
            },
            "type": [
              "array",
              "null"
            ]
          },
          "name": {
            "type": "string"
          },
          "output_inventory": {
            "items": {
              "$ref": "#/components/schemas/InventoryItemWithQuality"
            },
            "type": [
              "array",
              "null"
            ]
          },
          "position": {
            "$ref": "#/components/schemas/Position"
          }
        },
        "required": [
          "name",
          "position"
        ],
        "type": "object"
      },
      "Job": {
        "description": "One script run, live or historical.",
        "properties": {
          "error": {
            "description": "Set only for [`JobStatus::Failed`].",
            "type": [
              "string",
              "null"
            ]
          },
          "finished_at_ms": {
            "format": "int64",
            "minimum": 0,
            "type": [
              "integer",
              "null"
            ]
          },
          "id": {
            "$ref": "#/components/schemas/JobId"
          },
          "replay": {
            "description": "The run's replay document, already serialised as JSON, or `None` for a\nrun that has not produced one (yet, or ever).\n\nNo `skip_serializing_if`: the key is always present in the JSON, `null`\nwhen there is none, so a consumer never has to distinguish \"absent from\nthe document\" from \"absent as a fact\". This is what makes the backlog\npath -- a subscriber attaching after the job already finished -- able\nto recover the replay at all: a finished job has no live channel left,\nso a broadcast-only replay would be lost for exactly the common case\n(a fast script over before a browser can open the stream).",
            "type": [
              "string",
              "null"
            ]
          },
          "script": {
            "description": "Path of the script that was run, or `None` for inline code from the\neditor, which has no path.",
            "type": [
              "string",
              "null"
            ]
          },
          "started_at_ms": {
            "format": "int64",
            "minimum": 0,
            "type": "integer"
          },
          "status": {
            "$ref": "#/components/schemas/JobStatus"
          },
          "stderr": {
            "type": "string"
          },
          "stdout": {
            "type": "string"
          }
        },
        "required": [
          "id",
          "status",
          "started_at_ms",
          "stdout",
          "stderr"
        ],
        "type": "object"
      },
      "JobId": {
        "description": "Identifier of a job.\n\nSerialized as a *string* even though it is a counter: a JSON number large\nenough to lose precision in a browser would silently rename a job, and the\nwire contract should not depend on the width of the counter behind it.",
        "example": "1",
        "type": "string"
      },
      "JobStatus": {
        "description": "Where a job is in its life.",
        "enum": [
          "running",
          "succeeded",
          "failed"
        ],
        "type": "string"
      },
      "Lane": {
        "description": "One thing a bot did, placed on the tick axis.",
        "properties": {
          "action": {
            "description": "What the plan called it, e.g. `mine 4 iron-ore`.\n\nA walk has no label anywhere -- `StepKind::Walk` carries a destination\nand a radius and no text -- so its lane's label is composed here, from\nthe destination the schedule asked for. It is the one string in this\ntype that the plan did not write.",
            "type": "string"
          },
          "bot": {
            "format": "int32",
            "minimum": 0,
            "type": "integer"
          },
          "error": {
            "type": [
              "string",
              "null"
            ]
          },
          "from_tick": {
            "format": "int64",
            "minimum": 0,
            "type": "integer"
          },
          "id": {
            "description": "The action id, or `null` for a lane that is not an action.\n\nNot unique across a run when it is present: ids restart per plan, so it\nidentifies an entry only together with `bot` and `from_tick`.\n\n`null` means **this lane has no action id**, which today means it is a\nwalk -- the scheduler emits a walk as its own `StepKind` with no\n`ActionId`, and there is nothing to put here. Borrowing the walk's\n`step_index` for the field would put a number from a different id space\nunder a name that means action id, which is the trap\n[`super::EventKind::Teleport`]'s `action_id` already documents from the\nother direction.",
            "format": "int32",
            "minimum": 0,
            "type": [
              "integer",
              "null"
            ]
          },
          "status": {
            "description": "`null` while unterminated, otherwise the verdict the game gave.",
            "type": [
              "string",
              "null"
            ]
          },
          "to_tick": {
            "description": "`null` for an action that was dispatched and never settled.\n\nDrawn unterminated rather than dropped: the bot really did start it and\nnothing ever came back, which is a state worth seeing. Dropping it would\nmake a lost action look like one that never happened.",
            "format": "int64",
            "minimum": 0,
            "type": [
              "integer",
              "null"
            ]
          }
        },
        "required": [
          "bot",
          "action",
          "from_tick"
        ],
        "type": "object"
      },
      "MachineSample": {
        "description": "One machine's state at the sample's tick.\n\nFields that do not apply to an entity's type are **absent**, not null, and\nthat is a deliberate departure from this module's usual \"present-and-null,\nnever absent\" rule. That rule is for a value a producer always has an\nanswer for; here `recipe`, `crafting`, `progress` and `products_finished`\nare declared by the Factorio API for `CraftingMachine` only, so on a lab or\na boiler the question does not exist rather than having a null answer.\nReading them off one raises -- which is how a bad attribute read in\n`sample_bots` once took a live server down mid-run -- so the mod does not\nread them, and writes no key.",
        "properties": {
          "crafting": {
            "description": "`is_crafting()`. Absent on a non-crafting machine.",
            "type": [
              "boolean",
              "null"
            ]
          },
          "fuel": {
            "additionalProperties": {
              "format": "int32",
              "minimum": 0,
              "type": "integer"
            },
            "description": "`get_fuel_inventory()`, for burner machines -- stone furnaces, burner\ndrills, boilers. Empty with `status: no_fuel` is the whole story.",
            "propertyNames": {
              "type": "string"
            },
            "type": "object"
          },
          "input": {
            "additionalProperties": {
              "format": "int32",
              "minimum": 0,
              "type": "integer"
            },
            "description": "Ingredient inventory: `crafter_input` for a crafting machine,\n`lab_input` for a lab, empty for anything else.\n\n**The mod omits an empty one and this struct restores it.** Most\nmachines are idle most of the time, and `\"input\":{},\"output\":{},\n\"fuel\":{}` is forty bytes of nothing per machine per five seconds, so\nthe wire format drops them -- hence `#[serde(default)]` on all three.\nThey are still *serialised* unconditionally, because absence and\nemptiness mean the same thing here and normalising to one of them in\nthe archive is what lets the published schema, the TypeScript\ndeclaration and this struct all say the same thing.",
            "propertyNames": {
              "type": "string"
            },
            "type": "object"
          },
          "mining": {
            "description": "For a mining drill, the resource it is mining. Absent for every other\ntype, and absent for a drill over nothing -- which is what separates\n`no_minable_resources` on a depleted patch from a drill that was never\nplaced over ore.",
            "type": [
              "string",
              "null"
            ]
          },
          "name": {
            "description": "Prototype name, e.g. `assembling-machine-1`.",
            "type": "string"
          },
          "network": {
            "description": "`LuaEntity.electric_network_id`, or `None` for a machine connected to\nno electric network -- which for an assembling machine is itself the\nanswer to \"did the pole we placed actually connect it\".\n\nJoin against [`NetworkPower::sub_ids`] in the same tick's force sample.\nAn id matching no sampled network means no *pole* on the force reached\nthat network, since [`PowerSample::networks`] is enumerated from poles.",
            "format": "int32",
            "minimum": 0,
            "type": [
              "integer",
              "null"
            ]
          },
          "output": {
            "additionalProperties": {
              "format": "int32",
              "minimum": 0,
              "type": "integer"
            },
            "description": "`get_output_inventory()`, and for a chest its whole contents -- from\nthe run's point of view a chest is a thing the cell draws from, so\n\"what is in this thing\" stays one key rather than two that differ only\nby entity type.\n\nNon-empty with `status: full_output` is a cell that produced and then\njammed; empty on the chest feeding a machine reporting\n`no_ingredients` is a cell that ran dry. Opposite repairs, and the\nrecord could previously distinguish neither.",
            "propertyNames": {
              "type": "string"
            },
            "type": "object"
          },
          "position": {
            "$ref": "#/components/schemas/Position",
            "description": "As the game reports it. A tile centre stays `-40.5`; nothing rounds."
          },
          "produced": {
            "description": "**This machine's lifetime item count** -- the number the rate work\nneeds, and the one thing that turns \"who made this output\" from an\ninference into arithmetic: between two samples, what each machine\nproduced is the difference of this field.\n\n**Items, not crafts.** A `copper-cable` craft yields two, so an\nassembler with `products_finished: 3` reports `produced: 6`. Both are\nhere on purpose; summing `products_finished` against\n`production.made` would be short by half for every recipe with a yield\nabove one and would read as lost output.\n\n`None` for a machine that makes no items (a lab, a boiler, a steam\nengine, a chest) and for a producer whose count cannot be obtained (a\npumpjack on an infinite resource). [`Self::produced_source`] says\nwhich, always.",
            "format": "int64",
            "minimum": 0,
            "type": [
              "integer",
              "null"
            ]
          },
          "produced_shared": {
            "description": "Set when this drill shared the resource tile it was credited for with\nanother tracked drill in the same tick. Both drills saw the same fall\nand both took credit, so their individual counts are upper bounds and\ntheir sum double-counts. Absent when that never happened.",
            "type": [
              "boolean",
              "null"
            ]
          },
          "produced_source": {
            "description": "How [`Self::produced`] was obtained, so that nobody reads an\naccumulated number as an exact one:\n\n* `game` -- `products_finished` times the recipe's yield. The game\n  counted it.\n* `accumulated` -- the mod counted it, per tick, from the fall in the\n  `amount` of the resource a mining drill is pointed at. Factorio\n  offers a drill no lifetime counter, no progress attribute and no\n  mined event, so there is nothing else to count with. Exact for a\n  drill alone on a finite patch; an upper bound when two drills share a\n  tile, which [`Self::produced_shared`] flags; a lower bound under\n  mining productivity research, which no run of this project has done.\n* `unavailable` -- a producer whose output this mechanism cannot see:\n  an infinite resource never falls. Reported rather than written as\n  zero, which would read as \"produced nothing\".\n* `not-a-producer` -- a lab, boiler, generator or chest. Said in the\n  row rather than by omitting a key, because an absent key cannot tell\n  \"makes nothing\" from \"the counter is missing\".\n\n`None` only for a run archived before per-machine counters existed.\nThat is the honest reading of an old archive and the one\n`tools/run_analysis.py` falls back on.",
            "type": [
              "string",
              "null"
            ]
          },
          "products_finished": {
            "description": "`products_finished`: lifetime completed crafts. The blunt instrument,\nand often the fastest one -- an assembler still reporting `0` after\ntwenty minutes did not produce, whatever else its row says. Absent on a\nnon-crafting machine.",
            "format": "int64",
            "minimum": 0,
            "type": [
              "integer",
              "null"
            ]
          },
          "progress": {
            "description": "`crafting_progress`, 0.0 to 1.0, rounded mod-side to a thousandth.\nAbsent on a non-crafting machine.",
            "format": "double",
            "type": [
              "number",
              "null"
            ]
          },
          "recipe": {
            "description": "What `get_recipe()` reports, which is the only honest verdict on what a\ncrafting machine is set to: `set_recipe` returns the items it *removed*\nrather than a success flag, so a machine that ignored the call reads as\nconfigured everywhere except here.\n\nAbsent on a non-crafting machine; present and `None` never happens --\na crafting machine with no recipe simply gets no key, and its `status`\nsays `no_recipe`.",
            "type": [
              "string",
              "null"
            ]
          },
          "status": {
            "description": "`LuaEntity.status`, by name -- the game's own verdict on why this\nmachine is or is not running, and the single field most of this data\nexists for. `working`, `no_power`, `low_power`, `no_ingredients`,\n`full_output`, `not_enough_space_in_output`, `no_recipe`, `no_fuel`,\n`not_plugged_in_electric_network` and `no_minable_resources` are the\nones a run of this project actually hits.\n\nThe name, not the number: a `defines.entity_status` id is meaningless\nwithout the table that produced it, and an archive outlives the\nFactorio version that wrote it. An id the mod could not name arrives as\n`unmapped_<n>` rather than being dropped.\n\n`None` when the entity has no status at all -- the attribute is\n`optional` in the API.",
            "type": [
              "string",
              "null"
            ]
          },
          "type": {
            "description": "Entity type. The sampled set is exactly `assembling-machine`,\n`furnace`, `mining-drill`, `lab`, `boiler`, `generator`, `container`\nand `logistic-container` -- the machines a run's plan places, plus the\nchests it feeds them from.",
            "type": "string"
          }
        },
        "required": [
          "name",
          "type",
          "position"
        ],
        "type": "object"
      },
      "MapKind": {
        "oneOf": [
          {
            "properties": {
              "actual": {
                "$ref": "#/components/schemas/EntitySnapshot",
                "description": "What the game reports it created."
              },
              "bot": {
                "format": "int32",
                "minimum": 0,
                "type": "integer"
              },
              "drift": {
                "description": "Field names that differ, or null when they agree. Null and empty\nwould mean the same thing; only one of them is written.",
                "items": {
                  "type": "string"
                },
                "type": [
                  "array",
                  "null"
                ]
              },
              "intent": {
                "$ref": "#/components/schemas/EntitySnapshot",
                "description": "What the executor asked for."
              },
              "kind": {
                "enum": [
                  "placed"
                ],
                "type": "string"
              }
            },
            "required": [
              "bot",
              "intent",
              "actual",
              "kind"
            ],
            "type": "object"
          },
          {
            "description": "Reserved, not yet produced: there is no `take_removal` seam anywhere\nin this codebase, so nothing ever constructs this variant today. It is\ndeclared, published in the OpenAPI schema and mirrored in\n`app/src/api/types.ts` because a reader must be able to decode it the\nday a writer exists, but until then this is a promise about the shape,\nnot a fact about what `map.jsonl` contains. Producing it for real\nwould need the executor (or whatever notices an entity gone) to call\na `record.removed(...)` binding that does not exist yet, mirroring\n`Placed`'s `record.actions(...)` seam.",
            "properties": {
              "bot": {
                "format": "int32",
                "minimum": 0,
                "type": "integer"
              },
              "entity": {
                "$ref": "#/components/schemas/EntitySnapshot"
              },
              "kind": {
                "enum": [
                  "removed"
                ],
                "type": "string"
              }
            },
            "required": [
              "bot",
              "entity",
              "kind"
            ],
            "type": "object"
          },
          {
            "properties": {
              "bounds": {
                "$ref": "#/components/schemas/Bounds"
              },
              "divergence": {
                "description": "Entities present in exactly one of them.",
                "items": {
                  "$ref": "#/components/schemas/Divergence"
                },
                "type": "array"
              },
              "game": {
                "description": "What the game reports inside `bounds`.",
                "items": {
                  "$ref": "#/components/schemas/EntitySnapshot"
                },
                "type": "array"
              },
              "kind": {
                "enum": [
                  "keyframe"
                ],
                "type": "string"
              },
              "model": {
                "description": "What our `EntityGraph` believes is inside `bounds`.",
                "items": {
                  "$ref": "#/components/schemas/EntitySnapshot"
                },
                "type": "array"
              }
            },
            "required": [
              "bounds",
              "game",
              "model",
              "divergence",
              "kind"
            ],
            "type": "object"
          },
          {
            "description": "A kind this build does not know. Readers skip it; writers never emit it.",
            "properties": {
              "kind": {
                "enum": [
                  "unknown"
                ],
                "type": "string"
              }
            },
            "required": [
              "kind"
            ],
            "type": "object"
          }
        ]
      },
      "MapRecord": {
        "allOf": [
          {
            "$ref": "#/components/schemas/MapKind"
          },
          {
            "properties": {
              "tick": {
                "format": "int64",
                "minimum": 0,
                "type": "integer"
              }
            },
            "required": [
              "tick"
            ],
            "type": "object"
          }
        ],
        "description": "One line of `map.jsonl`."
      },
      "ModFingerprint": {
        "description": "The mod code a savepoint was written under.\n\n# Why a fingerprint and not the version number\n\n`mods/BotBridge/info.json` is pinned at `0.0.1` and has been for the whole\nlife of this project, because bumping it makes Factorio run migrations.\nSo the version says nothing about whether `control.lua` changed, and\n`control.lua` is what decides how `storage` is shaped and read. Two saves\nwith the same version and different code is the *ordinary* case here, not\nan exotic one.\n\n# What the digest is, and is not\n\nCRC-32 over every file in the mod directory, sorted by relative path, with\neach path and length folded in so that renames and truncations move it. It\ndetects drift; it is not a cryptographic commitment and must not be\ndescribed as one. Nothing here is defending against an adversary -- the\nquestion is only \"is this the same mod I ran last week\".",
        "properties": {
          "digest": {
            "description": "Lowercase hex CRC-32 over the sorted file list. See the type docs.",
            "type": "string"
          },
          "files": {
            "description": "How many files went into the digest. A digest over a directory that was\nhalf missing would otherwise look like an ordinary mismatch.",
            "minimum": 0,
            "type": "integer"
          },
          "version": {
            "description": "`version` out of the mod's `info.json`, when it could be read.",
            "type": [
              "string",
              "null"
            ]
          }
        },
        "required": [
          "digest",
          "files"
        ],
        "type": "object"
      },
      "MovePlayerBody": {
        "properties": {
          "goal": {
            "type": "string"
          },
          "player_id": {
            "$ref": "#/components/schemas/u8"
          },
          "radius": {
            "format": "double",
            "type": [
              "number",
              "null"
            ]
          }
        },
        "required": [
          "player_id",
          "goal"
        ],
        "type": "object"
      },
      "NetworkPower": {
        "description": "One electric network's own generation and demand.",
        "properties": {
          "consumed_kw": {
            "format": "double",
            "type": "number"
          },
          "demanded_kw": {
            "description": "What the network's consumers asked for, as opposed to what they got.\nPresent here and not in the force totals because the gap between demand\nand transfer is the whole diagnosis for a browning-out island.",
            "format": "double",
            "type": "number"
          },
          "generated_kw": {
            "format": "double",
            "type": "number"
          },
          "satisfaction": {
            "description": "Consumed over demanded, 1.0 when nothing is demanded.",
            "format": "double",
            "type": "number"
          },
          "sub_ids": {
            "description": "Every electric *sub*-network under this parent, ascending. A machine\nbelongs to this network when its [`MachineSample::network`] is one of\nthese. Several sub-networks share a parent when a closed power switch\njoins them, which is why this is a set and not a single id.",
            "items": {
              "format": "int32",
              "minimum": 0,
              "type": "integer"
            },
            "type": "array"
          }
        },
        "required": [
          "sub_ids",
          "generated_kw",
          "consumed_kw",
          "demanded_kw",
          "satisfaction"
        ],
        "type": "object"
      },
      "OperationResult": {
        "properties": {
          "success": {
            "type": "boolean"
          }
        },
        "required": [
          "success"
        ],
        "type": "object"
      },
      "PlaceEntityBody": {
        "properties": {
          "direction": {
            "format": "int32",
            "minimum": 0,
            "type": "integer"
          },
          "item": {
            "type": "string"
          },
          "player_id": {
            "$ref": "#/components/schemas/u8"
          },
          "position": {
            "type": "string"
          }
        },
        "required": [
          "player_id",
          "item",
          "position",
          "direction"
        ],
        "type": "object"
      },
      "PlaceEntityResult": {
        "properties": {
          "entity": {
            "$ref": "#/components/schemas/FactorioEntity"
          },
          "player": {
            "$ref": "#/components/schemas/FactorioPlayer"
          }
        },
        "required": [
          "player",
          "entity"
        ],
        "type": "object"
      },
      "PlannedStep": {
        "description": "One scheduled step, as the planner intended it -- carried on\n[`EventKind::PlanCreated`] so a run's record shows what was planned, not\nonly what happened.",
        "properties": {
          "action": {
            "description": "What the plan called it, e.g. `mine 10 iron-ore` -- the same label a\n[`Lane`](lanes::Lane) carries, so the two can be read side by side.",
            "type": "string"
          },
          "bot": {
            "format": "int32",
            "minimum": 0,
            "type": "integer"
          },
          "deps": {
            "description": "Ids this step waits on.",
            "items": {
              "format": "int32",
              "minimum": 0,
              "type": "integer"
            },
            "type": "array"
          },
          "id": {
            "format": "int32",
            "minimum": 0,
            "type": "integer"
          },
          "planned_duration": {
            "format": "int64",
            "minimum": 0,
            "type": "integer"
          },
          "planned_start": {
            "description": "Ticks from the plan's *start*, not an absolute `game.tick`: a plan is\ncomputed before it is dispatched and does not know its own origin.\nThe viewer converts planned ticks to observed ticks in exactly one\nplace (`observedOrigin()`); a field that already carried an absolute\ntick would make that conversion ambiguous about which ticks were\nalready absolute.",
            "format": "int64",
            "minimum": 0,
            "type": "integer"
          }
        },
        "required": [
          "id",
          "bot",
          "action",
          "deps",
          "planned_start",
          "planned_duration"
        ],
        "type": "object"
      },
      "PollutionSample": {
        "description": "Pollution and enemy evolution at one tick, per surface.\n\n# Why this is recorded at all\n\nThe owner's standing concern is that *\"as soon as we have radar or more\nthan 2 steam engines the biters will start to evolve and send out biters to\nattack the base\"*. Nothing in this project could evaluate that: `pollution`\nand `evolution` appeared nowhere in the mod, the planner or the executor,\nand the world dump carries only the `player` force -- the enemy force was\nnot in the model at all. This is the observation, and deliberately only the\nobservation: no pollution-aware planning, no attack predictor, no defence\ntrigger, because nobody has yet established that the numbers move on the\ntimescales this project runs at. A flat curve over a thirty-minute run is a\ncomplete and useful answer.",
        "properties": {
          "surfaces": {
            "additionalProperties": {
              "$ref": "#/components/schemas/SurfacePollution"
            },
            "description": "Keyed by surface name, e.g. `nauvis`.\n\nA map rather than a list for the same reason [`PowerSample::networks`]\nis one, and because pollution is only comparable *within* a surface:\nunder Space Age each surface has its own pollutant (Nauvis emits\npollution, Gleba emits spores), so summing them would add two different\nquantities.",
            "propertyNames": {
              "type": "string"
            },
            "type": "object"
          }
        },
        "required": [
          "surfaces"
        ],
        "type": "object"
      },
      "Position": {
        "properties": {
          "x": {
            "format": "double",
            "type": "number"
          },
          "y": {
            "format": "double",
            "type": "number"
          }
        },
        "required": [
          "x",
          "y"
        ],
        "type": "object"
      },
      "PowerSample": {
        "properties": {
          "consumed_kw": {
            "format": "double",
            "type": "number"
          },
          "generated_kw": {
            "format": "double",
            "type": "number"
          },
          "networks": {
            "additionalProperties": {
              "$ref": "#/components/schemas/NetworkPower"
            },
            "description": "The same figures per electric network, keyed by the smallest\nsub-network id under each parent network.\n\nThe totals above sum every network the force owns, and that sum hides\nthe failure it is most often consulted about. A run reading\n`generated_kw: 900, consumed_kw: 60` looks powered; if the 900 kW is on\nthe network holding the lab and the assemblers sit on an island whose\npole reaches no generator, the totals never mention it. Here that\nisland is its own entry with `generated_kw: 0`, and\n[`MachineSample::network`] says which entry each machine is on.\n\nKeyed by a sub-network id because `LuaElectricNetwork` has no `id`\nattribute at all in 2.1.17; [`NetworkPower::sub_ids`] carries the real\nmembership, and the key is only a handle.\n\n`#[serde(default)]` for archives written before this field existed --\nempty then means \"not recorded\", where on a current line it means \"this\nforce owns no electric poles\". Both are honestly \"no networks known\".",
            "propertyNames": {
              "type": "string"
            },
            "type": "object"
          },
          "satisfaction": {
            "description": "Consumed over demanded. Coverage is not capacity: an under-supplied\nnetwork reads as dead rather than slow, so this is the field that makes\nthat visible after the fact.",
            "format": "double",
            "type": "number"
          }
        },
        "required": [
          "generated_kw",
          "consumed_kw",
          "satisfaction"
        ],
        "type": "object"
      },
      "ProductionSample": {
        "properties": {
          "consumed": {
            "additionalProperties": {
              "format": "int64",
              "minimum": 0,
              "type": "integer"
            },
            "propertyNames": {
              "type": "string"
            },
            "type": "object"
          },
          "made": {
            "additionalProperties": {
              "format": "int64",
              "minimum": 0,
              "type": "integer"
            },
            "description": "Cumulative from game start, never per-interval: deltas are derivable\nfrom totals, and totals are unrecoverable from deltas once one sample\nis lost.",
            "propertyNames": {
              "type": "string"
            },
            "type": "object"
          }
        },
        "required": [
          "made",
          "consumed"
        ],
        "type": "object"
      },
      "Provenance": {
        "description": "The immutable facts about what a run was launched with.\n\nEvery field is `Option` except the identity, and each `None` is a real\nanswer: \"nothing observed this\". They are present-and-null rather than\nomitted for the reason [`super::EventKind::RunStarted`]'s `seed` is -- a key\nthat is always there says \"we looked\", where a missing key cannot be told\napart from \"we never asked\".",
        "properties": {
          "bot_mode": {
            "description": "`\"clients\"` or `\"characters\"`: whether the bots were graphical clients\nor server-side character entities (`--headless`). Play fidelity is\nthe same, so a comparison across the two is flagged, not refused. `None`\nfor runs older than the field and for attached servers.",
            "type": [
              "string",
              "null"
            ]
          },
          "factorio": {
            "description": "The installed game's version, e.g. `\"2.1.17\"`.",
            "type": [
              "string",
              "null"
            ]
          },
          "game_speed": {
            "description": "`game.speed` as set at start. Timings are in ticks and do not change\nwith it; whether the server *kept up* with it is what a reader wants\nto know when a run at speed 10 looks slower per tick than one at 1.",
            "format": "double",
            "type": [
              "number",
              "null"
            ]
          },
          "git": {
            "oneOf": [
              {
                "type": "null"
              },
              {
                "$ref": "#/components/schemas/GitProvenance",
                "description": "Where the code came from. See [`GitProvenance`]."
              }
            ]
          },
          "map": {
            "oneOf": [
              {
                "type": "null"
              },
              {
                "$ref": "#/components/schemas/ResourceFingerprint",
                "description": "An identity for the map computed from the world itself, which is the\nonly identity available for a map whose seed was never recorded.\n\nSee [`ResourceFingerprint`] for what a match and a mismatch each mean --\nthey are not symmetrical."
              }
            ]
          },
          "map_exchange_string": {
            "description": "The map-exchange string for the map this run played -- a **seed plus the\nmap-gen settings**, which is what actually identifies a map. The same\nseed under different settings, or on a Factorio version whose defaults\nmoved, is a different map, so the seed two fields up is necessary and\nnot sufficient.\n\nAsked of the running game ([`crate::factorio::rcon::FactorioRcon::\nmap_exchange_string`]) and falling back to `map-exchange-string.txt`,\nwhich setup writes only when a string was *supplied* to it. Until\n2026-09-06 the file was the only source and nothing produced a string\nfrom a live map, so all 20 archived runs carry `null` here and their\nmaps are permanently unidentifiable.\n\n**`None` means \"not captured\", never \"default settings\".** An RCON\nfailure, an old mod without the function, and a server this process did\nnot start all land here, and none of them is evidence about the map --\nthe same asymmetry [`ResourceFingerprint`] documents.",
            "type": [
              "string",
              "null"
            ]
          },
          "mods": {
            "additionalProperties": {
              "type": "string"
            },
            "description": "Every mod the running game **loaded**, as `name -> version`.\n\nRecorded because a run's mod set is part of what a run *is*, and nothing\nelse here captures it. Two runs on the same seed, the same commit and\nthe same game version are still not comparable if one loaded a mod that\nadds prototypes, changes recipes, or supplies infinite resources — and\nuntil 2026-09-07 there was no way to tell them apart afterwards. That is\nthe shape that already made every pre-2026-09-04 timing unidentifiable,\nwhen `--seed` was silently ignored and nothing recorded the map.\n\n**This includes BotBridge**, whose version has never been pinned to a\nrun. The mod resolves from different places in debug and release builds\n(see `CLAUDE.md`), so the same commit can genuinely run different Lua —\n`profile` hints at that and this settles it.\n\n`None` is **not captured**; `Some` of an empty map is the positive\nanswer that the game loaded no mods at all. A reader must not collapse\nthem: \"we could not ask\" and \"there are none\" are different facts, and\nthis file already keeps that distinction for `seed`, `map` and\n`map_exchange_string`.",
            "propertyNames": {
              "type": "string"
            },
            "type": [
              "object",
              "null"
            ]
          },
          "peaceful": {
            "description": "Whether **every surface** had peaceful mode on, as the running game\nanswered at run start.\n\n# What it does and does not mean\n\nPeaceful mode does **not** remove biters. Nests, worms and units all\nstill generate and still stand on the map; what changes is that they do\nnot attack unprovoked. So `true` here does not license \"there were no\nenemies\", only \"nothing attacked this run unless it was attacked\nfirst\". Anyone quoting a run that survived a walk past a nest needs\nthis field to know which claim they are making.\n\n# Why it has to exist at all\n\nA peaceful run and a hostile one are byte-identical in every other\nfield: the same seed, the same settings, the same commit, the same\nmods, the same map fingerprint. Without this, a run that never lost a\nbot because nothing was hunting it would compare directly against one\nthat did, and the difference would be attributed to whatever change was\nunder test. That is precisely the failure `CLAUDE.md` records for the\nmap-exchange string: a run that looks controlled and is not.\n\n# Observed, not requested\n\nAsked of the game ([`crate::factorio::rcon::FactorioRcon::\npeaceful_mode`]), the same way `map_exchange_string` is, so a run that\nasked for peaceful mode and did not get it records `false` rather than\nthe request. The `--peaceful` flag is a request; this is the answer.\n\n**`None` is \"not captured\" and must never be read as \"hostile\".** Every\nrun archived before this field existed carries no key at all, an RCON\nfailure lands here, and so does a server this process did not start.\nA reader that collapses `None` into `false` would silently declare\nevery historical run hostile on no evidence -- the exact conflation\nthis file already keeps apart for `seed`, `map`, `mods` and\n`map_exchange_string`.",
            "type": [
              "boolean",
              "null"
            ]
          },
          "profile": {
            "description": "`\"debug\"` or `\"release\"`. Worth recording because the two builds resolve\nmods and scripts from different places (see `CLAUDE.md`), so the same\ncommit can run different Lua in each.",
            "type": "string"
          },
          "resumed_from": {
            "description": "The save, if any, this run was resumed from rather than starting on a\nfresh world.\n\nAlways `None` today: nothing saves at a milestone yet. The field is here\nahead of that feature on purpose, because it is a *start-time* fact and\nadding it later would mean a schema bump on a file whose whole value is\nthat archived runs stay readable.\n\n**A resumed run is not benchmark-comparable with a fresh one.** It\nbegins with accumulated world state -- built furnaces, charged chests,\npartly mined patches -- so its timings measure a different thing. A\ncomparison tool must treat `Some(_)` against `None` the way it treats\ntwo different seeds, and for the same reason.",
            "type": [
              "string",
              "null"
            ]
          },
          "roster_requested": {
            "description": "The roster the run was launched for, ascending. What was *asked for*; a\nrun that degrades to fewer bots because clients never connected still\nrecords the request here, and `run_started.bots` records what it got.\nThe pair is the evidence that a run degraded.",
            "items": {
              "format": "int32",
              "minimum": 0,
              "type": "integer"
            },
            "type": "array"
          },
          "run_id": {
            "type": "string"
          },
          "schema": {
            "description": "Bumped when a field changes meaning, never when one is added. Readers\nmust accept any schema at or below their own, the same rule\n[`super::samples`] follows, so that archived runs stay readable.",
            "format": "int32",
            "minimum": 0,
            "type": "integer"
          },
          "seed": {
            "description": "The map-generation seed, when this workspace's `level.zip` was created\nby a build that recorded one.\n\n`None` for every map generated before\n[`crate::process::instance_setup::record_map_gen_seed`] existed, and for\nevery map Factorio seeded itself. Both mean \"unknown\", and neither may\nbe reported as a match against anything.",
            "type": [
              "string",
              "null"
            ]
          },
          "started_tick": {
            "description": "The game tick the run opened at.",
            "format": "int64",
            "minimum": 0,
            "type": "integer"
          },
          "started_unix": {
            "description": "Unix seconds at which the run directory was created. For \"when was\nthis\", never for comparing two runs -- runs are compared on ticks.",
            "format": "int64",
            "minimum": 0,
            "type": "integer"
          },
          "workspace": {
            "description": "The workspace the run used, absolute. Two runs from different\nworkspaces used different `level.zip` files and different mod copies.",
            "type": [
              "string",
              "null"
            ]
          }
        },
        "required": [
          "schema",
          "run_id",
          "started_unix",
          "started_tick",
          "profile",
          "roster_requested"
        ],
        "type": "object"
      },
      "RconBody": {
        "properties": {
          "command": {
            "type": "string"
          }
        },
        "required": [
          "command"
        ],
        "type": "object"
      },
      "Rect": {
        "properties": {
          "left_top": {
            "$ref": "#/components/schemas/Position"
          },
          "right_bottom": {
            "$ref": "#/components/schemas/Position"
          }
        },
        "required": [
          "left_top",
          "right_bottom"
        ],
        "type": "object"
      },
      "RemoveFromInventoryBody": {
        "properties": {
          "entity_name": {
            "type": "string"
          },
          "entity_position": {
            "type": "string"
          },
          "inventory_type": {
            "format": "int32",
            "minimum": 0,
            "type": "integer"
          },
          "item_count": {
            "format": "int32",
            "minimum": 0,
            "type": "integer"
          },
          "item_name": {
            "type": "string"
          },
          "player_id": {
            "$ref": "#/components/schemas/u8"
          }
        },
        "required": [
          "player_id",
          "entity_name",
          "entity_position",
          "inventory_type",
          "item_name",
          "item_count"
        ],
        "type": "object"
      },
      "ResearchSample": {
        "properties": {
          "eta_ticks": {
            "description": "Always `null` today: `mods/BotBridge/control.lua`'s `sample_force`\nhard-codes `eta_ticks = nil` because no writer computes an estimate\nyet. So a `null` here means \"not computed\", not \"the game reported\nnone\" -- there is no code path, mod-side or Rust-side, that has ever\nasked Factorio for this and gotten a real answer.",
            "format": "int64",
            "minimum": 0,
            "type": [
              "integer",
              "null"
            ]
          },
          "name": {
            "type": "string"
          },
          "progress": {
            "description": "0.0 to 1.0.",
            "format": "double",
            "type": "number"
          }
        },
        "required": [
          "name",
          "progress"
        ],
        "type": "object"
      },
      "ResourceFingerprint": {
        "description": "What [`EntityGraph::resource_fingerprint`] found: an identity for the map,\nand the human-readable counts behind it.",
        "properties": {
          "digest": {
            "description": "FNV-1a over every charted resource tile, sorted by name and position.\nEqual digests mean the same map; unequal digests mean **unknown**, since\ncharting grows as bots explore. See the method's own documentation.",
            "type": "string"
          },
          "tiles": {
            "additionalProperties": {
              "minimum": 0,
              "type": "integer"
            },
            "description": "How many tiles of each resource were charted, by name. This is the half\na person reads: \"one map had coal and the other did not\" is visible here\nand nowhere else in a run record.",
            "propertyNames": {
              "type": "string"
            },
            "type": "object"
          }
        },
        "required": [
          "digest",
          "tiles"
        ],
        "type": "object"
      },
      "RestApiSettings": {
        "properties": {
          "port": {
            "format": "int64",
            "type": "integer"
          },
          "web_root": {
            "description": "Directory containing the built SPA. Relative paths resolve against the\ncurrent working directory. When the directory does not exist, the server\nstill starts and serves the API only.",
            "type": [
              "string",
              "null"
            ]
          }
        },
        "required": [
          "port"
        ],
        "type": "object"
      },
      "RunDetail": {
        "description": "`GET /api/v1/runs/{id}` response.",
        "properties": {
          "splits": {
            "description": "From `splits.json` when the run finished, otherwise derived from the\nevents on the fly -- so a crashed run still shows the milestones it got\nthrough.",
            "items": {
              "$ref": "#/components/schemas/Split"
            },
            "type": "array"
          },
          "summary": {
            "$ref": "#/components/schemas/RunSummary"
          }
        },
        "required": [
          "summary",
          "splits"
        ],
        "type": "object"
      },
      "RunFlowResponse": {
        "description": "`GET /api/v1/runs/{id}/flow` response.",
        "properties": {
          "flow": {
            "items": {
              "$ref": "#/components/schemas/FlowExport"
            },
            "type": "array"
          },
          "skipped": {
            "description": "Lines that did not parse. Reported rather than swallowed, matching\n`RunMapResponse.skipped`.",
            "minimum": 0,
            "type": "integer"
          }
        },
        "required": [
          "flow",
          "skipped"
        ],
        "type": "object"
      },
      "RunLanesResponse": {
        "description": "`GET /api/v1/runs/{id}/lanes` response.",
        "properties": {
          "lanes": {
            "description": "What each bot did, in dispatch order.",
            "items": {
              "$ref": "#/components/schemas/Lane"
            },
            "type": "array"
          }
        },
        "required": [
          "lanes"
        ],
        "type": "object"
      },
      "RunMapResponse": {
        "description": "`GET /api/v1/runs/{id}/map` response.",
        "properties": {
          "map": {
            "items": {
              "$ref": "#/components/schemas/MapRecord"
            },
            "type": "array"
          },
          "skipped": {
            "description": "Lines that did not parse. Reported rather than swallowed, matching\n`EventsResponse.skipped`.",
            "minimum": 0,
            "type": "integer"
          }
        },
        "required": [
          "map",
          "skipped"
        ],
        "type": "object"
      },
      "RunSamplesResponse": {
        "description": "`GET /api/v1/runs/{id}/samples` response.",
        "properties": {
          "samples": {
            "items": {
              "$ref": "#/components/schemas/Sample"
            },
            "type": "array"
          },
          "skipped": {
            "description": "Lines that did not parse -- in practice `bots = {}` serialising to\n`\"{}\"` rather than `\"[]\"` when no player is connected. Reported rather\nthan swallowed, matching `EventsResponse.skipped`.",
            "minimum": 0,
            "type": "integer"
          }
        },
        "required": [
          "samples",
          "skipped"
        ],
        "type": "object"
      },
      "RunSavepointsResponse": {
        "description": "`GET /api/v1/runs/{id}/savepoints` response.",
        "properties": {
          "missing_zip": {
            "description": "Milestone indices whose metadata parsed but whose `file` is not on\ndisk. Named here and NOT listed in `savepoints`, because listing them\nwould offer a resume that cannot happen.",
            "items": {
              "format": "int32",
              "minimum": 0,
              "type": "integer"
            },
            "type": "array"
          },
          "savepoints": {
            "description": "Ascending by milestone index. Each names its `.zip` relative to the\nrun's `savepoints/` directory; `--resume-from <run>:<index>` is the\ncommand that uses one. Excludes any milestone whose metadata parsed\nbut whose `.zip` is missing -- see `missing_zip`.",
            "items": {
              "$ref": "#/components/schemas/Savepoint"
            },
            "type": "array"
          },
          "skipped": {
            "description": "Metadata files that did not parse -- in practice a truncated or\ncorrupt `milestone-N.json`. Reported rather than swallowed, matching\nthe sibling responses' `skipped`: a corrupt file must not read the\nsame as \"this milestone was never saved\".",
            "minimum": 0,
            "type": "integer"
          }
        },
        "required": [
          "savepoints",
          "skipped",
          "missing_zip"
        ],
        "type": "object"
      },
      "RunSummary": {
        "description": "One run, as it appears in a listing.\n\nEvery field except `run_id` and `finished` is nullable, because an\nunfinished run genuinely does not have them. They are present-and-null\nrather than omitted: a caller must be able to tell \"this run never finished\"\nfrom \"this build of the API does not report outcomes\".",
        "properties": {
          "elapsed_ticks": {
            "format": "int64",
            "minimum": 0,
            "type": [
              "integer",
              "null"
            ]
          },
          "events": {
            "minimum": 0,
            "type": [
              "integer",
              "null"
            ]
          },
          "finished": {
            "description": "Whether the run reached `finish`. False means crashed or still going --\nthis endpoint cannot tell those apart, and does not pretend to.",
            "type": "boolean"
          },
          "finished_unix": {
            "format": "int64",
            "minimum": 0,
            "type": [
              "integer",
              "null"
            ]
          },
          "map": {
            "description": "Lines in `map.jsonl`, same convention.",
            "minimum": 0,
            "type": [
              "integer",
              "null"
            ]
          },
          "outcome": {
            "type": [
              "string",
              "null"
            ]
          },
          "run_id": {
            "type": "string"
          },
          "samples": {
            "description": "Archived sample lines. `None` for an unfinished run; `Some(0)` for a\nfinished run with no samples file.",
            "minimum": 0,
            "type": [
              "integer",
              "null"
            ]
          },
          "samples_lag_ticks": {
            "description": "Ticks of the run's span the samples do NOT cover (see `Manifest`).\n`None` when the run has no samples at all or never finished.",
            "format": "int64",
            "minimum": 0,
            "type": [
              "integer",
              "null"
            ]
          },
          "splits": {
            "minimum": 0,
            "type": [
              "integer",
              "null"
            ]
          },
          "started_unix": {
            "format": "int64",
            "minimum": 0,
            "type": [
              "integer",
              "null"
            ]
          }
        },
        "required": [
          "run_id",
          "finished"
        ],
        "type": "object"
      },
      "RunsResponse": {
        "description": "`GET /api/v1/runs` response.",
        "properties": {
          "runs": {
            "description": "Newest first, by start time; runs with no manifest sort last, since\nnothing on disk says when they began.",
            "items": {
              "$ref": "#/components/schemas/RunSummary"
            },
            "type": "array"
          }
        },
        "required": [
          "runs"
        ],
        "type": "object"
      },
      "Sample": {
        "allOf": [
          {
            "$ref": "#/components/schemas/SampleKind"
          },
          {
            "properties": {
              "run": {
                "description": "The run id the mod stamped on this line, or `None` for a line written\nbefore this field existed. Absence must be distinguishable from a\nmismatch: [`ingest_samples`] treats a named-but-different run as a\nconfident exclusion and an absent run as a tick guess, and those are\nnot the same confidence level.\n\n`#[serde(default)]`, deliberately unlike the \"present-and-null, never\nabsent\" fields elsewhere in this module: those describe values a\n*current* producer always has an answer for (even if the answer is\n\"nothing\"), where here the missing-key case is a *past* producer that\nnever knew to ask. A key present with a `null` value and a key never\nwritten must both mean \"we don't know\", so both must decode to `None`.",
                "type": [
                  "string",
                  "null"
                ]
              },
              "schema": {
                "description": "The schema this line was written under. [`read_samples`] has already\nchecked this against [`SAMPLE_SCHEMA`] via `SchemaProbe` by the time a\nline reaches this struct, but the field must still be a *real* member\nhere -- not just probed and discarded -- or re-serialising an archived\nsample drops the stamp. A file this product produces would then be\nunable to trip its own schema guard on a later read: a future\n`SAMPLE_SCHEMA` bump would silently lose data on an old archive instead\nof failing loudly.",
                "format": "int32",
                "minimum": 0,
                "type": "integer"
              },
              "tick": {
                "format": "int64",
                "minimum": 0,
                "type": "integer"
              }
            },
            "required": [
              "schema",
              "tick"
            ],
            "type": "object"
          }
        ],
        "description": "One line of `samples.jsonl`."
      },
      "SampleKind": {
        "oneOf": [
          {
            "properties": {
              "bots": {
                "items": {
                  "$ref": "#/components/schemas/BotSample"
                },
                "type": "array"
              },
              "kind": {
                "enum": [
                  "bots"
                ],
                "type": "string"
              }
            },
            "required": [
              "bots",
              "kind"
            ],
            "type": "object"
          },
          {
            "properties": {
              "kind": {
                "enum": [
                  "force"
                ],
                "type": "string"
              },
              "pollution": {
                "oneOf": [
                  {
                    "type": "null"
                  },
                  {
                    "$ref": "#/components/schemas/PollutionSample",
                    "description": "Pollution and enemy evolution, per surface.\n\n`None` for every line written before schema 3 -- which is every\narchived run this project has -- and that must stay\ndistinguishable from a game that has no pollution and no evolved\nbiters. Both are quiet worlds; only one of them was measured.\n`tools/run_analysis.py` withholds judgement on `None` rather than\nrendering zero."
                  }
                ]
              },
              "power": {
                "$ref": "#/components/schemas/PowerSample"
              },
              "production": {
                "$ref": "#/components/schemas/ProductionSample"
              },
              "research": {
                "oneOf": [
                  {
                    "type": "null"
                  },
                  {
                    "$ref": "#/components/schemas/ResearchSample",
                    "description": "Null when nothing is queued -- present and null, so \"we looked and\nnothing was researching\" is distinguishable from \"we never asked\"."
                  }
                ]
              },
              "techs_unlocked": {
                "format": "int32",
                "minimum": 0,
                "type": "integer"
              }
            },
            "required": [
              "techs_unlocked",
              "production",
              "power",
              "kind"
            ],
            "type": "object"
          },
          {
            "description": "Per-machine state, on the same 300-tick beat as [`SampleKind::Force`]\nand written from the same handler, so a machine's `network` can be\njoined to that tick's [`PowerSample::networks`] without interpolating.\n\nThis is what makes \"is this machine actually working?\" answerable. The\nrecord used to carry bot state and force totals and nothing in between,\nso a run that built a cell, set both recipes and charged the chests\ncould not say whether the assemblers had power, held ingredients or\nproduced anything.",
            "properties": {
              "kind": {
                "enum": [
                  "machines"
                ],
                "type": "string"
              },
              "machines": {
                "additionalProperties": {
                  "$ref": "#/components/schemas/MachineSample"
                },
                "description": "Keyed by `unit_number` as a string, not an array.\n\n`helpers.table_to_json` renders an empty Lua table as `{}`, which\nis why a `bots` line with no connected players is unparseable and\nlands in [`ReadSamples::skipped`]. A run's first minutes\nlegitimately have no machines at all, so this shape sidesteps that\ndefect rather than reproducing it: as a map, \"none yet\" is `{}` and\ndecodes.",
                "propertyNames": {
                  "type": "string"
                },
                "type": "object"
              },
              "truncated": {
                "description": "Machines seen but not written, because the sample hit the mod's\nper-line cap. Zero on every run so far. Non-zero means this line is\na prefix of a larger base -- a reader must be able to tell that\nfrom \"this is all of them\", which is why it is a counted field and\nnot a dropped row.",
                "format": "int32",
                "minimum": 0,
                "type": "integer"
              }
            },
            "required": [
              "machines",
              "truncated",
              "kind"
            ],
            "type": "object"
          },
          {
            "description": "A kind this build does not know. Readers skip it; writers never emit it.",
            "properties": {
              "kind": {
                "enum": [
                  "unknown"
                ],
                "type": "string"
              }
            },
            "required": [
              "kind"
            ],
            "type": "object"
          }
        ]
      },
      "SatisfiedReason": {
        "description": "Why a milestone needed no work -- carried on\n[`EventKind::MilestoneSatisfied`].\n\nA milestone can close after zero iterations for two entirely different\nreasons, and until this field existed the record could not tell them\napart: `run-1788277287-11819` closed milestone 4 at the tick it started,\nand there was no way to know whether the bots already had what it asked\nfor or the planner returned an empty plan that the supervisor's \"an empty\nplan means satisfied\" rule then reported as success.",
        "enum": [
          "already_satisfied",
          "plan_empty",
          "unknown"
        ],
        "type": "string"
      },
      "Savepoint": {
        "description": "One savepoint's metadata, written beside the `.zip` it describes.\n\nDeliberately small. Seed, map fingerprint, git commit, Factorio version and\nbuild profile are **not** copied here: they are start-of-run facts and they\nare already in `provenance.json` one directory up, which this file is\nguaranteed to sit next to. Copying them would create a second source of\ntruth for values that cannot change mid-run, and second sources of truth in\nthis archive have been wrong before.\n\n[`ModFingerprint`] is the exception, and it is here because provenance does\nnot carry it and it is the one fact that decides whether this file can be\nsafely loaded at all.",
        "properties": {
          "bytes": {
            "description": "Size of the `.zip`, for the reader deciding whether the archive is\ngetting expensive.",
            "format": "int64",
            "minimum": 0,
            "type": "integer"
          },
          "created_unix": {
            "description": "Unix seconds at which the file finished being written.",
            "format": "int64",
            "minimum": 0,
            "type": "integer"
          },
          "file": {
            "description": "Name of the save file, relative to this metadata file.",
            "type": "string"
          },
          "milestone_index": {
            "description": "Which milestone had just been satisfied, as numbered by the supervisor.",
            "format": "int32",
            "minimum": 0,
            "type": "integer"
          },
          "mods": {
            "oneOf": [
              {
                "type": "null"
              },
              {
                "$ref": "#/components/schemas/ModFingerprint",
                "description": "The mod code the save was written under. See [`ModFingerprint`]."
              }
            ]
          },
          "run_id": {
            "description": "The run that produced it. Also the name of the directory two levels up,\nso the file identifies itself even if it is copied somewhere else.",
            "type": "string"
          },
          "schema": {
            "format": "int32",
            "minimum": 0,
            "type": "integer"
          },
          "tick": {
            "description": "The game tick the save was requested at -- the tick the world in it is\n(very nearly) at. The engine writes at the end of a tick, so the world\non disk may be a tick or two later; nothing here needs it exact and\nclaiming it would be a lie.",
            "format": "int64",
            "minimum": 0,
            "type": "integer"
          }
        },
        "required": [
          "schema",
          "run_id",
          "milestone_index",
          "tick",
          "created_unix",
          "bytes",
          "file"
        ],
        "type": "object"
      },
      "ScriptContent": {
        "properties": {
          "code": {
            "type": "string"
          }
        },
        "required": [
          "code"
        ],
        "type": "object"
      },
      "ScriptTreeNode": {
        "properties": {
          "children": {
            "items": {
              "$ref": "#/components/schemas/ScriptTreeNode"
            },
            "type": "array"
          },
          "key": {
            "type": "string"
          },
          "label": {
            "type": "string"
          },
          "leaf": {
            "type": "boolean"
          }
        },
        "required": [
          "key",
          "label",
          "leaf",
          "children"
        ],
        "type": "object"
      },
      "Split": {
        "description": "One milestone's timing.",
        "properties": {
          "elapsed_ticks": {
            "description": "`ended_tick - started_tick`, or `null` while unfinished. Materialised\nrather than left to every reader to subtract, because a reader that\nsubtracts a null gets zero and zero is a plausible-looking duration.",
            "format": "int64",
            "minimum": 0,
            "type": [
              "integer",
              "null"
            ]
          },
          "ended_tick": {
            "description": "The tick the milestone closed, or `null` when the run ended without\nclosing it. Present-and-null, never absent.",
            "format": "int64",
            "minimum": 0,
            "type": [
              "integer",
              "null"
            ]
          },
          "goal": {
            "type": "string"
          },
          "index": {
            "format": "int32",
            "minimum": 0,
            "type": "integer"
          },
          "outcome": {
            "description": "`satisfied`, `stuck`, `stuck_silent`, `exhausted`, or `unfinished` for a\nmilestone that was still open when the log ended.",
            "type": "string"
          },
          "started_tick": {
            "format": "int64",
            "minimum": 0,
            "type": "integer"
          }
        },
        "required": [
          "index",
          "goal",
          "started_tick",
          "outcome"
        ],
        "type": "object"
      },
      "StartAccepted": {
        "properties": {
          "accepted": {
            "type": "boolean"
          }
        },
        "required": [
          "accepted"
        ],
        "type": "object"
      },
      "SurfaceId": {
        "description": "Which surface something is on, identified by **name**.\n\nSpace Age is enabled in this workspace, so five planets and any number of\norbital platforms are one rocket away. Every run measured here so far has\nbeen a Space Age run that happened never to leave Nauvis, and the only\nthing that kept it that way is one `if` in `on_chunk_generated`\n(`mods/BotBridge/control.lua`) that drops a chunk from any other surface.\nSee `docs/superpowers/notes/2026-09-06-surfaces-survey.md`.\n\n**The name, not the index**, verified against\n`workspace/factorio-api-docs/runtime-api.json` (`application_version`\n2.1.17): `LuaSurface.name` is *\"unique among surfaces\"*, while\n`LuaSurface.index` *\"is assigned when a surface is created, and remains so\nuntil it is deleted. **Indexes of deleted surfaces can be reused.**\"* An\nindex is therefore an identity only within one save and only until a\ndeletion; a record somebody reads next year needs the name.\n\n**This does not go on [`Position`], deliberately.** A coordinate is only\never comparable within one surface, which `Position { x, y }` already says\ncorrectly; putting a surface on it would force `p1 - p2`, `Sub`,\n`manhattan_distance` and `PartialEq` to answer \"what is the distance\nbetween two planets?\", whose honest answer -- undefined -- cannot be\nreturned as an `f64`. The surface belongs on the **container**, which is\nrung one's successor, not on the value.",
        "type": "string"
      },
      "SurfacePollution": {
        "description": "One surface's pollution, and the evolution of the enemies on it.\n\n**Every field is optional and every absence means \"not read\", never\n\"zero\".** The mod writes each key only after a successful `pcall`, so a\nsurface whose reading raised produces no key at all rather than a `0.0`\nthat would read as a measured calm.",
        "properties": {
          "absorbed": {
            "additionalProperties": {
              "format": "double",
              "type": "number"
            },
            "description": "The same statistics' `output_counts`: cumulative pollution absorbed,\nkeyed by absorber (trees, tiles, spawners eating it).",
            "propertyNames": {
              "type": "string"
            },
            "type": [
              "object",
              "null"
            ]
          },
          "at_spawn": {
            "description": "`LuaSurface.get_pollution(position)` at the player force's spawn.\n\nThat call is a **chunk** reading, so it needs a position, and spawn is\nthe one position that means the same thing on every run of this\nproject: the roster starts there and the first blocks are built around\nit. A grid of probes was rejected -- it costs O(chunks) per beat and\nanswers a question nobody has asked.\n\n**It read exactly `0.0` in 375 of 375 samples on both arms of the first\nlive measurement, and that is a real reading, not a broken accessor.**\nA uniformly constant field is the tell for a plumbing failure, so it was\nchecked against a running game rather than believed:\n`surface.pollute({0,0}, 50)` then `get_pollution({0,0})` answers `50`.\n*Why* the spawn chunk held none while the surface held up to 41 is\n**not established** -- the build sitting in a neighbouring chunk with\nabsorption eating the spread is a hypothesis, not a measurement. See\n`docs/superpowers/notes/2026-09-08-what-our-own-pollution-provokes.md`.",
            "format": "double",
            "type": [
              "number",
              "null"
            ]
          },
          "evolution": {
            "oneOf": [
              {
                "type": "null"
              },
              {
                "$ref": "#/components/schemas/EvolutionSample",
                "description": "The `enemy` force's evolution on this surface, or `None` when there is\nno enemy force or the read raised."
              }
            ]
          },
          "pollutant": {
            "description": "`LuaSurface.pollutant_type`'s prototype name -- `pollution` on Nauvis,\n`spores` on Gleba, and `None` on a surface with no pollutant at all\n(a space platform). Without it the numbers below are unlabelled: two\nsurfaces' \"pollution\" are not the same substance.",
            "type": [
              "string",
              "null"
            ]
          },
          "produced": {
            "additionalProperties": {
              "format": "double",
              "type": "number"
            },
            "description": "`game.get_pollution_statistics(surface).input_counts`: cumulative\npollution emitted, keyed by the prototype that emitted it. This is the\nhalf that can say *which machine* -- the \"is it the steam engines or\nthe radar\" question, answered rather than argued.",
            "propertyNames": {
              "type": "string"
            },
            "type": [
              "object",
              "null"
            ]
          },
          "total": {
            "description": "`LuaSurface.get_total_pollution()`: the whole surface, cheap and\ncomparable across runs. The denominator every other reading here is\njudged against.",
            "format": "double",
            "type": [
              "number",
              "null"
            ]
          }
        },
        "type": "object"
      },
      "TickKind": {
        "description": "What one `ticks.jsonl` line is.\n\nShort keys (`t`, `w`, `k`) because there are thousands of these lines and\nthis file's only job is to be a table.",
        "enum": [
          "sample",
          "start",
          "gap",
          "stop"
        ],
        "type": "string"
      },
      "TickRange": {
        "description": "The observed span of a recording's clock.",
        "properties": {
          "from": {
            "format": "int64",
            "minimum": 0,
            "type": "integer"
          },
          "to": {
            "format": "int64",
            "minimum": 0,
            "type": "integer"
          }
        },
        "required": [
          "from",
          "to"
        ],
        "type": "object"
      },
      "TickSample": {
        "description": "One line of `ticks.jsonl`, and one element of `GET /api/v1/video/ticks`.\n\nDeliberately one type for both. The file *is* the wire format; a second\nstruct for the HTTP side is how two halves of a system come to disagree\nabout what a sample is.",
        "properties": {
          "k": {
            "$ref": "#/components/schemas/TickKind",
            "description": "Always on the wire, even for the ordinary [`TickKind::Sample`].\n\nThe design's example lines omit it, and dropping it would save ~11 bytes\na line -- 60 KB across a 45-minute run, against a recording measured in\nhundreds of megabytes. It is kept because this struct is also the HTTP\nwire shape, and a key that is sometimes absent has to be declared\noptional in `app/src/api/types.ts`, where \"absent\" and \"null\" then become\na distinction the client has to make and this format never intends. A\nline written *without* `k` still parses, via `default`, so every document\nthe design describes is still readable.\n\n`#[schema(required)]` because the **key is always on the wire**: there is\nno `skip_serializing_if`, so serde always emits it. Without this utoipa\nwould publish it as optional purely because `#[serde(default)]` lets a\n*reader* omit it, and a generated client would then have to handle an\nabsence this server never produces."
          },
          "reason": {
            "description": "Why, for a gap. Absent otherwise.",
            "type": [
              "string",
              "null"
            ]
          },
          "t": {
            "description": "`game.tick` at the observation.\n\nAbsent on a [`TickKind::Gap`] line, and only there: a gap records that\nnothing was observed, which is not a tick with an unknown value but the\nabsence of an observation altogether.",
            "format": "int64",
            "minimum": 0,
            "type": [
              "integer",
              "null"
            ]
          },
          "w": {
            "description": "Milliseconds since the recorder's own epoch -- the same epoch\n`VideoRecord::calibration` measures the video's PTS against.\n\nStamped at the **midpoint of send and receive**, never at receipt. The\ntick is the value inside the game at the moment the command ran, which\nis somewhere between the two; receipt time carries a systematic late\nbias of one full round trip, while the midpoint bounds the error at\nhalf a round trip and centres it on zero.",
            "format": "int64",
            "minimum": 0,
            "type": "integer"
          }
        },
        "required": [
          "w",
          "k"
        ],
        "type": "object"
      },
      "TileFluid": {
        "description": "Which fluid an **offshore pump standing on this tile** would draw from it.\n\n# The question a fluidbox filter turned out not to answer\n\nA Factorio 2.0 offshore pump takes its fluid from the *tile*, not from its\nown prototype: `LuaEntity::get_fluid_source_fluid` is documented as\n*\"Checks what is expected fluid to be produced from the offshore pump's\nsource tile\"*, and its output fluidbox is honestly\n[`FluidFilter::Any`]. So \"what does a pump here produce\" is a question\nabout ground, and this is the field that carries the answer.\n\n**`get_fluid_source_fluid` is not the crossing, and cannot be.** It is a\n*method* on `LuaEntity` with `subclasses: [\"OffshorePump\"]` (checked in\n`workspace/factorio-api-docs/runtime-api.json`, `application_version`\n2.1.17), so it can only be asked of a pump that already stands. A planner\ndeciding *where* to put one has no such entity. The prototype-side answer\nis `LuaTilePrototype::fluid`, an optional `LuaFluidPrototype` described as\n*\"The fluid offshore pump produces on this tile, if any\"* -- the same fact,\navailable with no entity and no runtime call, which is what the mod sends.\n\n# Three answers, and the two that look alike are the point\n\nThe same shape as [`FluidFilter`], for the same reason -- an\n`Option<String>` has two slots for three states:\n\n- [`TileFluid::Yields`] -- *\"a `water` tile gives `water`\"*. Definite.\n- [`TileFluid::Dry`] -- *\"a `grass-1` tile gives nothing\"*. **Also\n  definite, and a completely different fact.** It is what makes a refusal\n  (\"there is no water here\") sayable.\n- [`TileFluid::Unknown`] -- the sender never said: an archived dump, a mod\n  predating this field, a read that raised, or a chunk whose covered tiles\n  the bulk writeout would have had to guess at. Not a fact about the game.\n\n[`Self::yields`] and [`Self::is_dry`] are both *positive* claims and both\nare false for `Unknown`; neither is the negation of the other. A single\n`has_fluid(name) -> bool` could not distinguish its own two meanings, which\nis the defect class this type exists to avoid.\n\n# Why it is not a `bool` called `is_water`\n\n[`FactorioTile::is_water`] tests the name against a hard-coded pair,\n`[\"deepwater\", \"water\"]`, read off a *vanilla* capture. This install runs\nSpace Age, where `ammoniacal-ocean` yields ammonia and Vulcanus' lava\nyields lava, and where none of those names appear in that list. Naming the\nfluid rather than the tile is what makes the rule survive a mod -- the\nsame argument that replaced the hard-coded smelting rate.",
        "oneOf": [
          {
            "description": "The tile prototype names the fluid a pump on it would produce.",
            "properties": {
              "fluid": {
                "type": "string"
              },
              "kind": {
                "enum": [
                  "yields"
                ],
                "type": "string"
              }
            },
            "required": [
              "fluid",
              "kind"
            ],
            "type": "object"
          },
          {
            "description": "The tile prototype names no fluid: a pump here produces nothing. An\nanswer, not an absence.",
            "properties": {
              "kind": {
                "enum": [
                  "dry"
                ],
                "type": "string"
              }
            },
            "required": [
              "kind"
            ],
            "type": "object"
          },
          {
            "description": "Nobody said. The default, so an archived record or an older mod\ndeserialises to *\"not captured\"* rather than to `Dry`.",
            "properties": {
              "kind": {
                "enum": [
                  "unknown"
                ],
                "type": "string"
              }
            },
            "required": [
              "kind"
            ],
            "type": "object"
          }
        ]
      },
      "TransportLine": {
        "description": "One of a belt-like entity's transport lines, and what is riding on it.\n\nA belt is not an inventory and modelling it as one loses the thing that\nmakes it a belt: a `transport-belt` has **two** lanes, an\n`underground-belt` four and a `splitter` eight, and which lane an item is\non decides whether a furnace arm can reach it. Two of this project's own\nmeasured failures are lane failures -- an inserter drops on the belt's\n**far** lane while a side-loading belt lands on the **near** one, and\ngetting either backwards puts ore and coal on one lane where they crowd\neach other out while every entity still places 100% correctly.\n\n**Counts, not positions.** `LuaTransportLine` also offers\n`get_detailed_contents()`, which is every item with its position along the\nline; the owner's ruling at scale is *\"the direction and, for a whole\nchain, what types of items are on it\"*, so this carries\n`get_contents()` -- one aggregated count per item kind -- and no item\npositions at all.",
        "properties": {
          "contents": {
            "description": "What is on this lane, by item kind. Empty is a real and ordinary\nanswer -- most lanes of most belts are empty most of the time -- and it\nis *not* the same as the lane not existing, which is expressed by the\nlane being absent from [`FactorioEntity::transport_lines`] entirely.",
            "items": {
              "$ref": "#/components/schemas/InventoryItemWithQuality"
            },
            "type": "array"
          },
          "line": {
            "description": "`defines.transport_line`'s own name for this line: `left_line`,\n`right_line`, `left_underground_line`, `secondary_right_line`,\n`left_split_line` and so on.\n\n**The name, not the raw index**, because the index alone is\nuninterpretable: line 3 is `left_underground_line` on an\nunderground-belt and something else on a splitter, so a caller reading\na number would have to re-derive the mapping from the entity type and\nwould be inventing it. An index this build's `defines` cannot name\narrives as `unmapped_<n>` rather than as a bare number, the same way\n`machine_row` reports an unknown entity status.",
            "type": "string"
          }
        },
        "required": [
          "line"
        ],
        "type": "object"
      },
      "UndergroundHalf": {
        "description": "Which half of an underground-belt pair an entity is.\n\nCarried onto `FactorioEntity::underground_half` (`crates/core/src/types.rs`)\nso the placement path -- ultimately `rcon_place_entity` in\n`mods/BotBridge/control.lua`, which sends this as `type` to\n`surface.create_entity` -- can tell the two halves of a pair apart.\n`#[serde(rename_all = \"snake_case\")]` gives the wire spelling `\"input\"` /\n`\"output\"`, which is also Factorio's own `belt_to_ground_type` spelling.",
        "enum": [
          "input",
          "output"
        ],
        "type": "string"
      },
      "VideoManifest": {
        "description": "Everything a caller needs to know about one video directory, derived from a\ndirectory listing every time it is asked for.\n\n**One type for the live directory and the archived one.** They hold the same\nfour files and answer the same questions; two shapes would be two chances to\ndisagree about what a recording is.",
        "properties": {
          "bytes": {
            "description": "The recording's size on disk, `null` when there is no file. A caller can\ntell \"the encoder was started and produced nothing\" from \"no encoder was\nstarted\" by pairing this with `video`.",
            "format": "int64",
            "minimum": 0,
            "type": [
              "integer",
              "null"
            ]
          },
          "run": {
            "description": "The opaque run identifier from `video/run.json`, or `null` when the\ndirectory has no readable sidecar.\n\n`null` means *unknown*, never *no match*: a consumer compares it for\nequality and does nothing else with it. Absence is not evidence of a\nmismatch -- treating it as one refuses a good join.",
            "type": [
              "string",
              "null"
            ]
          },
          "samples": {
            "description": "How many lines `ticks.jsonl` holds, gaps included.",
            "minimum": 0,
            "type": "integer"
          },
          "skipped": {
            "description": "Lines of `ticks.jsonl` that did not parse -- in practice the torn last\nline of a killed recorder. Reported rather than swallowed.",
            "minimum": 0,
            "type": "integer"
          },
          "tick_range": {
            "oneOf": [
              {
                "type": "null"
              },
              {
                "$ref": "#/components/schemas/TickRange",
                "description": "The span of ticks the clock actually observed, `null` when it observed\nnone.\n\nPublished so the viewer can size its axis without fetching thousands of\nsamples: `runTimeline.ts` feeds this into the `drawn` set that decides\nwhere the axis starts."
              }
            ]
          },
          "video": {
            "oneOf": [
              {
                "type": "null"
              },
              {
                "$ref": "#/components/schemas/VideoRecord",
                "description": "`video.json`, or `null` when the recorder never wrote one (which for a\nlive directory means video was never asked for on this run)."
              }
            ]
          }
        },
        "required": [
          "run",
          "video",
          "bytes",
          "samples",
          "skipped",
          "tick_range"
        ],
        "type": "object"
      },
      "VideoRecord": {
        "description": "`video.json`: what the encoder was asked for, what it did, and how it ended.",
        "properties": {
          "calibration": {
            "description": "The calibration pairs, in order. Zero on a failed start, one on a\nrecording that never reached a clean stop, two normally.",
            "items": {
              "$ref": "#/components/schemas/Calibration"
            },
            "type": "array"
          },
          "ffmpeg_exit": {
            "description": "ffmpeg's exit code, `null` when it never ran or was killed by a signal.",
            "format": "int32",
            "type": [
              "integer",
              "null"
            ]
          },
          "file": {
            "description": "The recording's filename inside the video directory.",
            "type": "string"
          },
          "fps": {
            "format": "int32",
            "minimum": 0,
            "type": "integer"
          },
          "height": {
            "format": "int32",
            "minimum": 0,
            "type": "integer"
          },
          "rate_ok": {
            "description": "Whether the two calibration pairs agree on the rate within\n[`RATE_TOLERANCE`]. `null` when there are not two pairs to compare --\n*unknown*, never \"fine\".",
            "type": [
              "boolean",
              "null"
            ]
          },
          "reason": {
            "description": "Why, for a `failed`, `died` or `stopped_low_disk` recording.",
            "type": [
              "string",
              "null"
            ]
          },
          "requested_height": {
            "format": "int32",
            "minimum": 0,
            "type": "integer"
          },
          "requested_width": {
            "description": "The geometry that was *asked for*. Kept beside the observed one rather\nthan instead of it: a tiling compositor ignores a size request unless\nthe window is floated, and a mismatch is a warning on the run, not a\nfailure -- the video is still usable and still joins on ticks.",
            "format": "int32",
            "minimum": 0,
            "type": "integer"
          },
          "run": {
            "description": "The run this recording belongs to.",
            "type": "string"
          },
          "status": {
            "$ref": "#/components/schemas/VideoStatus"
          },
          "width": {
            "description": "The geometry `xwininfo` **observed**, which is what was actually\ncaptured. Zero on a recording that never started.",
            "format": "int32",
            "minimum": 0,
            "type": "integer"
          },
          "window_id": {
            "description": "The X11 window that was filmed, `0x`-prefixed. `null` when none was\nresolved.",
            "type": [
              "string",
              "null"
            ]
          }
        },
        "required": [
          "run",
          "file",
          "width",
          "height",
          "requested_width",
          "requested_height",
          "fps",
          "status",
          "reason",
          "ffmpeg_exit",
          "calibration",
          "rate_ok",
          "window_id"
        ],
        "type": "object"
      },
      "VideoStatus": {
        "description": "Where a recording got to.",
        "enum": [
          "recording",
          "stopped",
          "killed",
          "died",
          "failed",
          "stopped_low_disk"
        ],
        "type": "string"
      },
      "VideoTicksResponse": {
        "description": "`GET /api/v1/video/ticks` response.\n\nThe whole clock, in file order. The viewer interpolates between the two\nnearest samples and **refuses to interpolate across a gap** -- which is why\nthe gap lines are in this list rather than filtered out of it.",
        "properties": {
          "samples": {
            "items": {
              "$ref": "#/components/schemas/TickSample"
            },
            "type": "array"
          },
          "skipped": {
            "description": "Lines that did not parse -- in practice the torn last line of a recorder\nthat was killed. Reported rather than swallowed, matching\n`EventsResponse.skipped`.",
            "minimum": 0,
            "type": "integer"
          }
        },
        "required": [
          "samples",
          "skipped"
        ],
        "type": "object"
      },
      "WaitingStep": {
        "description": "One step that is waiting rather than working, carried on\n[`EventKind::BatchProgress`].\n\nThe identity half is deliberately the same three fields\n[`EventKind::ActionDispatched`] carries -- `id`, `bot`, `action`, `target`\n-- so a reader who finds a step here and later finds its dispatch line is\nlooking at the same names, and can join on `id` without a second vocabulary\nto learn.",
        "properties": {
          "action": {
            "description": "What the plan called it, verbatim: `Action::label`, the same string\n[`EventKind::ActionDispatched::action`] and [`PlannedStep::action`]\ncarry. For a walk, a description of the walk, because a walk has no\nlabel of its own.",
            "type": "string"
          },
          "blocked_by": {
            "description": "The step this one is blocked by, for `predecessor` and\n`background_conflict`. `None` for the others, which are not waiting on\nanother step at all.",
            "format": "int32",
            "minimum": 0,
            "type": [
              "integer",
              "null"
            ]
          },
          "bot": {
            "description": "The bot whose step this is, from the schedule -- the only thing that\nknows, since an action does not carry its bot.",
            "format": "int32",
            "minimum": 0,
            "type": "integer"
          },
          "deadline_tick": {
            "description": "For `lag_deadline`, the absolute `game.tick` the wait runs until when a\npredecessor supplied a finish tick to anchor it to. A reader compares\nit to the event's own `tick` to see how much of the wait is left --\nwhich is the difference between \"obediently smelting\" and \"stuck\".\n`None` for every other state, and also for a lag nothing could anchor.",
            "format": "int64",
            "minimum": 0,
            "type": [
              "integer",
              "null"
            ]
          },
          "id": {
            "description": "The action's `ActionId`, joinable to [`EventKind::ActionDispatched`]'s\n`id` and to [`PlannedStep::id`].\n\n`None` for a **walk**, which has no `ActionId` at all -- the scheduler\nemits it as its own step kind. A true absence, exactly as in\n[`EventKind::WalkDispatched`], and not a failure to look it up. A walk\nis identified by `bot` plus `step_index` instead.",
            "format": "int32",
            "minimum": 0,
            "type": [
              "integer",
              "null"
            ]
          },
          "step_index": {
            "description": "Which walk this is, in this bot's own slice of the schedule -- the same\nkey [`EventKind::WalkDispatched::step_index`] uses, and subject to the\nsame warning: **it is not an `ActionId` and not an index into\n[`EventKind::PlanCreated`]'s `plan`.** `None` for an action.",
            "format": "int32",
            "minimum": 0,
            "type": [
              "integer",
              "null"
            ]
          },
          "target": {
            "oneOf": [
              {
                "type": "null"
              },
              {
                "$ref": "#/components/schemas/Position",
                "description": "Where the plan sent it, when it sends it anywhere. Same meaning and\nsame caveats as [`EventKind::ActionDispatched::target`]: the planner's\nintent, `None` for a `craft`/`research`, and the walk's destination for\na walk."
              }
            ]
          },
          "waiting_ms": {
            "description": "How long this step has been in this state, in wall-clock milliseconds,\nmeasured by the executor from the moment it entered the state.\n\n**Not** the interval rounded, and not \"since a heartbeat first noticed\nit\": a wait that began between two beats reports its true age on the\nfirst beat that sees it. Wall clock rather than ticks for the same\nreason `elapsed_ms` is -- a stopped game is exactly the case the tick\nclock cannot answer.\n\nThe clock restarts when the state changes, so a step that waited on a\npredecessor and then served that predecessor's lag reports the age of\nthe lag wait, not of both together.",
            "format": "int64",
            "minimum": 0,
            "type": "integer"
          },
          "waiting_on": {
            "description": "**What it is waiting for.** One of `predecessor`,\n`background_conflict`, `lag_deadline`, `research`, `reply` or `walk` --\n`factorio_bot_executor::WaitKind::name`, which is a stable string\nrather than a `Debug` rendering so archived runs keep grouping with new\nones.\n\nThe three that used to be indistinguishable are `predecessor` (blocked\non another step, possibly another bot's), `lag_deadline` (serving\nmachine time the plan asked for -- **not a fault**, however long) and\n`reply` (dispatched, and the game has not answered).\n\nA `reply` whose `waiting_ms` is past ~360,000 is a finding on its own:\nthe RCON layer gives a dispatched action 360 wall-clock seconds and\nthen reports it lost, so a longer one means the time went into a path\nrequest, an out-of-reach `move_player` or the placement retry loop --\nall of which happen inside the same call. This is why the run that\nprompted the field reported `lost: 0` through an eleven-minute silence\nand was **right** to.",
            "type": "string"
          }
        },
        "required": [
          "bot",
          "action",
          "waiting_on",
          "waiting_ms"
        ],
        "type": "object"
      },
      "WalkFailure": {
        "description": "A structured walk failure, carried *beside* [`EventKind::WalkSettled`]'s\n`error` string rather than instead of it, for the same reason\n[`ActionFailure`] is: the string is what a person reads, the kind is what a\nquery groups by, and replacing one with the other loses an audience.",
        "properties": {
          "destination": {
            "oneOf": [
              {
                "type": "null"
              },
              {
                "$ref": "#/components/schemas/Position",
                "description": "**Observed.** The destination the mod was actually steering to: the\nlast waypoint of the path the *game* returned.\n\nNot the same thing as [`EventKind::WalkSettled`]'s `to`, and the\ndifference is the finding. `to` is what the schedule asked for; this is\nwhat the walk was really trying to reach after Factorio answered the\npath request, and in run 30 all three failures had this land strictly\ninside the collision box of a furnace the run had placed, up to 1.2\ntiles from the `to` that was requested. Recording only `to` would have\nhidden exactly the fact that had to be reconstructed by hand from\n`workspace/server-log.txt`."
              }
            ]
          },
          "from": {
            "oneOf": [
              {
                "type": "null"
              },
              {
                "$ref": "#/components/schemas/Position",
                "description": "**Observed.** Where the character actually stood when the mod gave up,\nas the mod's own message named it. `None` for every failure whose\nwording carries no position.\n\nThis is the one place the walk record states a real position of a bot,\nand it is trustworthy in a way `on_player_changed_position` is not: the\nmod read `player.character.position` at that instant rather than\ninferring it from the last tile boundary crossed."
              }
            ]
          },
          "kind": {
            "$ref": "#/components/schemas/WalkFailureKind"
          }
        },
        "required": [
          "kind"
        ],
        "type": "object"
      },
      "WalkFailureKind": {
        "description": "Why a walk did not arrive.\n\nA separate enum from [`FailureKind`] rather than more variants on it,\nbecause the two classify different things and share only the word\n\"failure\": every distinction here is about the *pathfinder*, and none of\n[`FailureKind`]'s five substantive variants has any meaning for a walk.\nMerging them would produce one enum where two thirds of the variants are\ninapplicable to whichever event you are holding.\n\nThe distinction the run record exists to preserve is\n[`WalkFailureKind::NoPath`] against [`WalkFailureKind::PathfinderBusy`].\n`FactorioRcon::player_path_attempt` (`crates/core/src/factorio/rcon.rs`)\nbranches on exactly that: `try again later` means the request queue was full\nand **nothing was searched**, so asking again is the right thing to do and\nit does; `failed to path find` means the pathfinder searched and found\nnothing, which is a fact about the destination that no amount of repeating\nwill change. A record that collapsed them would say \"the walk failed\" for\nboth and leave the reader to guess which of \"try again\" and \"this place is\nunreachable\" applies.\n\n**Two of these variants are archive-only.** BotBridge used to re-path a\nstalled walk for itself, and produced `PathfinderBusy` and `RepathLimit`\nfrom wordings of its own; that machinery is gone -- retrying a stuck walk\nlives in `FactorioRcon::move_player_timed`, where the goal, the radius and\nthe standability judgement are. The variants stay because\n`workspace/runs` is full of runs that used them, and this enum is what\nreads those runs.",
        "enum": [
          "no_path",
          "pathfinder_busy",
          "repath_limit",
          "stalled",
          "timeout",
          "no_character",
          "destination_blocked",
          "boxed_in",
          "other"
        ],
        "type": "string"
      },
      "WalkStallRecord": {
        "description": "One stall a walk was retried through, as `events.jsonl` carries it.\n\nThe archive's shape of [`crate::factorio::rcon::WalkStall`]: the same facts,\nwith the blocker reduced to what a query groups by. The full clause is still\nin `error`, and [`crate::factorio::rcon::walk_blocker`] re-reads it, so\nnothing is lost -- the reduction only spares the record a nested object\nwhose fields no reader has asked for yet.\n\n**This exists because a recovered stall used to leave no trace at all.**\n`FactorioRcon::move_player_timed` answers a stalled leg with a fresh path\nand the walk then settles `success`; the only evidence was one `warn!` on\nstdout, which the next run overwrites. Counted 2026-09-08 over\n`workspace/session-logs`: **17 recovered stalls, none of them in any\n`events.jsonl`**, against exactly one stall the whole 80-file archive holds\n-- the `tree-01` of `run-1788833726-34821`, which is the one the retry could\nnot fix. A record that shows 1 of 18 cannot tell a healthy run from a run\nrecovery is carrying.",
        "properties": {
          "attempt": {
            "description": "Which attempt stalled, counting from 1.",
            "format": "int32",
            "minimum": 0,
            "type": "integer"
          },
          "blocker": {
            "description": "What the mod's probe found in the way, as\n[`crate::factorio::rcon::WalkBlockerKind::name`] spells it\n(`character`, `tree`, `entity`, `nothing`, `probe_failed`, ...).\n\n**`None` means the message carried no clause this build could read** --\nan older mod, or a reworded one. It does not mean the tile was clear:\nthat is `\"nothing\"`, which is an answer. And a wording this build's\ngrammar does not cover is `\"unknown\"`, a third thing again; the census\nthat read 19 of 20 walk failures as `other` is what those three\nseparate answers exist to prevent.\n\nA string rather than the enum, for the reason\n[`EventKind::Teleport`]'s `reason` gives: the record does not need to\npublish a closed list, and a name that survives a re-read verbatim\ncannot be silently remapped onto the wrong variant on the way in.",
            "type": [
              "string",
              "null"
            ]
          },
          "blocker_name": {
            "description": "The prototype name of whatever was in the way (`tree-01`,\n`stone-furnace`), when the blocker had one.",
            "type": [
              "string",
              "null"
            ]
          },
          "error": {
            "description": "The verdict as the game and the mod worded it. Kept because `blocker`\nis derived from it and a reader must be able to check the derivation --\nthe `kind: \"other\"` census that cost this project a day was exactly a\nclassifier that had fallen behind the wording.",
            "type": "string"
          },
          "tick": {
            "description": "`game.tick` the stalled attempt was dispatched at, when the game\nstamped one. `None` is a tick nobody observed, never tick zero.",
            "format": "int64",
            "minimum": 0,
            "type": [
              "integer",
              "null"
            ]
          }
        },
        "required": [
          "attempt",
          "error"
        ],
        "type": "object"
      },
      "u8": {
        "format": "int32",
        "minimum": 0,
        "type": "integer"
      }
    }
  },
  "info": {
    "contact": {
      "email": "arturh@arturh.de",
      "name": "Artur Hallmann"
    },
    "description": "factorio-bot HTTP API",
    "license": {
      "name": ""
    },
    "title": "factorio-bot",
    "version": "0.2.4-dev"
  },
  "openapi": "3.1.0",
  "paths": {
    "/api/v1/fs/exists": {
      "get": {
        "operationId": "exists",
        "parameters": [
          {
            "in": "query",
            "name": "path",
            "required": true,
            "schema": {
              "type": "string"
            }
          }
        ],
        "responses": {
          "200": {
            "content": {
              "application/json": {
                "schema": {
                  "$ref": "#/components/schemas/ExistsResponse"
                }
              }
            },
            "description": ""
          },
          "400": {
            "content": {
              "application/json": {
                "schema": {
                  "$ref": "#/components/schemas/ErrorResponse"
                }
              }
            },
            "description": ""
          }
        },
        "summary": "Reports whether a path exists on the server",
        "tags": [
          "Admin"
        ]
      }
    },
    "/api/v1/game/add-research": {
      "post": {
        "operationId": "add_research",
        "requestBody": {
          "content": {
            "application/json": {
              "schema": {
                "$ref": "#/components/schemas/AddResearchBody"
              }
            }
          },
          "required": true
        },
        "responses": {
          "200": {
            "content": {
              "application/json": {
                "schema": {
                  "$ref": "#/components/schemas/OperationResult"
                }
              }
            },
            "description": ""
          },
          "400": {
            "content": {
              "application/json": {
                "schema": {
                  "$ref": "#/components/schemas/ErrorResponse"
                }
              }
            },
            "description": ""
          }
        },
        "summary": "Add Research to Queue",
        "tags": [
          "Control"
        ]
      }
    },
    "/api/v1/game/all-players": {
      "get": {
        "operationId": "all_players",
        "responses": {
          "200": {
            "content": {
              "application/json": {
                "schema": {
                  "items": {
                    "$ref": "#/components/schemas/FactorioPlayer"
                  },
                  "type": "array"
                }
              }
            },
            "description": ""
          },
          "400": {
            "content": {
              "application/json": {
                "schema": {
                  "$ref": "#/components/schemas/ErrorResponse"
                }
              }
            },
            "description": ""
          }
        },
        "summary": "List all connected Players",
        "tags": [
          "Query"
        ]
      }
    },
    "/api/v1/game/cheat-all-technologies": {
      "post": {
        "operationId": "cheat_all_technologies",
        "responses": {
          "200": {
            "content": {
              "application/json": {
                "schema": {
                  "$ref": "#/components/schemas/OperationResult"
                }
              }
            },
            "description": ""
          },
          "400": {
            "content": {
              "application/json": {
                "schema": {
                  "$ref": "#/components/schemas/ErrorResponse"
                }
              }
            },
            "description": ""
          }
        },
        "summary": "Cheat all Technologies",
        "tags": [
          "Control"
        ]
      }
    },
    "/api/v1/game/cheat-item": {
      "post": {
        "operationId": "cheat_item",
        "requestBody": {
          "content": {
            "application/json": {
              "schema": {
                "$ref": "#/components/schemas/CheatItemBody"
              }
            }
          },
          "required": true
        },
        "responses": {
          "200": {
            "content": {
              "application/json": {
                "schema": {
                  "$ref": "#/components/schemas/FactorioPlayer"
                }
              }
            },
            "description": ""
          },
          "400": {
            "content": {
              "application/json": {
                "schema": {
                  "$ref": "#/components/schemas/ErrorResponse"
                }
              }
            },
            "description": ""
          }
        },
        "summary": "Cheat items and give them to player",
        "tags": [
          "Control"
        ]
      }
    },
    "/api/v1/game/cheat-technology": {
      "post": {
        "operationId": "cheat_technology",
        "requestBody": {
          "content": {
            "application/json": {
              "schema": {
                "$ref": "#/components/schemas/CheatTechnologyBody"
              }
            }
          },
          "required": true
        },
        "responses": {
          "200": {
            "content": {
              "application/json": {
                "schema": {
                  "$ref": "#/components/schemas/OperationResult"
                }
              }
            },
            "description": ""
          },
          "400": {
            "content": {
              "application/json": {
                "schema": {
                  "$ref": "#/components/schemas/ErrorResponse"
                }
              }
            },
            "description": ""
          }
        },
        "summary": "Cheat Technology",
        "tags": [
          "Control"
        ]
      }
    },
    "/api/v1/game/entity-prototypes": {
      "get": {
        "operationId": "entity_prototypes",
        "responses": {
          "200": {
            "content": {
              "application/json": {
                "schema": {
                  "additionalProperties": {
                    "$ref": "#/components/schemas/FactorioEntityPrototype"
                  },
                  "propertyNames": {
                    "type": "string"
                  },
                  "type": "object"
                }
              }
            },
            "description": ""
          },
          "400": {
            "content": {
              "application/json": {
                "schema": {
                  "$ref": "#/components/schemas/ErrorResponse"
                }
              }
            },
            "description": ""
          }
        },
        "summary": "List all EntityPrototypes",
        "tags": [
          "Query"
        ]
      }
    },
    "/api/v1/game/find-entities": {
      "get": {
        "operationId": "find_entities",
        "parameters": [
          {
            "in": "query",
            "name": "area",
            "required": false,
            "schema": {
              "type": "string"
            }
          },
          {
            "in": "query",
            "name": "position",
            "required": false,
            "schema": {
              "type": "string"
            }
          },
          {
            "in": "query",
            "name": "radius",
            "required": false,
            "schema": {
              "format": "double",
              "type": "number"
            }
          },
          {
            "in": "query",
            "name": "name",
            "required": false,
            "schema": {
              "type": "string"
            }
          },
          {
            "in": "query",
            "name": "entity_type",
            "required": false,
            "schema": {
              "type": "string"
            }
          }
        ],
        "responses": {
          "200": {
            "content": {
              "application/json": {
                "schema": {
                  "items": {
                    "$ref": "#/components/schemas/FactorioEntity"
                  },
                  "type": "array"
                }
              }
            },
            "description": ""
          },
          "400": {
            "content": {
              "application/json": {
                "schema": {
                  "$ref": "#/components/schemas/ErrorResponse"
                }
              }
            },
            "description": ""
          }
        },
        "summary": "Finds entities in given area/radius",
        "tags": [
          "Query"
        ]
      }
    },
    "/api/v1/game/find-tiles": {
      "get": {
        "operationId": "find_tiles",
        "parameters": [
          {
            "in": "query",
            "name": "area",
            "required": false,
            "schema": {
              "type": "string"
            }
          },
          {
            "in": "query",
            "name": "position",
            "required": false,
            "schema": {
              "type": "string"
            }
          },
          {
            "in": "query",
            "name": "radius",
            "required": false,
            "schema": {
              "format": "double",
              "type": "number"
            }
          },
          {
            "in": "query",
            "name": "name",
            "required": false,
            "schema": {
              "type": "string"
            }
          }
        ],
        "responses": {
          "200": {
            "content": {
              "application/json": {
                "schema": {
                  "items": {
                    "$ref": "#/components/schemas/FactorioTile"
                  },
                  "type": "array"
                }
              }
            },
            "description": ""
          },
          "400": {
            "content": {
              "application/json": {
                "schema": {
                  "$ref": "#/components/schemas/ErrorResponse"
                }
              }
            },
            "description": ""
          }
        },
        "summary": "Finds tiles in given area/radius",
        "tags": [
          "Query"
        ]
      }
    },
    "/api/v1/game/flow": {
      "get": {
        "description": "`tick` comes from the instance's own RCON connection, not from the\ncaller: a live query has exactly one tick worth reporting, the game's\ncurrent one.",
        "operationId": "flow",
        "responses": {
          "200": {
            "content": {
              "application/json": {
                "schema": {
                  "$ref": "#/components/schemas/FlowExport"
                }
              }
            },
            "description": ""
          },
          "400": {
            "content": {
              "application/json": {
                "schema": {
                  "$ref": "#/components/schemas/ErrorResponse"
                }
              }
            },
            "description": ""
          }
        },
        "summary": "The flow graph for the running world's one surface -- the live analogue\nof `GET /api/v1/runs/{id}/flow`'s archived keyframes.",
        "tags": [
          "Query"
        ]
      }
    },
    "/api/v1/game/insert-to-inventory": {
      "post": {
        "operationId": "insert_to_inventory",
        "requestBody": {
          "content": {
            "application/json": {
              "schema": {
                "$ref": "#/components/schemas/InsertToInventoryBody"
              }
            }
          },
          "required": true
        },
        "responses": {
          "200": {
            "content": {
              "application/json": {
                "schema": {
                  "$ref": "#/components/schemas/FactorioPlayer"
                }
              }
            },
            "description": ""
          },
          "400": {
            "content": {
              "application/json": {
                "schema": {
                  "$ref": "#/components/schemas/ErrorResponse"
                }
              }
            },
            "description": ""
          }
        },
        "summary": "Insert items into inventory",
        "tags": [
          "Control"
        ]
      }
    },
    "/api/v1/game/inventory-contents-at": {
      "get": {
        "operationId": "inventory_contents_at",
        "parameters": [
          {
            "in": "query",
            "name": "query",
            "required": true,
            "schema": {
              "type": "string"
            }
          }
        ],
        "responses": {
          "200": {
            "content": {
              "application/json": {
                "schema": {
                  "items": {
                    "oneOf": [
                      {
                        "type": "null"
                      },
                      {
                        "$ref": "#/components/schemas/InventoryResponse"
                      }
                    ]
                  },
                  "type": "array"
                }
              }
            },
            "description": ""
          },
          "400": {
            "content": {
              "application/json": {
                "schema": {
                  "$ref": "#/components/schemas/ErrorResponse"
                }
              }
            },
            "description": ""
          }
        },
        "summary": "List inventory contents at position",
        "tags": [
          "Query"
        ]
      }
    },
    "/api/v1/game/item-prototypes": {
      "get": {
        "operationId": "item_prototypes",
        "responses": {
          "200": {
            "content": {
              "application/json": {
                "schema": {
                  "additionalProperties": {
                    "$ref": "#/components/schemas/FactorioItemPrototype"
                  },
                  "propertyNames": {
                    "type": "string"
                  },
                  "type": "object"
                }
              }
            },
            "description": ""
          },
          "400": {
            "content": {
              "application/json": {
                "schema": {
                  "$ref": "#/components/schemas/ErrorResponse"
                }
              }
            },
            "description": ""
          }
        },
        "summary": "List all ItemPrototypes",
        "tags": [
          "Query"
        ]
      }
    },
    "/api/v1/game/move-player": {
      "post": {
        "operationId": "move_player",
        "requestBody": {
          "content": {
            "application/json": {
              "schema": {
                "$ref": "#/components/schemas/MovePlayerBody"
              }
            }
          },
          "required": true
        },
        "responses": {
          "200": {
            "content": {
              "application/json": {
                "schema": {
                  "$ref": "#/components/schemas/FactorioPlayer"
                }
              }
            },
            "description": ""
          },
          "400": {
            "content": {
              "application/json": {
                "schema": {
                  "$ref": "#/components/schemas/ErrorResponse"
                }
              }
            },
            "description": ""
          }
        },
        "summary": "Move player to position",
        "tags": [
          "Control"
        ]
      }
    },
    "/api/v1/game/place-entity": {
      "post": {
        "operationId": "place_entity",
        "requestBody": {
          "content": {
            "application/json": {
              "schema": {
                "$ref": "#/components/schemas/PlaceEntityBody"
              }
            }
          },
          "required": true
        },
        "responses": {
          "200": {
            "content": {
              "application/json": {
                "schema": {
                  "$ref": "#/components/schemas/PlaceEntityResult"
                }
              }
            },
            "description": ""
          },
          "400": {
            "content": {
              "application/json": {
                "schema": {
                  "$ref": "#/components/schemas/ErrorResponse"
                }
              }
            },
            "description": ""
          }
        },
        "summary": "Place entity by given player",
        "tags": [
          "Control"
        ]
      }
    },
    "/api/v1/game/plan-path": {
      "get": {
        "operationId": "plan_path",
        "parameters": [
          {
            "in": "query",
            "name": "entity_name",
            "required": true,
            "schema": {
              "type": "string"
            }
          },
          {
            "in": "query",
            "name": "entity_type",
            "required": true,
            "schema": {
              "type": "string"
            }
          },
          {
            "in": "query",
            "name": "underground_entity_name",
            "required": true,
            "schema": {
              "type": "string"
            }
          },
          {
            "in": "query",
            "name": "underground_entity_type",
            "required": true,
            "schema": {
              "type": "string"
            }
          },
          {
            "in": "query",
            "name": "underground_max",
            "required": true,
            "schema": {
              "format": "int32",
              "minimum": 0,
              "type": "integer"
            }
          },
          {
            "in": "query",
            "name": "from_position",
            "required": true,
            "schema": {
              "type": "string"
            }
          },
          {
            "in": "query",
            "name": "to_position",
            "required": true,
            "schema": {
              "type": "string"
            }
          },
          {
            "in": "query",
            "name": "to_direction",
            "required": true,
            "schema": {
              "format": "int32",
              "minimum": 0,
              "type": "integer"
            }
          }
        ],
        "responses": {
          "200": {
            "content": {
              "application/json": {
                "schema": {
                  "items": {
                    "$ref": "#/components/schemas/FactorioEntity"
                  },
                  "type": "array"
                }
              }
            },
            "description": ""
          },
          "400": {
            "content": {
              "application/json": {
                "schema": {
                  "$ref": "#/components/schemas/ErrorResponse"
                }
              }
            },
            "description": ""
          }
        },
        "summary": "Plan path from one position to another",
        "tags": [
          "Query"
        ]
      }
    },
    "/api/v1/game/player-info": {
      "get": {
        "operationId": "player_info",
        "parameters": [
          {
            "in": "query",
            "name": "player_id",
            "required": true,
            "schema": {
              "$ref": "#/components/schemas/u8"
            }
          }
        ],
        "responses": {
          "200": {
            "content": {
              "application/json": {
                "schema": {
                  "$ref": "#/components/schemas/FactorioPlayer"
                }
              }
            },
            "description": ""
          },
          "400": {
            "content": {
              "application/json": {
                "schema": {
                  "$ref": "#/components/schemas/ErrorResponse"
                }
              }
            },
            "description": ""
          }
        },
        "summary": "Player Information",
        "tags": [
          "Query"
        ]
      }
    },
    "/api/v1/game/remove-from-inventory": {
      "post": {
        "operationId": "remove_from_inventory",
        "requestBody": {
          "content": {
            "application/json": {
              "schema": {
                "$ref": "#/components/schemas/RemoveFromInventoryBody"
              }
            }
          },
          "required": true
        },
        "responses": {
          "200": {
            "content": {
              "application/json": {
                "schema": {
                  "$ref": "#/components/schemas/FactorioPlayer"
                }
              }
            },
            "description": ""
          },
          "400": {
            "content": {
              "application/json": {
                "schema": {
                  "$ref": "#/components/schemas/ErrorResponse"
                }
              }
            },
            "description": ""
          }
        },
        "summary": "Remove items from inventory",
        "tags": [
          "Control"
        ]
      }
    },
    "/api/v1/game/server-save": {
      "post": {
        "operationId": "server_save",
        "responses": {
          "200": {
            "content": {
              "application/json": {
                "schema": {
                  "$ref": "#/components/schemas/OperationResult"
                }
              }
            },
            "description": ""
          },
          "400": {
            "content": {
              "application/json": {
                "schema": {
                  "$ref": "#/components/schemas/ErrorResponse"
                }
              }
            },
            "description": ""
          }
        },
        "summary": "Server Save",
        "tags": [
          "Control"
        ]
      }
    },
    "/api/v1/instance": {
      "get": {
        "operationId": "get_instance",
        "responses": {
          "200": {
            "content": {
              "application/json": {
                "schema": {
                  "$ref": "#/components/schemas/InstanceStatus"
                }
              }
            },
            "description": ""
          }
        },
        "summary": "Reports whether a Factorio instance is running",
        "tags": [
          "Admin"
        ]
      }
    },
    "/api/v1/instance/start": {
      "post": {
        "description": "Answers `202` and never blocks: `FactorioInstance::start` runs the\narchive extraction on first use, which takes 8-10 minutes, and a browser\n(or any proxy in front of it) would time out long before that. Progress is\nobserved through `GET /api/v1/instance`.",
        "operationId": "start_instance",
        "responses": {
          "202": {
            "content": {
              "application/json": {
                "schema": {
                  "$ref": "#/components/schemas/StartAccepted"
                }
              }
            },
            "description": ""
          },
          "400": {
            "content": {
              "application/json": {
                "schema": {
                  "$ref": "#/components/schemas/ErrorResponse"
                }
              }
            },
            "description": ""
          },
          "409": {
            "content": {
              "application/json": {
                "schema": {
                  "$ref": "#/components/schemas/ErrorResponse"
                }
              }
            },
            "description": ""
          },
          "500": {
            "content": {
              "application/json": {
                "schema": {
                  "$ref": "#/components/schemas/ErrorResponse"
                }
              }
            },
            "description": ""
          }
        },
        "summary": "Starts a Factorio instance in the background.",
        "tags": [
          "Admin"
        ]
      }
    },
    "/api/v1/instance/stop": {
      "post": {
        "operationId": "stop_instance",
        "responses": {
          "204": {
            "description": ""
          },
          "400": {
            "content": {
              "application/json": {
                "schema": {
                  "$ref": "#/components/schemas/ErrorResponse"
                }
              }
            },
            "description": ""
          },
          "500": {
            "content": {
              "application/json": {
                "schema": {
                  "$ref": "#/components/schemas/ErrorResponse"
                }
              }
            },
            "description": ""
          }
        },
        "summary": "Stops the running Factorio instance",
        "tags": [
          "Admin"
        ]
      }
    },
    "/api/v1/jobs": {
      "get": {
        "operationId": "list_jobs",
        "responses": {
          "200": {
            "content": {
              "application/json": {
                "schema": {
                  "items": {
                    "$ref": "#/components/schemas/Job"
                  },
                  "type": "array"
                }
              }
            },
            "description": ""
          }
        },
        "summary": "Lists every remembered script run, newest first",
        "tags": [
          "Admin"
        ]
      }
    },
    "/api/v1/jobs/{id}": {
      "get": {
        "description": "An id that does not parse is a `404` rather than a `400`: from the caller's\nside \"job `abc`\" and \"job `9999`\" are the same mistake, and there is nothing\nuseful to say about the difference.",
        "operationId": "get_job",
        "parameters": [
          {
            "description": "Job id, as returned by the execute endpoint",
            "in": "path",
            "name": "id",
            "required": true,
            "schema": {
              "type": "string"
            }
          }
        ],
        "responses": {
          "200": {
            "content": {
              "application/json": {
                "schema": {
                  "$ref": "#/components/schemas/Job"
                }
              }
            },
            "description": ""
          },
          "404": {
            "content": {
              "application/json": {
                "schema": {
                  "$ref": "#/components/schemas/ErrorResponse"
                }
              }
            },
            "description": ""
          }
        },
        "summary": "Reports one script run",
        "tags": [
          "Admin"
        ]
      }
    },
    "/api/v1/jobs/{id}/events": {
      "get": {
        "description": "Carries the script's output only -- not the Factorio server process's\nstdout, which belongs to the instance rather than to any one job.\n\nA subscriber that attaches late is not punished for it: the job's buffered\noutput is replayed first, and a job that already finished gets that\nbacklog, a `finished` event and end-of-stream. That case is the common one\n-- a fast script is over before a browser can open the stream -- and it is\nwhy the handler goes through [`crate::jobs::JobRegistry::attach`], whose\n`None` means \"no such job\" and nothing else. Reading a missing\n*subscription* as `404` would fail almost every real request.",
        "operationId": "job_events",
        "parameters": [
          {
            "description": "Job id, as returned by the execute endpoint",
            "in": "path",
            "name": "id",
            "required": true,
            "schema": {
              "type": "string"
            }
          }
        ],
        "responses": {
          "200": {
            "content": {
              "text/event-stream": {}
            },
            "description": "Server-Sent Events: `output`, `lagged`, an optional `replay` (the run's replay document, verbatim JSON in `data:`), then a terminal `finished`"
          },
          "404": {
            "content": {
              "application/json": {
                "schema": {
                  "$ref": "#/components/schemas/ErrorResponse"
                }
              }
            },
            "description": ""
          }
        },
        "summary": "Streams one script run's output as Server-Sent Events",
        "tags": [
          "Admin"
        ]
      }
    },
    "/api/v1/rcon": {
      "post": {
        "operationId": "send_rcon",
        "requestBody": {
          "content": {
            "application/json": {
              "schema": {
                "$ref": "#/components/schemas/RconBody"
              }
            }
          },
          "required": true
        },
        "responses": {
          "204": {
            "description": ""
          },
          "400": {
            "content": {
              "application/json": {
                "schema": {
                  "$ref": "#/components/schemas/ErrorResponse"
                }
              }
            },
            "description": ""
          }
        },
        "summary": "Sends a raw RCON command to the running Factorio server",
        "tags": [
          "Admin"
        ]
      }
    },
    "/api/v1/runs": {
      "get": {
        "operationId": "list_runs",
        "responses": {
          "200": {
            "content": {
              "application/json": {
                "schema": {
                  "$ref": "#/components/schemas/RunsResponse"
                }
              }
            },
            "description": "archived runs, newest first"
          },
          "400": {
            "content": {
              "application/json": {
                "schema": {
                  "$ref": "#/components/schemas/ErrorResponse"
                }
              }
            },
            "description": ""
          }
        },
        "summary": "Lists archived runs.",
        "tags": [
          "Runs"
        ]
      }
    },
    "/api/v1/runs/{id}": {
      "get": {
        "operationId": "get_run",
        "parameters": [
          {
            "description": "the run id",
            "in": "path",
            "name": "id",
            "required": true,
            "schema": {
              "type": "string"
            }
          }
        ],
        "responses": {
          "200": {
            "content": {
              "application/json": {
                "schema": {
                  "$ref": "#/components/schemas/RunDetail"
                }
              }
            },
            "description": ""
          },
          "400": {
            "content": {
              "application/json": {
                "schema": {
                  "$ref": "#/components/schemas/ErrorResponse"
                }
              }
            },
            "description": ""
          },
          "404": {
            "content": {
              "application/json": {
                "schema": {
                  "$ref": "#/components/schemas/ErrorResponse"
                }
              }
            },
            "description": ""
          }
        },
        "summary": "One run's summary and splits.",
        "tags": [
          "Runs"
        ]
      }
    },
    "/api/v1/runs/{id}/events": {
      "get": {
        "operationId": "get_run_events",
        "parameters": [
          {
            "description": "the run id",
            "in": "path",
            "name": "id",
            "required": true,
            "schema": {
              "type": "string"
            }
          },
          {
            "description": "Return only events of this `kind`.",
            "in": "query",
            "name": "kind",
            "required": false,
            "schema": {
              "type": [
                "string",
                "null"
              ]
            }
          }
        ],
        "responses": {
          "200": {
            "content": {
              "application/json": {
                "schema": {
                  "$ref": "#/components/schemas/EventsResponse"
                }
              }
            },
            "description": ""
          },
          "400": {
            "content": {
              "application/json": {
                "schema": {
                  "$ref": "#/components/schemas/ErrorResponse"
                }
              }
            },
            "description": ""
          },
          "404": {
            "content": {
              "application/json": {
                "schema": {
                  "$ref": "#/components/schemas/ErrorResponse"
                }
              }
            },
            "description": ""
          }
        },
        "summary": "A run's event log.",
        "tags": [
          "Runs"
        ]
      }
    },
    "/api/v1/runs/{id}/flow": {
      "get": {
        "description": "A run recorded before this feature existed, or one that never reached a\nkeyframe, answers an empty list, not a 404 -- matching `get_run_map`: an\nabsent capability and a present-but-empty one read the same here, and the\npage already treats an empty flow list as \"nothing to draw yet\".",
        "operationId": "get_run_flow",
        "parameters": [
          {
            "description": "the run id",
            "in": "path",
            "name": "id",
            "required": true,
            "schema": {
              "type": "string"
            }
          },
          {
            "description": "First tick to include.",
            "in": "query",
            "name": "from",
            "required": false,
            "schema": {
              "format": "int64",
              "minimum": 0,
              "type": [
                "integer",
                "null"
              ]
            }
          },
          {
            "description": "Last tick to include.",
            "in": "query",
            "name": "to",
            "required": false,
            "schema": {
              "format": "int64",
              "minimum": 0,
              "type": [
                "integer",
                "null"
              ]
            }
          }
        ],
        "responses": {
          "200": {
            "content": {
              "application/json": {
                "schema": {
                  "$ref": "#/components/schemas/RunFlowResponse"
                }
              }
            },
            "description": ""
          },
          "400": {
            "content": {
              "application/json": {
                "schema": {
                  "$ref": "#/components/schemas/ErrorResponse"
                }
              }
            },
            "description": ""
          },
          "404": {
            "content": {
              "application/json": {
                "schema": {
                  "$ref": "#/components/schemas/ErrorResponse"
                }
              }
            },
            "description": ""
          }
        },
        "summary": "One archived run's flow-graph keyframes -- one per milestone boundary,\nplus its opening one. Each entry is a full snapshot (unlike `map.jsonl`,\nnothing here reconstructs from a base plus deltas), so a caller wanting\nthe graph nearest a cursor tick picks the latest entry at or before it.",
        "tags": [
          "Runs"
        ]
      }
    },
    "/api/v1/runs/{id}/lanes": {
      "get": {
        "operationId": "get_run_lanes",
        "parameters": [
          {
            "description": "the run id",
            "in": "path",
            "name": "id",
            "required": true,
            "schema": {
              "type": "string"
            }
          }
        ],
        "responses": {
          "200": {
            "content": {
              "application/json": {
                "schema": {
                  "$ref": "#/components/schemas/RunLanesResponse"
                }
              }
            },
            "description": ""
          },
          "400": {
            "content": {
              "application/json": {
                "schema": {
                  "$ref": "#/components/schemas/ErrorResponse"
                }
              }
            },
            "description": ""
          },
          "404": {
            "content": {
              "application/json": {
                "schema": {
                  "$ref": "#/components/schemas/ErrorResponse"
                }
              }
            },
            "description": ""
          }
        },
        "summary": "What each bot did, derived from the run's event log.",
        "tags": [
          "Runs"
        ]
      }
    },
    "/api/v1/runs/{id}/map": {
      "get": {
        "operationId": "get_run_map",
        "parameters": [
          {
            "description": "the run id",
            "in": "path",
            "name": "id",
            "required": true,
            "schema": {
              "type": "string"
            }
          },
          {
            "description": "First tick to include.",
            "in": "query",
            "name": "from",
            "required": false,
            "schema": {
              "format": "int64",
              "minimum": 0,
              "type": [
                "integer",
                "null"
              ]
            }
          },
          {
            "description": "Last tick to include.",
            "in": "query",
            "name": "to",
            "required": false,
            "schema": {
              "format": "int64",
              "minimum": 0,
              "type": [
                "integer",
                "null"
              ]
            }
          }
        ],
        "responses": {
          "200": {
            "content": {
              "application/json": {
                "schema": {
                  "$ref": "#/components/schemas/RunMapResponse"
                }
              }
            },
            "description": ""
          },
          "400": {
            "content": {
              "application/json": {
                "schema": {
                  "$ref": "#/components/schemas/ErrorResponse"
                }
              }
            },
            "description": ""
          },
          "404": {
            "content": {
              "application/json": {
                "schema": {
                  "$ref": "#/components/schemas/ErrorResponse"
                }
              }
            },
            "description": ""
          }
        },
        "summary": "A run's entity map: what got built, and whether the game agreed.",
        "tags": [
          "Runs"
        ]
      }
    },
    "/api/v1/runs/{id}/provenance": {
      "get": {
        "description": "A 404, not an empty object, when `provenance.json` is missing: the file is\nwritten at run *start* since 2026-09-06, so its absence means an older run\nand the client must show \"not captured\", never a default.",
        "operationId": "get_run_provenance",
        "parameters": [
          {
            "description": "the run id",
            "in": "path",
            "name": "id",
            "required": true,
            "schema": {
              "type": "string"
            }
          }
        ],
        "responses": {
          "200": {
            "content": {
              "application/json": {
                "schema": {
                  "$ref": "#/components/schemas/Provenance"
                }
              }
            },
            "description": ""
          },
          "400": {
            "content": {
              "application/json": {
                "schema": {
                  "$ref": "#/components/schemas/ErrorResponse"
                }
              }
            },
            "description": ""
          },
          "404": {
            "content": {
              "application/json": {
                "schema": {
                  "$ref": "#/components/schemas/ErrorResponse"
                }
              }
            },
            "description": ""
          }
        },
        "summary": "What a run was launched with -- the fields that decide whether two runs\nmay be compared at all.",
        "tags": [
          "Runs"
        ]
      }
    },
    "/api/v1/runs/{id}/replay": {
      "get": {
        "description": "Written by the run at its end since Phase 2 of Run Anatomy; a run archived\nbefore that, or one that never ran a plan, has none, and that is a 404\nrather than an empty document: an empty replay would read as a run that\nplanned nothing.",
        "operationId": "get_run_replay",
        "parameters": [
          {
            "description": "the run id",
            "in": "path",
            "name": "id",
            "required": true,
            "schema": {
              "type": "string"
            }
          }
        ],
        "responses": {
          "200": {
            "content": {
              "application/json": {
                "schema": {}
              }
            },
            "description": "the replay document, as the executor serialised it"
          },
          "400": {
            "content": {
              "application/json": {
                "schema": {
                  "$ref": "#/components/schemas/ErrorResponse"
                }
              }
            },
            "description": ""
          },
          "404": {
            "content": {
              "application/json": {
                "schema": {
                  "$ref": "#/components/schemas/ErrorResponse"
                }
              }
            },
            "description": ""
          }
        },
        "summary": "The executor's replay -- planned against observed per step, with evidence.",
        "tags": [
          "Runs"
        ]
      }
    },
    "/api/v1/runs/{id}/samples": {
      "get": {
        "operationId": "get_run_samples",
        "parameters": [
          {
            "description": "the run id",
            "in": "path",
            "name": "id",
            "required": true,
            "schema": {
              "type": "string"
            }
          },
          {
            "description": "First tick to include.",
            "in": "query",
            "name": "from",
            "required": false,
            "schema": {
              "format": "int64",
              "minimum": 0,
              "type": [
                "integer",
                "null"
              ]
            }
          },
          {
            "description": "Last tick to include.",
            "in": "query",
            "name": "to",
            "required": false,
            "schema": {
              "format": "int64",
              "minimum": 0,
              "type": [
                "integer",
                "null"
              ]
            }
          },
          {
            "description": "Return only samples of this `kind`.",
            "in": "query",
            "name": "kind",
            "required": false,
            "schema": {
              "type": [
                "string",
                "null"
              ]
            }
          }
        ],
        "responses": {
          "200": {
            "content": {
              "application/json": {
                "schema": {
                  "$ref": "#/components/schemas/RunSamplesResponse"
                }
              }
            },
            "description": ""
          },
          "400": {
            "content": {
              "application/json": {
                "schema": {
                  "$ref": "#/components/schemas/ErrorResponse"
                }
              }
            },
            "description": ""
          },
          "404": {
            "content": {
              "application/json": {
                "schema": {
                  "$ref": "#/components/schemas/ErrorResponse"
                }
              }
            },
            "description": ""
          }
        },
        "summary": "A run's world-state samples.",
        "tags": [
          "Runs"
        ]
      }
    },
    "/api/v1/runs/{id}/savepoints": {
      "get": {
        "operationId": "get_run_savepoints",
        "parameters": [
          {
            "description": "the run id",
            "in": "path",
            "name": "id",
            "required": true,
            "schema": {
              "type": "string"
            }
          }
        ],
        "responses": {
          "200": {
            "content": {
              "application/json": {
                "schema": {
                  "$ref": "#/components/schemas/RunSavepointsResponse"
                }
              }
            },
            "description": ""
          },
          "400": {
            "content": {
              "application/json": {
                "schema": {
                  "$ref": "#/components/schemas/ErrorResponse"
                }
              }
            },
            "description": ""
          },
          "404": {
            "content": {
              "application/json": {
                "schema": {
                  "$ref": "#/components/schemas/ErrorResponse"
                }
              }
            },
            "description": ""
          }
        },
        "summary": "The milestone savepoints a run wrote.",
        "tags": [
          "Runs"
        ]
      }
    },
    "/api/v1/runs/{id}/video": {
      "get": {
        "description": "A run recorded before video existed -- or one that never asked for it, which\nis every run by default -- answers an empty manifest, not a 404. Video is\nopt-in, so \"there is none\" is the ordinary case and must be distinguishable\nfrom a route that is not there.",
        "operationId": "get_run_video",
        "parameters": [
          {
            "description": "the run id",
            "in": "path",
            "name": "id",
            "required": true,
            "schema": {
              "type": "string"
            }
          }
        ],
        "responses": {
          "200": {
            "content": {
              "application/json": {
                "schema": {
                  "$ref": "#/components/schemas/VideoManifest"
                }
              }
            },
            "description": ""
          },
          "400": {
            "content": {
              "application/json": {
                "schema": {
                  "$ref": "#/components/schemas/ErrorResponse"
                }
              }
            },
            "description": ""
          },
          "404": {
            "content": {
              "application/json": {
                "schema": {
                  "$ref": "#/components/schemas/ErrorResponse"
                }
              }
            },
            "description": ""
          }
        },
        "summary": "A run's archived video manifest.",
        "tags": [
          "Runs"
        ]
      }
    },
    "/api/v1/runs/{id}/video/file": {
      "get": {
        "description": "Cached `immutable`, unlike the live route: this run is over, so these bytes\nnever change. The live `<workspace>/video/video.mp4` is overwritten by the\nnext run at the same URL and must not be.",
        "operationId": "get_run_video_file",
        "parameters": [
          {
            "description": "the run id",
            "in": "path",
            "name": "id",
            "required": true,
            "schema": {
              "type": "string"
            }
          }
        ],
        "responses": {
          "200": {
            "content": {
              "video/mp4": {}
            },
            "description": "the whole recording"
          },
          "206": {
            "content": {
              "video/mp4": {}
            },
            "description": "a byte range, for seeking"
          },
          "400": {
            "content": {
              "application/json": {
                "schema": {
                  "$ref": "#/components/schemas/ErrorResponse"
                }
              }
            },
            "description": ""
          },
          "404": {
            "content": {
              "application/json": {
                "schema": {
                  "$ref": "#/components/schemas/ErrorResponse"
                }
              }
            },
            "description": ""
          }
        },
        "summary": "One archived recording's bytes, with range support.",
        "tags": [
          "Runs"
        ]
      }
    },
    "/api/v1/runs/{id}/video/ticks": {
      "get": {
        "operationId": "get_run_video_ticks",
        "parameters": [
          {
            "description": "the run id",
            "in": "path",
            "name": "id",
            "required": true,
            "schema": {
              "type": "string"
            }
          }
        ],
        "responses": {
          "200": {
            "content": {
              "application/json": {
                "schema": {
                  "$ref": "#/components/schemas/VideoTicksResponse"
                }
              }
            },
            "description": ""
          },
          "400": {
            "content": {
              "application/json": {
                "schema": {
                  "$ref": "#/components/schemas/ErrorResponse"
                }
              }
            },
            "description": ""
          },
          "404": {
            "content": {
              "application/json": {
                "schema": {
                  "$ref": "#/components/schemas/ErrorResponse"
                }
              }
            },
            "description": ""
          }
        },
        "summary": "A run's archived video clock.",
        "tags": [
          "Runs"
        ]
      }
    },
    "/api/v1/scripts": {
      "get": {
        "operationId": "list_scripts",
        "parameters": [
          {
            "description": "Slash-separated path relative to the scripts root, e.g. `/` or\n`/sub/example.lua`. A leading slash is optional.",
            "in": "query",
            "name": "path",
            "required": true,
            "schema": {
              "type": "string"
            }
          }
        ],
        "responses": {
          "200": {
            "content": {
              "application/json": {
                "schema": {
                  "items": {
                    "$ref": "#/components/schemas/ScriptTreeNode"
                  },
                  "type": "array"
                }
              }
            },
            "description": ""
          },
          "400": {
            "content": {
              "application/json": {
                "schema": {
                  "$ref": "#/components/schemas/ErrorResponse"
                }
              }
            },
            "description": ""
          },
          "404": {
            "content": {
              "application/json": {
                "schema": {
                  "$ref": "#/components/schemas/ErrorResponse"
                }
              }
            },
            "description": ""
          },
          "500": {
            "content": {
              "application/json": {
                "schema": {
                  "$ref": "#/components/schemas/ErrorResponse"
                }
              }
            },
            "description": ""
          }
        },
        "summary": "Lists a directory under the scripts root as a script tree",
        "tags": [
          "Admin"
        ]
      }
    },
    "/api/v1/scripts/execute": {
      "post": {
        "description": "The order of the checks below is the contract, not an accident:\n\n1. the body (`400`) -- nothing else is knowable until it parses;\n2. a running Factorio instance (`503`) -- the server is fine, the game is\n   not, which is not the caller's fault;\n3. the script itself (`404` missing, `400` outside the scripts root);\n4. and only then the single execution slot (`409`).\n\nTaking the slot before a check that can still fail would leave it held by a\nrequest that never runs, and the slot is single: every later execution would\nanswer `409` until the process restarts. [`JobHandle`]'s `Drop` frees it on\nany exit path once it *is* taken, so the happy path is not what keeps this\nsafe -- but not taking it early is cheaper than relying on that.",
        "operationId": "post_execute",
        "requestBody": {
          "content": {
            "application/json": {
              "schema": {
                "$ref": "#/components/schemas/ExecuteRequest"
              }
            }
          },
          "required": true
        },
        "responses": {
          "202": {
            "content": {
              "application/json": {
                "schema": {
                  "$ref": "#/components/schemas/ExecuteAccepted"
                }
              }
            },
            "description": "Execution started"
          },
          "400": {
            "content": {
              "application/json": {
                "schema": {
                  "$ref": "#/components/schemas/ErrorResponse"
                }
              }
            },
            "description": "Bad request"
          },
          "404": {
            "content": {
              "application/json": {
                "schema": {
                  "$ref": "#/components/schemas/ErrorResponse"
                }
              }
            },
            "description": "Script not found"
          },
          "409": {
            "content": {
              "application/json": {
                "schema": {
                  "$ref": "#/components/schemas/ErrorResponse"
                }
              }
            },
            "description": "A script is already running"
          },
          "503": {
            "content": {
              "application/json": {
                "schema": {
                  "$ref": "#/components/schemas/ErrorResponse"
                }
              }
            },
            "description": "No running Factorio instance"
          }
        },
        "summary": "Starts a script and answers immediately with the job that runs it",
        "tags": [
          "Admin"
        ]
      }
    },
    "/api/v1/scripts/file": {
      "delete": {
        "operationId": "delete_script",
        "parameters": [
          {
            "description": "Slash-separated path relative to the scripts root, e.g. `/` or\n`/sub/example.lua`. A leading slash is optional.",
            "in": "query",
            "name": "path",
            "required": true,
            "schema": {
              "type": "string"
            }
          }
        ],
        "responses": {
          "204": {
            "description": ""
          },
          "400": {
            "content": {
              "application/json": {
                "schema": {
                  "$ref": "#/components/schemas/ErrorResponse"
                }
              }
            },
            "description": ""
          },
          "404": {
            "content": {
              "application/json": {
                "schema": {
                  "$ref": "#/components/schemas/ErrorResponse"
                }
              }
            },
            "description": ""
          },
          "500": {
            "content": {
              "application/json": {
                "schema": {
                  "$ref": "#/components/schemas/ErrorResponse"
                }
              }
            },
            "description": ""
          }
        },
        "summary": "Deletes an existing script file. Deleting a directory is not supported.",
        "tags": [
          "Admin"
        ]
      },
      "get": {
        "operationId": "read_script",
        "parameters": [
          {
            "description": "Slash-separated path relative to the scripts root, e.g. `/` or\n`/sub/example.lua`. A leading slash is optional.",
            "in": "query",
            "name": "path",
            "required": true,
            "schema": {
              "type": "string"
            }
          }
        ],
        "responses": {
          "200": {
            "content": {
              "application/json": {
                "schema": {
                  "$ref": "#/components/schemas/ScriptContent"
                }
              }
            },
            "description": ""
          },
          "400": {
            "content": {
              "application/json": {
                "schema": {
                  "$ref": "#/components/schemas/ErrorResponse"
                }
              }
            },
            "description": ""
          },
          "404": {
            "content": {
              "application/json": {
                "schema": {
                  "$ref": "#/components/schemas/ErrorResponse"
                }
              }
            },
            "description": ""
          },
          "500": {
            "content": {
              "application/json": {
                "schema": {
                  "$ref": "#/components/schemas/ErrorResponse"
                }
              }
            },
            "description": ""
          }
        },
        "summary": "Reads the contents of a script file",
        "tags": [
          "Admin"
        ]
      },
      "post": {
        "description": "Deliberately does not check `target.exists()` and then call\n`std::fs::write`: `exists()` follows symlinks and reports `false` for a\n*dangling* one, and `std::fs::write` (via `File::create`) also follows\nsymlinks when opening. A dangling symlink planted inside the scripts\nroot pointing outside it (e.g. `sub/evil.lua -> /etc/evil.lua`, where\n`/etc/evil.lua` does not exist yet) would therefore pass the `exists()`\ncheck and then have its *target* created and written by `fs::write` —\na write outside the scripts root that neither `resolve_new_script_path`\nnor an `exists()` check catches, since neither one ever has reason to\nlook at `evil.lua` once the parent has resolved. `OpenOptions::create_new`\navoids this: POSIX `open` with `O_CREAT | O_EXCL` fails with `EEXIST`\nwhen the final path component is a symlink, dangling or not, so this\nnever follows one to create a file. It also removes the\ncheck-then-write TOCTOU race between the `exists()` check and the write.",
        "operationId": "create_script",
        "parameters": [
          {
            "description": "Slash-separated path relative to the scripts root, e.g. `/` or\n`/sub/example.lua`. A leading slash is optional.",
            "in": "query",
            "name": "path",
            "required": true,
            "schema": {
              "type": "string"
            }
          }
        ],
        "requestBody": {
          "content": {
            "application/json": {
              "schema": {
                "$ref": "#/components/schemas/ScriptContent"
              }
            }
          },
          "required": true
        },
        "responses": {
          "201": {
            "description": ""
          },
          "400": {
            "content": {
              "application/json": {
                "schema": {
                  "$ref": "#/components/schemas/ErrorResponse"
                }
              }
            },
            "description": ""
          },
          "404": {
            "content": {
              "application/json": {
                "schema": {
                  "$ref": "#/components/schemas/ErrorResponse"
                }
              }
            },
            "description": ""
          },
          "409": {
            "content": {
              "application/json": {
                "schema": {
                  "$ref": "#/components/schemas/ErrorResponse"
                }
              }
            },
            "description": ""
          },
          "500": {
            "content": {
              "application/json": {
                "schema": {
                  "$ref": "#/components/schemas/ErrorResponse"
                }
              }
            },
            "description": ""
          }
        },
        "summary": "Creates a new script file. Rejects the request if the target already\nexists — use `PUT` to overwrite an existing script instead.",
        "tags": [
          "Admin"
        ]
      },
      "put": {
        "description": "Mirrors the original desktop command's behavior: this only writes to a\nfile that already exists under the scripts root. Creating new scripts is\nnot supported here; that would be a separate decision.",
        "operationId": "write_script",
        "parameters": [
          {
            "description": "Slash-separated path relative to the scripts root, e.g. `/` or\n`/sub/example.lua`. A leading slash is optional.",
            "in": "query",
            "name": "path",
            "required": true,
            "schema": {
              "type": "string"
            }
          }
        ],
        "requestBody": {
          "content": {
            "application/json": {
              "schema": {
                "$ref": "#/components/schemas/ScriptContent"
              }
            }
          },
          "required": true
        },
        "responses": {
          "204": {
            "description": ""
          },
          "400": {
            "content": {
              "application/json": {
                "schema": {
                  "$ref": "#/components/schemas/ErrorResponse"
                }
              }
            },
            "description": ""
          },
          "404": {
            "content": {
              "application/json": {
                "schema": {
                  "$ref": "#/components/schemas/ErrorResponse"
                }
              }
            },
            "description": ""
          },
          "500": {
            "content": {
              "application/json": {
                "schema": {
                  "$ref": "#/components/schemas/ErrorResponse"
                }
              }
            },
            "description": ""
          }
        },
        "summary": "Overwrites the contents of an existing script file",
        "tags": [
          "Admin"
        ]
      }
    },
    "/api/v1/settings": {
      "get": {
        "operationId": "get_settings",
        "responses": {
          "200": {
            "content": {
              "application/json": {
                "schema": {
                  "$ref": "#/components/schemas/AppSettings"
                }
              }
            },
            "description": ""
          }
        },
        "summary": "Returns the current application settings",
        "tags": [
          "Admin"
        ]
      },
      "put": {
        "operationId": "put_settings",
        "requestBody": {
          "content": {
            "application/json": {
              "schema": {
                "$ref": "#/components/schemas/AppSettings"
              }
            }
          },
          "required": true
        },
        "responses": {
          "200": {
            "content": {
              "application/json": {
                "schema": {
                  "$ref": "#/components/schemas/AppSettings"
                }
              }
            },
            "description": ""
          },
          "400": {
            "content": {
              "application/json": {
                "schema": {
                  "$ref": "#/components/schemas/ErrorResponse"
                }
              }
            },
            "description": ""
          },
          "500": {
            "content": {
              "application/json": {
                "schema": {
                  "$ref": "#/components/schemas/ErrorResponse"
                }
              }
            },
            "description": ""
          }
        },
        "summary": "Replaces the application settings and persists them",
        "tags": [
          "Admin"
        ]
      }
    },
    "/api/v1/video": {
      "get": {
        "description": "Answers a manifest describing *nothing* rather than a 404 when no video was\nrecorded: \"this run had no video\" is the ordinary state -- video is opt-in\n-- and a 404 would make a caller unable to tell it from a broken route.",
        "operationId": "get_video",
        "responses": {
          "200": {
            "content": {
              "application/json": {
                "schema": {
                  "$ref": "#/components/schemas/VideoManifest"
                }
              }
            },
            "description": ""
          },
          "400": {
            "content": {
              "application/json": {
                "schema": {
                  "$ref": "#/components/schemas/ErrorResponse"
                }
              }
            },
            "description": ""
          }
        },
        "summary": "The manifest of the current run's recording.",
        "tags": [
          "Admin"
        ]
      }
    },
    "/api/v1/video/file": {
      "get": {
        "description": "**`Cache-Control: no-cache`, deliberately.** `<workspace>/video/video.mp4`\nis *overwritten by the next run*, at the same URL. Serving it `immutable`\nwould show a browser the previous run's recording beside this run's\ntimeline, and nothing would say so. The archived route\n(`/api/v1/runs/{id}/video/file`) is the one whose bytes never change, and it\nis cached accordingly.",
        "operationId": "get_video_file",
        "responses": {
          "200": {
            "content": {
              "video/mp4": {}
            },
            "description": "the whole recording"
          },
          "206": {
            "content": {
              "video/mp4": {}
            },
            "description": "a byte range, for seeking"
          },
          "400": {
            "content": {
              "application/json": {
                "schema": {
                  "$ref": "#/components/schemas/ErrorResponse"
                }
              }
            },
            "description": ""
          },
          "404": {
            "content": {
              "application/json": {
                "schema": {
                  "$ref": "#/components/schemas/ErrorResponse"
                }
              }
            },
            "description": ""
          }
        },
        "summary": "Serves the recording's bytes, with range support.",
        "tags": [
          "Admin"
        ]
      }
    },
    "/api/v1/video/ticks": {
      "get": {
        "operationId": "get_video_ticks",
        "responses": {
          "200": {
            "content": {
              "application/json": {
                "schema": {
                  "$ref": "#/components/schemas/VideoTicksResponse"
                }
              }
            },
            "description": ""
          },
          "400": {
            "content": {
              "application/json": {
                "schema": {
                  "$ref": "#/components/schemas/ErrorResponse"
                }
              }
            },
            "description": ""
          }
        },
        "summary": "The clock: every `(tick, wall_ms)` pair the recorder sampled.",
        "tags": [
          "Admin"
        ]
      }
    }
  },
  "tags": [
    {
      "description": "World queries",
      "name": "Query"
    },
    {
      "description": "Bot control",
      "name": "Control"
    },
    {
      "description": "Entity placement",
      "name": "Place"
    },
    {
      "description": "Cheats",
      "name": "Cheat"
    },
    {
      "description": "Inventory manipulation",
      "name": "Inventory"
    },
    {
      "description": "Server administration",
      "name": "Admin"
    },
    {
      "description": "Research",
      "name": "Research"
    }
  ]
}
